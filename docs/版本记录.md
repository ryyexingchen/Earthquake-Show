# Earthquake Show 版本记录

## 维护规则

本文档长期记录每个版本实际完成的小功能，是判断某个版本“已经具备什么能力”的唯一版本清单。

- 每次新增、修改或移除用户可见功能时更新本文档。
- 每个版本必须记录实现内容、明确未包含内容、验证结果和已知问题。
- 正式原生版本号统一维护于 `Directory.Build.props`，并由各 `.csproj` 和界面版本信息读取。
- 尚未实现或尚未验证的能力不得写入“已实现”。
- Bug 修复除记录在这里外，还要在[工程实现文档](./工程实现文档.md)的故障记录中说明原因和验证方法。

## 0.67.18 - 2026-08-31

### 已实现

- 预报、观测、时间线和详情标签内容统一 Stretch，占满所属详情栏宽度，避免按内容自动收缩。
- 预报区为海啸注意报且未提供数值或文字高度时显示“——”。
- 海啸详情中的发布时间、发生时间、预计到达、高潮和实测时间统一转换为 JST，并显示 `JST` 标记。
- 震源名为“不明/未知”时优先显示报文描述；解析并在摘要显示 `固定付加文`，事件报文累计时去重保留。
- 拆分预报徽标与观测高度徽标样式，修复预报/时间线对象错误绑定 `ObservationStatusText` 产生的 WPF BindingExpression 日志错误；地图过期请求的 `TaskCanceledException` 保持为正常取消路径。
- 增加解析器和 ViewModel 单元测试，覆盖固定附加文、不明震源描述、JST 转换和注意报无高度占位符。

### 本版本未包含

- 未改变 VTSE 报文身份、事件累计覆盖规则、地图 LOD 或 SQLite schema 版本。

### 验证结果

- Core 测试 108 项、Infrastructure 测试 81 项、App 测试 279 项通过；Release 构建 0 警告、0 错误。
- `git diff --check` 通过。

版本总结：统一海啸详情显示规则并修复绑定日志异常。

提交日志：`0.67.18: 统一海啸详情显示规则并修复绑定日志异常`

## 0.67.17 - 2026-08-31

### 已实现

- 支持在同一海啸事件中通过时间线、上一报和下一报切换不同原始报文。
- 当前报文详情按事件历史累计预报区、观测点、推定区域和信息项；后报重复 code 覆盖前报，未重复 code 的既有情报继续保留。
- 时间线条目可点击切换，刷新时仍保持当前来源报文身份。
- 增加三报增量继承、重复 code 更新和报文导航边界单元测试。

### 本版本未包含

- 未改变报文数据库结构、VTSE 解析规则和事件列表按 `EventId` 聚合规则。

### 验证结果

- App 测试 277 项通过；Release 构建 0 警告、0 错误。
- 全量解决方案测试通过；`git diff --check` 通过。

版本总结：支持海啸事件报文切换并累计继承未更新情报。

提交日志：`0.67.17: 支持海啸报文切换并累计继承事件情报`

## 0.67.16 - 2026-08-31

### 已实现

- 观测点表不再在状态列显示“观测到海啸”。
- 将“欠测”“观测中”“微弱”投影到实测高度列；前两者使用黑色背景，“微弱”使用海啸预报浅蓝背景。
- 同一 `EventId` 的多条海啸报文在左侧列表聚合为一条，代表记录取最新报文。
- 保留完整报文用于时间线、上一报差异和原始数据；刷新时继续保持当前精确报文选择，列表高亮使用事件代表记录。
- 增加事件代表记录与历史详情分离的单元测试。

### 本版本未包含

- 未改变 VTSE 解析、数据库存储、地图观测点过滤和时间线排序规则。

### 验证结果

- App 测试 276 项通过；Release 构建 0 警告、0 错误。
- `git diff --check` 通过。

版本总结：整合海啸事件列表并调整观测状态到实测高度显示。

提交日志：`0.67.16: 整合海啸事件列表并调整观测状态显示`

## 0.67.15 - 2026-08-31

### 已实现

- 将海啸警报 / 预报表从摘要页分离为独立的“警报 / 预报”标签页。
- 观测点表的状态列改为纯文本，不再使用“观测到海啸”彩色徽标。
- 实测高度列沿用海啸高度分级：`<0.2m` 浅蓝、`0.2–<1m` 黄色、`1–3m` 红色、`>3m` 紫色，并保持红色/紫色背景上的白色文字。
- 增加观测高度等级映射单元测试，并更新五标签 UI 结构测试。

### 本版本未包含

- 未改变观测状态文本生成、VTSE 解析、数据库、地图和观测点过滤规则。

### 验证结果

- App 测试 274 项通过；Release 构建 0 警告、0 错误。
- 固定数据校验和 `git diff --check` 通过。

版本总结：分离海啸预报标签并按等级着色观测高度、简化状态列。

提交日志：`0.67.15: 分离海啸预报标签并按等级着色观测高度`

## 0.67.14 - 2026-08-31

### 已实现

- 海啸右栏改为与地震情报页面一致的四标签切换布局：摘要、观测点、时间线、原始数据。
- 摘要页展示震源信息和独立的海啸警报 / 预报表；观测点页展示选中站点和独立的海啸情报 / 实际观测表。
- 保留观测表行选中、站点详情和地图高亮联动；原始数据页保留 XML 查看和复制功能。
- 增加 WPF STA 结构测试，验证四个标签按预期加载。

### 本版本未包含

- 未改变 VTSE 解析、ViewModel 数据投影、SQLite 持久化、观测过滤和地图绘制逻辑。

### 验证结果

- App 测试 273 项通过；Release 构建 0 警告、0 错误。
- 固定数据校验和 `git diff --check` 通过。

版本总结：将海啸右栏改为四标签切换并分别承载预报与观测表格。

提交日志：`0.67.14: 将海啸右栏改为四标签切换并分别承载预报与观测表格`

## 0.67.13 - 2026-08-31

### 已实现

- 右侧详情栏按职责拆分为“海啸警报 / 预报”和“海啸情报 / 实际观测”两个独立子栏。
- 两个子栏均改为紧凑表格：预报表展示等级、预报区、预计首波和最大高度；观测表展示区域、观测点、首波/高潮、实测高度和观测状态。
- 观测表继续保留行选中、站点详情和地图高亮联动；无观测记录时整块隐藏，只有观测没有预报时仍单独显示观测表。

### 本版本未包含

- 未改变 VTSE 解析、等级判定、观测点过滤、SQLite 持久化和地图绘制逻辑。

### 验证结果

- 新增“只有观测没有预报”单元测试，验证两个表格数据源相互独立。
- Release 测试、构建、固定数据校验和 `git diff --check` 通过。

版本总结：拆分海啸警报预报与实际观测表格并优化右栏布局。

提交日志：`0.67.13: 拆分海啸警报预报与实际观测表格并优化右栏布局`

## 0.67.12 - 2026-08-31

### 已实现

- 修复海啸报文列表刷新时 WPF `ListBox` 的瞬时空选中反向清空 ViewModel，当前报文、详情和地图不再因后台刷新消失。
- 报文列表改为单向选中同步，用户选择通过 `SelectionChanged` 显式提交，并增加刷新后保持报文身份的单元测试。
- 海啸页面调整为左侧报文列表、中间地图、右侧详情与数据表的三栏布局。

### 本版本未包含

- 未改变海啸报文接收、解析、SQLite 持久化、地图数据和等级判定逻辑。
- 未改变用户主动切换地震/海啸页面的行为。

### 验证结果

- 解决方案全量测试 `460` 项通过（Core 107、Infrastructure 81、App 272）。
- Release 构建通过，`0` 个警告、`0` 个错误；固定测试数据校验和 `git diff --check` 通过。

版本总结：修复海啸报文刷新丢失选择并调整三栏页面布局。

提交日志：`0.67.12: 修复海啸报文刷新丢失选择并调整三栏布局`

## 0.67.11 - 2026-08-29

### 已实现

- 修复用户查看海啸页面时，新地震报文到达后主窗口无条件切回地震页面的问题。
- 海啸页面可见时仍接收并更新地震数据，但不改变当前页面、海啸报文选择或地图显示；地震页面可见时保留原有新报文自动聚焦行为。
- 增加自动导航分支单元测试，覆盖海啸页面可见和不可见两种状态。

### 本版本未包含

- 未改变地震或海啸报文的接收、解析、缓存、排序和地图渲染逻辑。
- 未改变用户主动切换地震/海啸页面的导航行为。

### 验证结果

- 解决方案全量测试 `459` 项通过（Core 107、Infrastructure 81、App 271）。
- Release 构建通过，`0` 个警告、`0` 个错误；固定测试数据校验和 `git diff --check` 通过。

版本总结：修复海啸页面被新地震报文自动切走的问题。

提交日志：`0.67.11: 修复新地震报文打断海啸页面`

## 0.67.10 - 2026-08-29

### 已实现

- 海啸地图最大缩放从 `24` 调整为 `16`，保留 `13–16` 的高精度海岸线查看范围。
- App 启动时加载三份固定 `VTSE41/51/52` 训练样例，并以 `sample-jma-tsunami` 独立来源写入海啸 SQLite，确保无网络、无缓存时页面仍可演示报文、预报区、观测和地图。
- 新增固定样例加载单元测试，验证报文代码、来源标识、预报区和观测集合。

### 本版本未包含

- 未改变在线 JMA XML 来源、海啸报文结构、站点目录、预报区映射或观测点过滤规则。
- `sample-jma-tsunami` 仅用于本地演示，不代表在线来源状态；移除 seed 调用和资源链接即可恢复纯缓存行为。

### 验证结果

- 解决方案全量测试 `458` 项通过（Core 107、Infrastructure 81、App 270）。
- Release 构建通过，`0` 个警告、`0` 个错误；固定测试数据校验和 `git diff --check` 通过。

版本总结：收紧海啸地图缩放并加入离线演示样例。

提交日志：`0.67.10: 收紧海啸地图缩放并加入离线演示样例`

## 0.67.9 - 2026-08-29

### 已实现

- 海啸沿岸线按屏幕空间点距简化，普通几何使用 `0.65px`，点数密集时使用 `1px`，每条线始终保留首尾点。
- 沿岸线 `Path` 使用圆角连接和圆端点，避免默认斜接线在锐角处产生尖刺；线宽按缩放倍率反向补偿，保持放大后的视觉宽度稳定。
- 连续滚轮、窗口尺寸和属性通知通过 32ms 后台优先级定时器合并重绘，减少重复创建 `StreamGeometry`。
- 增加海啸地图简化边界和高密度阈值单元测试。

### 本版本未包含

- 未改变海啸 GeoJSON 原始坐标、三级 LOD 资源、预报区 code 或观测点过滤规则。
- 未增加在线地图瓦片或改变海啸地图拖动交互。

### 验证结果

- App 测试 269 项通过；解决方案全量测试、Release 构建、固定测试数据校验和 `git diff --check` 在提交前串行执行。

版本总结：优化海啸地图尖刺显示和放大后的重绘性能。

提交日志：`0.67.9: 优化海啸地图尖刺显示和重绘性能`

## 0.67.8 - 2026-08-29

### 已实现

- 海啸地图改为三级本地 LOD：`MapZoomLevel <= 2` 使用低精度，`2 < MapZoomLevel <= 12` 使用中精度，`>12` 使用高精度。
- 缩放范围调整为 `0.5–24`，地图滚轮和工具按钮每次调整 `1` 倍；中/高精度资源首次进入对应倍率后后台加载并缓存，加载期间继续显示已加载层。
- 新增中精度资源和 `tools/generate_tsunami_lod.py` 生成工具；低、中、高三层沿岸线保持相同预报区 code，低精度抽稀后点数少于中精度，中精度少于详细层。

### 本版本未包含

- 未改变海啸报文、观测点过滤、站点目录或 SQLite 数据结构。
- 未将海啸沿岸线改为在线瓦片或新增地图拖动能力。

### 验证结果

- Core 测试 107 项、Infrastructure 测试 81 项、App 测试 268 项全部通过，共 456 项；解决方案 Release 构建、固定测试数据校验和 `git diff --check` 均通过。
- `git diff --check` 通过。

版本总结：调整海啸地图三级精度与加载阈值。

提交日志：`0.67.8: 调整海啸地图三级精度与加载阈值`

## 0.67.6 - 2026-08-29

### 已实现

- 修复海啸地图被 `HasSelectedReport` 详情容器一起隐藏的问题。
- 地图组件只依赖 `HasMapGeometry` 显示；没有选中报文时仍绘制中性 JMA 沿岸线。
- 选中报文时保持摘要、地图、观测详情和数据列表的布局顺序。

### 本版本未包含

- 未改变海啸报文数据、地图 LOD、观测点过滤或表格与地图联动规则。
- 未实现新的地图数据源或地图拖动功能。

### 验证结果

- Core 测试 107 项、Infrastructure 测试 81 项、App 测试 265 项全部通过，共 453 项。
- 解决方案 Release 构建、固定测试数据校验和 `git diff --check` 通过。

版本总结：修复无选中报文时海啸地图不可见的问题。

提交日志：`0.67.6: 修复海啸地图不可见问题`

## 0.67.5 - 2026-08-29

### 已实现

- 观测表选中有效观测点后，海啸地图按当前缩放自动将该点居中并保持高亮。
- 地图点点击后沿用原始站点 code 选择对应表格行，并在选择变化后自动滚动到该行。
- 欠测、无坐标和未知状态站点仍只显示表格与详情，不强制移动地图；报文切换继续清理失效选择。

### 本版本未包含

- 未实现海啸地图拖动、站点聚合或异步后台加载详细 GeoJSON。
- 未增加新的官方高精度 GIS 数据源。

### 验证结果

- Core 测试 107 项、Infrastructure 测试 81 项、App 测试 264 项全部通过，共 452 项。
- 解决方案 Release 构建、固定测试数据校验和 `git diff --check` 通过。

版本总结：实现海啸观测表与地图的双向选择、定位和滚动联动。

提交日志：`0.67.5: 增加海啸观测表与地图双向联动`

## 0.67.4 - 2026-08-29

### 已实现

- 重新组织海啸详情页：先显示当前报文和震源摘要，再显示地图、选中站点详情及预报/观测数据。
- 增加海啸地图缩放控件和两级本地 LOD：低于 `2.0×` 使用低精度沿岸线，达到阈值后按需加载详细沿岸线。
- 观测表改为可选列表；默认地图只显示有坐标且状态为“观测到海啸”或“微弱”的点，`欠测`、未知高度和无海啸记录不生成地图点；选中表格行后显示详情并同步地图高亮。

### 本版本未包含

- 未新增海啸官方高精度 GIS 来源；详细层仍使用现有 JMA 沿岸线资源。
- 未实现地图拖动、站点聚合或按表格选择自动定位。

### 验证结果

- Core 测试 107 项、Infrastructure 测试 81 项、App 测试 262 项全部通过，共 450 项。
- App Debug 和解决方案 Release 构建均为 0 警告、0 错误；固定测试数据校验通过。
- `git diff --check` 通过。

版本总结：重排海啸页面布局，加入低精度地图 LOD，并让观测表选择控制站点详情和地图显示。

提交日志：`0.67.4: 重排海啸页面并加入地图 LOD`

## 0.67.3 - 2026-08-29

### 已实现

- 修复从已手动放大的 A 地震切换到 B 地震时，地图中心已切换但缩放级别沿用旧事件的问题。
- 报告切换由视图根据当前画布尺寸、新事件包络和震源位置一次性计算自动 Zoom，避免 ViewModel 先写入隐藏默认缩放值造成状态竞争。
- 增加自动缩放计算和报告切换不改写手动缩放的回归测试。

### 本版本未包含

- 未改变手动缩放、拖动跟随、事件包络、地图中心和 LOD 阈值规则。
- 未改变报文解析、数据源、SQLite schema 或历史数据格式。

### 验证结果

- Core 测试 107 项、Infrastructure 测试 81 项、App 测试 259 项全部通过，共 447 项；Release 构建和 `git diff --check` 通过。

版本总结：消除地震情报页面跨事件切换时自动缩放与旧手动缩放状态的竞争。

提交日志：`0.67.3: 修复地震报文切换自动缩放竞态；统一视图自动缩放计算；增加跨事件缩放回归测试`

## 0.67.2 - 2026-08-29

### 已实现

- 海啸地图为具备完整官方坐标的观测点增加点击选择和高亮显示。
- 地图下方新增选中观测点详情，展示区域、站点名、原始 code、近海发布 code、观测状态、实时高度、首波时间和高潮时间。
- 报文切换后自动清理不存在于新报文中的站点选择；未匹配、无坐标站点仍保留原有表格行为，不按名称或距离推断。

### 本版本未包含

- 未实现地图定位、多个近邻站点聚合或观测点拖拽。
- 未改变海啸报文结构、等级判定、SQLite 目录表或历史数据格式。

### 验证结果

- Core 测试 107 项、Infrastructure 测试 81 项、App 测试 260 项全部通过，共 448 项。
- App Debug 和解决方案 Release 构建均为 0 警告、0 错误；固定测试数据校验通过。
- `git diff --check` 无空白错误。

版本总结：增加海啸地图观测点点击高亮和详情展示，并在报文切换时保持选择状态正确。

提交日志：`0.67.2: 增加海啸地图观测点点击详情`

## 0.67.1 - 2026-08-29

### 已实现

- 增加独立 `ITsunamiStationCatalogRepository`，SQLite 目录表支持海啸观测点、近海发布点及其一对多 code 映射的读写。
- 海啸页面加载目录时优先读取 SQLite；目录为空或发生可恢复读取异常时回退固定 JSON 资源，保留前导零并继续使用精确 code 匹配。
- 观测点显示和地图点层使用数据库目录补充名称、坐标和近海发布 code；无坐标或未匹配站点仍显示表格记录但不绘制地图点。

### 本版本未包含

- 未改变 JMAXML 报文结构、来源请求、海啸等级枚举或历史报文存储格式。
- 未实现地图点选定位、站点详情弹窗或发布区面填充。

### 验证结果

- Core 测试 107 项、Infrastructure 测试 81 项、App 测试 259 项全部通过。
- App Debug 构建 0 警告、0 错误；`git diff --check` 无空白错误。

版本总结：让海啸观测点和 PointTsunami 映射以 SQLite 为运行时优先目录，并补齐地图观测点的精确 code 关联。

提交日志：`0.67.1: 接入海啸站点目录 SQLite 读写与精确映射`

## 0.66.63 - 2026-08-28

### 已实现

- 报文切换时保留 ViewModel 的自动适配状态重置，但抑制中间 `AutoScale(1)` 的 `ZoomLevel` 通知，避免与视图按画布和事件包络计算出的最终缩放重复触发重绘和 LOD 调度。
- 在 `AutoScale` 调试日志中增加 `notifyZoom` 字段，区分内部重置和最终可见缩放。

### 本版本未包含

- 未改变事件包络计算、缩放上下限、地图中心、LOD 阈值或高精度几何；本轮只合并报文切换时的自动缩放通知。

### 验证结果

- 新增报文切换不发布中间 `ZoomLevel` 通知的回归测试；Release 测试、构建和 `git diff --check` 结果见本次交付说明。

版本总结：消除报文切换时自动缩放的中间状态通知和无效地图调度。

提交日志：`0.66.63: 合并报文切换时的自动缩放通知`

## 0.66.62 - 2026-08-28

### 已实现

- 高精度静态重绘中的轮廓、区域、市町村和选中高亮视口筛选统一使用单次索引循环，减少 LINQ 迭代器和中间数组分配。
- 保持原有包围盒相交规则、项目顺序、空集合行为和地图显示结果不变。

### 本版本未包含

- 未改变高精度资源缓存容量、视口缓冲、边界简化阈值、LOD 阈值、地图中心或地图精度；本轮只降低视口筛选阶段的 UI 线程开销。

### 验证结果

- 新增通用视口筛选顺序和相交结果回归测试；Release 测试、构建和 `git diff --check` 结果见本次交付说明。

版本总结：降低高精度静态重绘进入几何构建前的筛选分配和枚举开销。

提交日志：`0.66.62: 优化高精度静态几何视口筛选`

## 0.66.61 - 2026-08-28

### 已实现

- 高精度边界视口筛选改为单次索引循环和单次不可变数组构建。
- 保持边界层顺序、边界顺序、空层过滤规则和最终显示结果不变。

### 本版本未包含

- 未改变高精度资源缓存容量、视口缓冲、边界简化阈值、LOD 阈值或地图中心；本轮只降低视口筛选过程的分配和枚举开销。

### 验证结果

- 新增可见边界层筛选回归测试；Release 构建、全量测试和 `git diff --check` 结果见本次交付说明。

版本总结：降低高精度静态重绘中边界视口筛选的临时对象和迭代器开销。

提交日志：`0.66.61: 降低高精度边界视口筛选开销`

## 0.66.60 - 2026-08-28

### 已实现

- 高精度范围缓存改为 LRU，缓存命中时更新访问顺序。
- 缓存容量继续限制为 3，不增加高精度地图内存上限；来回拖动时优先复用最近使用范围。

### 本版本未包含

- 未改变高精度资源格式、瓦片大小、视口缓冲、LOD 阈值或静态几何绘制；本轮只修正缓存淘汰顺序。

### 验证结果

- 新增高精度缓存 LRU 淘汰测试；Release 构建、全量测试和 `git diff --check` 结果见本次交付说明。

版本总结：减少高精度地图来回拖动时因错误淘汰导致的重复资源解析。

提交日志：`0.66.60: 优化高精度范围缓存淘汰策略`

## 0.66.59 - 2026-08-28

### 已实现

- 高精度边界绘制前使用索引循环检查最少点数和有限坐标，避免 LINQ 委托枚举造成的重复遍历开销。
- 保持无效边界跳过规则、边界简化阈值和最终绘制结果不变。

### 本版本未包含

- 未改变高精度资源加载范围、缓存容量、地图中心和 LOD 阈值；本轮只降低边界绘制前的 CPU 枚举开销。

### 验证结果

- 新增边界有效性判定单元测试；Release 构建、全量测试和 `git diff --check` 结果见本次交付说明。

版本总结：降低高精度密集边界进入静态几何构建前的重复坐标扫描开销。

提交日志：`0.66.59: 降低高精度边界绘制枚举开销`

## 0.66.58 - 2026-08-28

### 已实现

- 报文切换自动缩放期间，如果目标 LOD 低于当前精度，暂时抑制旧高精度静态层重绘，等待目标 LOD 应用后再统一绘制。
- 同级或精度升级的报文切换仍绘制当前精度过渡层；手动拖动中的报文更新不改变原有中心保持和延迟策略。

### 本版本未包含

- 未改变自动缩放结果、LOD 阈值、资源范围、地图中心和高精度边界简化规则；只去除降级切换时无效的中间重绘。

### 验证结果

- 新增报文切换降级重绘延迟判定测试；App 测试、全量测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：避免报文切换时先重绘旧高精度图层再立即降级重绘造成的双重卡顿。

提交日志：`0.66.58: 优化报文切换时的LOD降级重绘`

## 0.66.57 - 2026-08-28

### 已实现

- 高精度静态绘制覆盖范围失效但无需切换 LOD 时，拖动结束不再立即重建静态几何，而是通过 `80ms` 空闲窗口合并补绘。
- 连续拖动会取消中间补绘，仅在最后一次稳定视口统一重绘；高精度资源重载路径仍等待几何应用后再重绘。
- 沿用 `PanRenderDeferred`/`PanRenderResumed` 日志标记覆盖补绘的延迟和恢复。

### 本版本未包含

- 未改变高精度资源范围、LOD 阈值、地图中心逻辑、边界简化阈值或观测点绘制规则；只调整覆盖失效后的补绘时机。

### 验证结果

- 新增高精度覆盖补绘延迟判定测试；App 测试、全量测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：减少连续大范围拖动中每次松手都同步重建高精度静态几何造成的卡顿。

提交日志：`0.66.57: 合并高精度覆盖补绘`

## 0.66.56 - 2026-08-28

### 已实现

- 高精度地图拖动越过最近静态绘制覆盖范围时，先保留并平移当前视觉层，不再立即按新中心重建旧高精度静态几何。
- 新高精度范围应用后再统一安排一次最终绘制；加载失败、取消或无资源时通过加载结束通知恢复一次绘制。
- 增加 `PanRenderDeferred` 和 `PanRenderResumed` 调试日志，便于确认拖动期间是否跳过中间重绘。

### 本版本未包含

- 未改变高精度资源范围、缓存数量、地图中心提交逻辑和边界简化阈值；拖动未越界时仍复用原有视觉层。

### 验证结果

- 新增高精度重载延迟重绘判定测试；App 测试、全量测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：避免高精度范围切换时旧图层和新图层连续重建，降低大数据事件拖动后的输入卡顿。

提交日志：`0.66.56: 延迟高精度重载期间的中间重绘`

## 0.66.55 - 2026-08-28

### 已实现

- 高精度地图根据当前视野内边界数量选择屏幕空间简化阈值：边界少于 `8000` 条时使用 `0.65px`，达到或超过 `8000` 条时使用 `1px`。
- 简化只合并连续不可见小线段，保留边界首尾点，不改变高精度资源范围、区域筛选、颜色或地图中心逻辑；`RenderComplete` 日志输出 `boundarySimplify` 实际阈值。

### 本版本未包含

- 未减少当前视野的边界数量，也未改变中精度、概览精度和观测点绘制策略。

### 验证结果

- 新增边界密度到简化阈值的回归测试；App 测试、全量测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：降低边界密集地震情报在高精度静态几何构建阶段的 CPU 和 WPF 几何写入开销。

提交日志：`0.66.55: 优化高精度密集边界绘制`

## 0.66.54 - 2026-08-28

### 已实现

- 报文切换期间由 `ZoomLevel`、`ViewedReportKey`、跟随状态和地图尺寸等属性通知触发的 LOD 检查统一排入一个后台调度周期，重复通知只保留一次检查。
- 用户主动缩放、拖动结束、滚轮提交和定位按钮仍直接执行 LOD 检查，保持交互完成后的精度响应。

### 本版本未包含

- 未改变地图精度阈值、缩放范围、地图中心保持逻辑和高精度资源；本版本只合并重复的检查调度。

### 验证结果

- 新增 LOD 检查调度去重判定测试；App 测试、全量测试和 Release 构建结果见本次交付说明。

版本总结：减少报文切换时重复 LOD 检查造成的 UI 调度和地图读取开销。

提交日志：`0.66.54: 合并报文切换中的LOD检查`

## 0.66.53 - 2026-08-28

### 已实现

- 高精度边界线绘制采用 `0.65px` 屏幕空间最小距离简化，连续过密点不再逐点创建 `StreamGeometry` 线段。
- 每条边界首尾点始终保留；中精度和概览不启用简化，保持原有显示精度。

### 本版本未包含

- 未改变边界资源、区域颜色、轮廓颜色和地图缩放规则；该优化只减少高精度绘制阶段的屏幕不可见折线点。

### 验证结果

- 新增边界点简化判定测试；App 测试 233/233 通过，Release 构建及 `git diff --check` 通过。

版本总结：降低高精度边界投影和 `StreamGeometry` 构建成本，缓解切换地震信息时的单次卡顿。

提交日志：`0.66.53: 优化高精度边界绘制`

## 0.66.52 - 2026-08-28

### 已实现

- 将地图基础区域、区域边界、市町村和轮廓几何与选中区域/市町村高亮拆分为独立 `DrawingVisual`。
- 基础静态几何缓存键不再携带选中高亮数量和节点身份；选中变化只创建轻量高亮层。
- `RenderComplete` 日志新增 `selection` 阶段耗时，便于确认高亮绘制成本。

### 本版本未包含

- 未改变区域颜色、轮廓颜色、观测点绘制、自动缩放或地图交互规则；震源和观测点仍按原有 marker 层绘制。

### 验证结果

- 新增基础静态层判定测试；App 测试 229/229 通过，Release 构建及 `git diff --check` 通过。

版本总结：选择高亮不再触发整张高精度静态地图重建。

提交日志：`0.66.52: 拆分地图基础几何与高亮图层`

## 0.66.51 - 2026-08-28

### 已实现

- 为观测树 `TreeViewItem` 显式设置横向内容拉伸和纵向内容居中，消除默认模板在容器尚未挂接时查找 `ItemsControl` 祖先产生的绑定错误日志。

### 本版本未包含

- 未改变观测树数据、节点选择、高亮和虚拟化行为；本版本仅固定容器对齐属性。

### 验证结果

- Release 测试、Release 构建和 `git diff --check` 通过；运行时应不再出现 `TreeViewItem RelativeSource ItemsControl` 绑定错误。

版本总结：清理观测树容器初始化阶段的 WPF 绑定错误噪声。

提交日志：`0.66.51: 修复观测树容器绑定错误`

## 0.66.50 - 2026-08-28

### 已实现

- 自动刷新生成等价事件快照时不再重建详情观测树；内容确实变化需要重建时，按节点类型、编码和名称恢复当前都道府县、区域、市町村或观测点高亮。
- TreeView 观测项首次点击直接执行选中/取消切换，并阻止默认选择流程反转结果。

### 本版本未包含

- 未改变新增报文自动跳转最新报文的既有规则；报文切换后不存在的旧观测节点仍会清除。

### 验证结果

- 新增等价刷新保持高亮回归测试；Release 测试 226/226 通过，Release 构建及 `git diff --check` 通过。

版本总结：修复自动刷新和 TreeView 默认选择流程导致的首次点击高亮立即消失。

提交日志：`0.66.50: 修复首次点击高亮并保持刷新选择`

## 0.66.49 - 2026-08-28

### 已实现

- 静态几何缓存键对投影中心、缩放和画布尺寸的极小浮点误差使用安全容差，避免同一视图因反算抖动重复构建高精度边界。
- 详情 ViewModel 只在选中事件或查看报文实例真正变化时重建观测树；地图缩放、拖动和跟随状态变化会保留当前观测点、区域或市町村高亮。

### 本版本未包含

- 未放宽不同视口或不同几何内容的缓存匹配；容差仅用于小于可见像素级别的数值误差。
- 未改变观测点选择、地图定位和报文切换时清除旧选择的既有规则。

### 验证结果

- 新增静态缓存浮点容差测试和地图状态变化不清除观测点高亮测试；Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：减少高精度静态几何的无效重复投影，并修复首次选择观测点后高亮被地图状态更新清除的问题。

提交日志：`0.66.49: 稳定高精度缓存并保持观测点高亮`

## 0.66.48 - 2026-08-27

### 已实现

- 滚轮缩放触发 `High → Medium/Overview` 或 `Medium → Overview` 时，立即提交当前地图中心，但延迟旧图层的待执行重绘。
- 目标 LOD 应用后统一恢复一次最终绘制；加载取消、失败或用户在加载期间开始拖动时也不会永久停留在滚轮预览状态。
- `Medium → High` 精度升级和同级缩放继续使用原有重绘流程，保留中精度过渡层。

### 本版本未包含

- 未改变高精度资源加载、视口裁剪、缓存和单次静态几何构建逻辑；本版本只消除精度下降过程中的旧层重复重绘。

### 验证结果

- 新增精度下降方向判断测试；Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：避免滚轮缩回低精度前重绘即将废弃的高精度图层。

提交日志：`0.66.48: 避免精度下降前重复重绘旧图层`

## 0.66.47 - 2026-08-27

### 已实现

- 高精度静态图层保存最近一次实际绘制的经纬度覆盖范围，拖动提交时同时校验新视野是否仍被该范围完整包含。
- 累计拖动超出已绘制范围时不再使用 `PanVisualReuse`，松手后按当前中心安排一次静态图层补绘；高精度数据仍在现有瓦片内时不重复加载资源。
- 绘制缓冲从视野四周 15% 增加到 25%，减少单次普通拖动过程中临时露出未绘制边缘的概率；新增 `PanRenderCoverageExpired` 诊断日志。

### 本版本未包含

- 未恢复整个高精度瓦片绘制，也未在鼠标按住期间同步重建静态图层；大幅拖动时优先保持交互流畅，松手后保证边界完整恢复。

### 验证结果

- 新增视觉复用必须满足绘制覆盖有效、累计拖动超出缓冲可被识别、25% 缓冲范围正确的单元测试；Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：修复高精度裁剪后累计拖动造成的区域和边界缺失。

提交日志：`0.66.47: 修复高精度拖动后的边界缺失`

## 0.66.46 - 2026-08-27

### 已实现

- GeoJSON 解析时一次性记录区域、市町村和边界线的经纬度包围盒，避免高精度重绘时重新遍历坐标计算范围。
- 高精度静态图层只投影和绘制当前视野及四周 15% 缓冲区内的区域、市町村、边界和选中轮廓；概览和中精度绘制逻辑保持不变。
- `RenderComplete` 的 `outline/areas/municipalities/boundaries` 改为“实际绘制数/当前加载数”，便于直接核验裁剪效果。

### 本版本未包含

- 未改变高精度瓦片加载范围、缓存数量、观测点绘制和 `BitmapCache` 策略；本版本只减少主线程静态几何构建量。

### 验证结果

- 新增多环多岛多边形包围盒、边界线包围盒、视野缓冲扩展和包围盒相交测试；Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：按视野裁剪高精度静态几何，减少拖动和缩放后的主线程投影开销。

提交日志：`0.66.46: 裁剪高精度静态几何绘制范围`

## 0.66.45 - 2026-08-27

### 已实现

- 自动刷新、来源状态、搜索、排序和刷新标记等非地图页面状态变化不再调用 `RebuildLayers()`，避免无意义地发布区域、市町村、观测点和边界通知。
- 跟随和焦点模式变化只通知对应地图状态属性；当前报文或所选事件真正变化时仍完整重建地图图层。
- 一次拖动期间重复到达的 LOD 检查继续合并，并只输出一条 `EnsureDetailDeferred` 日志。

### 本版本未包含

- 未移除 WPF `BitmapCache`，也未改变高精度几何构建方式；先验证去除自动刷新重绘后的真实交互耗时。

### 验证结果

- 新增非地图页面状态不发布 `Areas/Markers`、地图跟随状态仍正常通知的单元测试；Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：阻止自动刷新和无关页面状态反复重建高精度地图。

提交日志：`0.66.45: 避免非地图状态重复重建图层`

## 0.66.44 - 2026-08-27

### 已实现

- 高精度地图跨缓存范围时不再立即按新中心重建旧高精度几何；保留拖动后的视觉层，等待新范围加载完成后只执行一次完整重绘。
- 临时 `[MapDebug]` 日志只写入标准输出一次，移除调试输出的重复副本；拖动采样间隔从 100ms 调整为 250ms，降低附加调试器时的 UI 线程输出压力。

### 本版本未包含

- 未修改高精度地图资源和瓦片尺寸；本版本只消除跨范围时的重复重绘和诊断日志开销。

### 验证结果

- 补充高精度范围变化与 LOD 级别切换分离的回归断言；Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：高精度跨范围只重绘新几何一次，并降低临时日志对拖动性能的影响。

提交日志：`0.66.44: 减少高精度跨范围重绘和日志开销`

## 0.66.43 - 2026-08-27

### 已实现

- 高精度视口边界统一按缓存瓦片归一化，瓦片内平移不再被误判为需要重新加载，因此可以真正复用相机视觉层。
- 同一非高精度级别的在途请求合并；中精度资源在资源提供器内缓存，避免重复解析地图文件。
- 高精度换瓦片期间保留已有高精度几何，避免每次拖动都产生不必要的“高精度→中精度→高精度”双重完整重绘。

### 本版本未包含

- 未改变高精度 GeoJSON 的数据格式和缓存瓦片大小；跨出缓存瓦片时仍会按当前视口异步加载新的高精度几何。

### 验证结果

- 新增缓存瓦片边界与同级别在途请求复用单元测试；Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：减少高精度地图拖动中的误重载和重复解析，降低换范围时的双重重绘。

提交日志：`0.66.43: 统一高精度缓存边界并合并重复LOD请求`

## 0.66.42 - 2026-08-27

### 已实现

- 地图拖动结束时，如果内容、窗口尺寸和 LOD 都没有变化，则复用现有 `DrawingVisual`，只提交新的地理中心和平移变换，避免重新投影区域、市町村和观测点。
- 报文、选中状态、窗口尺寸或精度切换发生变化时仍执行完整重绘，避免复用过期视觉内容。

### 本版本未包含

- 未改变高精度 GeoJSON 的读取方式、缓存范围和中精度兜底流程；后台预取和地图切片将在后续步骤评估。

### 验证结果

- 新增相机平移复用判定单元测试；Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：稳定拖动场景下复用现有地图视觉层，减少鼠标松开后的整层重建。

提交日志：`0.66.42: 复用稳定拖动的地图视觉层`

## 0.66.41 - 2026-08-27

### 已实现

- 地图观测点图层只为有明确震度且有坐标的观测点创建标记，未知震度观测点继续保留在详情数据中但不参与地图绘制，减少高精度场景的投影和绘制数量。
- 为地图重绘增加 `legend`、`static`、`markers` 分阶段耗时日志，便于下一步区分图例、静态几何和观测点层的实际开销。

### 本版本未包含

- 未改变拖动中心提交、LOD 切换和高精度资源缓存逻辑；相机平移复用将在后续步骤单独实现。

### 验证结果

- 新增未知震度观测点不进入地图标记的回归测试；Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：先减少无显示价值的观测点绘制，并补充分阶段性能观测数据。

提交日志：`0.66.41: 裁剪未知震度地图观测点并增加分阶段耗时日志`

## 0.66.40 - 2026-08-27

### 已实现

- 高精度资源按约 1° 网格扩大视口并缓存已解析的区域、市町村和边界数据，连续大范围移动返回已访问区域时优先复用 `MapGeometrySet`。
- 高精度资源缓存限制为最多 3 个网格范围，避免因来回移动无限增加内存；原有取消令牌和 generation 校验保持不变。
- 增加 `HighResourceCacheHit` 日志，便于区分资源缓存命中和重新解析。

### 本版本未包含

- 未改变高精度地图显示阈值、中心保持逻辑、区域绘法或静态几何投影缓存。

### 验证结果

- Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：用有界空间缓存减少高精度大范围来回移动时的重复 GeoJSON 解析。

提交日志：`0.66.40: 缓存高精度地图范围`

## 0.66.39 - 2026-08-27

### 已实现

- 缓存完全相同投影状态下的区域、市町村、边界和区域高亮静态几何宿主，重复渲染时直接复用已有 `DrawingVisual`。
- 静态缓存键包含报文、精度、倍率、画布尺寸、中心、焦点、区域类型和选中对象等状态，状态变化时自动重新构建，避免复用过期几何。
- 增加 `StaticGeometryReuse`、`staticCache=hit/miss` 日志，用于确认重复渲染是否命中静态几何缓存。

### 本版本未包含

- 未跨不同地图中心或倍率强行缩放复用几何，未改变 LOD 范围、地图精度和地图中心逻辑。

### 验证结果

- Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：避免相同投影状态下重复执行区域坐标投影和 `StreamGeometry` 构建。

提交日志：`0.66.39: 复用静态地图几何`

## 0.66.38 - 2026-08-27

### 已实现

- 缓存完全相同投影状态下的区域、市町村、边界和区域高亮静态 `DrawingVisual`，重复渲染时直接复用已有宿主。
- 静态缓存键包含报文、精度、倍率、画布尺寸、中心、焦点、区域类型和选中对象等状态，状态变化时自动重新构建，避免复用过期几何。
- 新增 `StaticGeometryReuse` 和 `staticCache=hit/miss` 调试日志，便于确认重复渲染是否命中缓存。

### 本版本未包含

- 未跨不同地图中心或倍率强行缩放复用几何，未改变 LOD 范围、地图精度和地图中心逻辑。

### 验证结果

- Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：避免相同投影状态下重复重建静态地图几何。

提交日志：`0.66.38: 复用静态地图几何`

## 0.66.37 - 2026-08-27

### 已实现

- 按当前地震情报实际出现的震源/观测点和震度类型，懒加载并缓存圆圈与标签模板，重复观测点只复用模板并进行位置变换。
- 新增 `markerTypes` 调试字段，确认当前报文只准备实际需要的 marker 类型。

### 本版本未包含

- 未改变观测点颜色、尺寸、绘制顺序、LOD 范围或高精度几何加载策略。

### 验证结果

- Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：缓存当前事件实际需要的观测点圆圈模板，减少重复绘制对象创建。

提交日志：`0.66.37: 缓存观测点圆圈模板`

## 0.66.36 - 2026-08-27

### 已实现

- 缓存高倍率观测点的有限种震度标签排版结果，避免每次重绘为每个观测点重复创建 `FormattedText`。
- 保持观测点圆圈、标签位置、字体、颜色对比和震源置顶顺序不变。

### 本版本未包含

- 未改变观测点数量、区域几何、LOD 范围和地图中心处理逻辑。

### 验证结果

- Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：减少高精度大量观测点标签重绘时的文字排版分配。

提交日志：`0.66.36: 缓存观测点标签排版`

## 0.66.35 - 2026-08-27

### 已实现

- 将观测点圆圈、震度标签和观测点高亮合并到单个 `DrawingVisual`，减少高精度重绘时创建的 WPF 子元素数量。
- 保留低倍率小圆点、高倍率标签圆圈、未知震度 `?`、震度文字黑白对比以及震源最后绘制的层级顺序。
- 增加观测点绘制尺寸和视觉对象创建条件的单元测试。

### 本版本未包含

- 未改变高精度地图的资源范围、缓冲比例或几何缓存策略。

### 验证结果

- Release 测试、Release 构建和 `git diff --check` 结果见本次交付说明。

版本总结：把高精度地图中的大量观测点控件改为统一矢量绘制，降低重绘和布局开销。

提交日志：`0.66.35: 合并观测点地图绘制`

## 0.66.34 - 2026-08-27

### 已实现

- 拖动期间不再启动由属性、尺寸或跟随状态变化触发的 LOD 读取，只记录一次待检查请求。
- 多个地图 LOD 检查在 View 侧合并为串行的最新请求，正在执行时到达的重复通知会在当前检查完成后合并处理。
- 鼠标按下时先设置拖动状态，再关闭跟随，避免 `BeginManualInteraction` 的通知启动无效高精度加载。
- 增加拖动期间延迟 LOD 检查回归测试。

### 本版本未包含

- 未改变地图精度阈值、缩放范围、缓冲比例、几何资源和观测点绘制方式。

### 验证结果

- 全量 Release 测试 Core 106、Infrastructure 80、App 188 项全部通过；Release 构建通过（0 警告、0 错误），`git diff --check` 通过。

版本总结：减少拖动开始和结束阶段的重复 LOD 加载，降低输入卡顿。

提交日志：`0.66.34: 合并拖动期间的 LOD 检查`

## 0.66.33 - 2026-08-27

### 已实现

- 修复连续缩放触发旧高精度 LOD 请求取消时，索引 GeoJSON 读取抛出 `OperationCanceledException` 的异常报告。
- 高精度地图读取增加非异常式取消路径，过期请求不会提交部分解析结果；保留原有 `LoadHigh` 直接调用契约。
- 增加高精度 LOD 预取消回归测试。

### 本版本未包含

- 未改变地图精度阈值、视口裁剪范围、几何资源和缓存策略。

### 验证结果

- App 测试 186 项通过；Release 构建通过，`git diff --check` 通过。

版本总结：让高精度索引读取的正常取消退出不再产生异常报告。

提交日志：`0.66.33: 修复高精度 LOD 请求取消异常`

## 0.66.32 - 2026-08-27

### 已实现

- 修复连续缩放触发旧中精度 LOD 请求取消时，将预期取消报告为未处理异常的问题。
- 中精度资源加载在文件之间使用非异常式取消返回，过期请求不会提交几何；视图异步入口增加取消兜底。
- 增加预取消令牌的中精度加载和 LOD 状态回归测试。

### 本版本未包含

- 未改变地图精度阈值、缩放范围、几何资源和高精度加载策略。

### 验证结果

- App 测试 185 项通过；Release 构建通过，`git diff --check` 通过。

版本总结：让连续 LOD 请求的正常取消退出不再产生未处理异常报告。

提交日志：`0.66.32: 修复中精度 LOD 请求取消异常`

## 0.66.31 - 2026-08-27

### 已实现

- 修复高精度地图几何异步加载完成后视角在旧请求中心、震源和当前视口之间跳动的问题。
- 几何替换静止时以替换前画布实际中心为准；拖动时保留已提交中心并继续使用像素位移，请求快照中心不再覆盖当前视口。
- 增加几何替换中心优先级和拖动兜底回归测试，并保留 `[MapDebug]` 的 `GeometryChanging`、`LoadApplied`、`AutoScale` 日志。

### 本版本未包含

- 未改变地图精度阈值、缩放范围、几何资源和高精度缓存策略。

### 验证结果

- 全量 Release 测试 Core 106、Infrastructure 80、App 183 项全部通过；Release 构建通过（0 警告、0 错误），`git diff --check` 通过。

版本总结：按交互状态锁定几何替换时的正确地图中心。

提交日志：`0.66.31: 修复高精度加载完成后视角跳动`

## 0.66.30 - 2026-08-27

### 已实现

- 手动拖动地图期间，自动刷新收到新报文时跳过 `AutoScale`，不再清空当前地图中心或把视角拉回震源。
- View 和 ViewModel 同时保留当前拖动中心；报文图层仍正常更新，主动切换事件、点击跟随和复位仍按原逻辑自动适配。
- 增加自动适配跳过条件回归测试，并输出 `AutoScaleSkipped` 调试日志。

### 本版本未包含

- 未改变自动刷新频率、全局缩放范围或高精度 LOD 阈值。
- 未禁止用户主动触发的自动适配操作。

### 验证结果

- 全量 Release 测试 Core 106、Infrastructure 80、App 180 项全部通过。
- Release 构建通过（0 警告、0 错误），`git diff --check` 通过。

版本总结：避免自动刷新在手动拖动期间重置高精度地图中心。

提交日志：`0.66.30: 修复手动拖动时自动适配重置中心`

## 0.66.29 - 2026-08-27

### 已实现

- 滚轮缩放期间改为使用指针锚定的临时 `ScaleTransform`，连续滚轮输入不再逐次重建地图几何。
- 滚轮停止约 120ms 后一次性提交地图中心、完整重绘和 LOD 检查；事件切换、复位、拖动和卸载会结束或取消临时缩放预览。
- 增加滚轮预览比例、交互期间延迟重绘和缓存生命周期回归测试。

### 本版本未包含

- 未改变全局 `ZoomLevel` 范围、缩放步长、LOD 阈值或高精度资源格式。
- 临时缩放只复用当前缓存，滚轮停止后仍会按现有逻辑重建最终矢量地图。

### 验证结果

- 全量 Release 测试 Core 106、Infrastructure 80、App 177 项全部通过。
- Release 构建通过（0 警告、0 错误），`git diff --check` 通过。

版本总结：将连续滚轮缩放从逐事件重绘改为缓存预览加一次性提交。

提交日志：`0.66.29: 优化连续滚轮缩放交互`

## 0.66.28 - 2026-08-27

### 已实现

- 地图拖动使用的 `BitmapCache` 提前在视图加载阶段初始化，避免第一次按下鼠标时才进行大范围矢量栅格化。
- 拖动结束后保留缓存，连续拖动复用同一缓存；仅在控件卸载时释放，日志增加 `cache=on/off` 状态，便于确认缓存生命周期。

### 本版本未包含

- 未改变地图缩放范围、LOD 阈值、地图几何资源和绘制样式。
- 未将缓存写入磁盘，也未增加额外后台线程。

### 验证结果

- 全量 Release 测试 Core 106、Infrastructure 80、App 171 项全部通过。
- Release 构建通过（0 警告、0 错误），`git diff --check` 通过。

版本总结：消除每次拖动首帧重复创建位图缓存造成的卡顿。

提交日志：`0.66.28: 优化地图拖动缓存生命周期`

## 0.66.27 - 2026-08-27

### 已实现

- 区域、震度区域、市町村、边界和区域高亮统一绘制到单个 `DrawingVisual` 宿主，减少高精度地图的 WPF `Path` 元素和布局开销。
- 观测点、震源和震度标签继续保留独立元素及原有绘制顺序；相同颜色和线宽的冻结 `Pen` 复用缓存。

### 本版本未包含

- 未改变地图精度阈值、几何资源格式、观测点样式或高精度视口加载策略。
- 未将高精度地图改造成空间瓦片；本版本只优化 UI 渲染对象数量。

### 验证结果

- 全量 Release 测试 Core 106、Infrastructure 80、App 169 项全部通过。
- Release 构建通过（0 警告、0 错误），`git diff --check` 通过。

版本总结：合并静态地图图形并缓存画笔，降低高精度重绘的 WPF 元素开销。

提交日志：`0.66.27: 合并静态地图图形以降低高精度重绘开销`

## 0.66.26 - 2026-08-27

### 已实现

- 地图完整重绘计时器由 `Render` 优先级调整为 `Background`，输入事件优先处理。
- 拖动期间只更新已有地图内容的位移变换，完整重绘统一延迟到松开鼠标后执行。
- 高精度几何加载完成时若仍在拖动则暂存结果；松开后当前视口仍在加载缓冲范围内时直接复用，否则按最新视口重新加载，避免拖动中重建图层和视角跳动。
- `[MapDebug]` 日志增加 `RenderComplete` 的 `elapsed/build/children` 及 LOD 加载耗时、延迟应用状态。

### 本版本未包含

- 未改变地图精度阈值、几何资源格式或观测点绘制样式；高精度资源仍按现有索引读取。
- 未写入同步性能日志文件，避免文件 I/O 反过来影响交互性能。

### 验证结果

- `EarthquakeMapProjectionTests`：38 项通过；全量 Release 测试 Core 106、Infrastructure 80、App 167 项全部通过。
- Release 构建通过（0 警告、0 错误），`git diff --check` 通过。

版本总结：让地图输入优先，并延迟拖动期间的高精度几何提交。

提交日志：`0.66.26: 优化高精度地图交互性能`

## 0.66.25 - 2026-08-27

### 已实现

- 高倍率标签模式下，不明观测点与已知观测点使用相同的 20px 圆圈，圆心显示 `?`，填充色和轮廓色继续沿用原有不明震度样式；低倍率仍保持小圆点。
- 高精度地图加载增加在途任务复用：新的视口仍被当前高精度缓冲范围包含时，不取消并重新解析 GeoJSON，仅更新待应用的视角中心；移出缓冲范围或切换精度时才启动新任务。目标精度变化会立即取消旧任务并通过 generation 丢弃过期结果。

### 本版本未包含

- 未改变已知震度标签、震源样式、绘制顺序或 ZoomLevel 阈值。
- 高精度地图仍按视口读取真实几何；本版本通过避免重复加载降低缩放和拖动卡顿，未将高精度替换为低精度最终画面。

### 验证结果

- App：165 项通过，覆盖不明观测点 `?` 标签、震源绘制顺序和在途高精度加载复用。
- Core：106 项通过；Infrastructure：80 项通过；全量测试共 351 项通过。
- Release 构建与 `git diff --check` 在本版本提交前执行并记录结果。

版本总结：放大不明观测点标签，并复用视口内进行中的高精度加载以降低卡顿。

提交日志：`0.66.25: 优化高精度地图渲染和不明观测点标签`

## 0.66.24 - 2026-08-27

### 已实现

- 高倍率标签模式下，不明观测点也放大为 20px，并在圆圈中心显示 `?`；填充色和轮廓色继续沿用原有不明震度样式。
- 高精度地图拖拽期间启用 `BitmapCache`，让已有矢量图层以缓存位图参与平移，减少连续拖动时的栅格化开销；松手、丢失鼠标捕获或卸载时清除缓存，后续重绘恢复矢量质量。

### 本版本未包含

- 未改变已知震度标签、观测点颜色、震源样式、绘制顺序或 ZoomLevel 阈值。
- 缩放仍会重建真实高精度几何；若重绘日志显示几何生成占主要耗时，后续再评估视口几何缓存或分层合批。

### 验证结果

- App：164 项通过，覆盖不明观测点 `?` 标签和震源绘制顺序。
- Core：106 项通过；Infrastructure：80 项通过；全量测试共 350 项通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：完善不明观测点标签并降低高精度地图拖拽卡顿。

提交日志：`0.66.24: 优化高精度地图拖拽和不明标签`

## 0.66.23 - 2026-08-27

### 已实现

- 震度标签字体集中到地图视图的 `StationLabelFont` 字段，修改该字段即可单独调整标签字体，不影响地图其它文字。
- 观测点先绘制，震源最后绘制，确保震源位于所有观测点圆圈和震度标签上方。
- 高精度完整重绘复用并冻结相同颜色的 `SolidColorBrush`，减少反复创建 WPF 画刷的开销；每次完整重绘输出耗时和图层数量，便于后续定位几何生成或布局瓶颈。

### 本版本未包含

- 未改变观测点标签内容、圆圈颜色、轮廓颜色或 ZoomLevel 阈值。
- 未进行破坏现有 Tooltip/轮廓交互的整图几何合批；若耗时日志显示几何生成仍是主要瓶颈，再单独评估分层缓存或合批渲染。

### 验证结果

- App：164 项通过，包含震源绘制顺序和观测点标签回归测试。
- Core：106 项通过；Infrastructure：80 项通过；全量测试共 350 项通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：提升高精度地图重绘效率并保证震源标记始终位于标签上方。

提交日志：`0.66.23: 优化高精度地图标记渲染`

## 0.66.22 - 2026-08-27

### 已实现

- 观测点震度标签改为在圆圈内容区域内垂直居中，修复字形视觉上偏向上方的问题。
- 标签显式使用 `Segoe UI` 加粗字体；后续可通过 `FontFamily`、`FontSize` 和 `FontWeight` 属性调整字体样式。

### 本版本未包含

- 未改变观测点圆圈大小、填充色、轮廓色、震度标签内容或 ZoomLevel 阈值。

### 验证结果

- App：163 项通过；标签映射、阈值、未知震度和对比色测试保持通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：修复观测点标签垂直位置并明确标签字体设置方式。

提交日志：`0.66.22: 修复观测点标签垂直居中`

## 0.66.21 - 2026-08-27

### 已实现

- 全局 `ZoomLevel >= 8` 时，观测点圆圈由 8px 放大为 20px，并在中心显示 `1`–`7` 及半级震度 `5-`、`5+`、`6-`、`6+`。
- 标签文字根据震度填充色亮度自动选择黑色或白色；圆圈填充色、轮廓色和震度从小到大的绘制顺序保持不变。
- `ZoomLevel < 8` 时继续使用原有小圆点样式，降低低倍率地图的重叠程度。

### 本版本未包含

- 未改变观测点数据、震度配色、震源标记或地图缩放范围。
- 未为未知震度生成标签；未知震度仍只显示原有圆圈。

### 验证结果

- App：163 项通过，覆盖标签阈值、标准震度文本、未知震度和黑白对比色选择。
- Core：106 项通过；Infrastructure：80 项通过；全量测试共 349 项通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：在高倍率地图中提升观测点震度辨识度，并控制低倍率标记拥挤。

提交日志：`0.66.21: 放大观测点并显示震度标签`

## 0.66.20 - 2026-08-26

### 已实现

- 将本地 XML 导入的报文写入和导入批次/文件明细写入合并到同一个 SQLite 事务。
- 任一报文或明细写入失败时，整批导入回滚，不会留下只有报文或只有批次的半成品状态。
- 保持普通网络刷新、WebSocket 写入和固定样本初始化的原有事务路径不变。
- 增加 SQLite 故障回滚测试，验证导入明细写入失败时事件报文和最近导入记录均不残留。

### 本版本未包含

- 未把来源状态写入与本地导入报文/明细合并为同一事务；来源状态仍沿用现有刷新流程。
- 未增加历史导入批次列表、删除或撤销功能。

### 验证结果

- `dotnet test tests\\EarthquakeShow.Infrastructure.Tests\\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：80 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：全量通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore`：0 个警告、0 个错误；未执行 Git commit。

版本总结：统一本地 JMA XML 报文与导入明细的 SQLite 事务，失败时整批回滚。

提交日志：`0.66.20: 统一本地XML导入事务`

## 0.66.19 - 2026-08-26

### 已实现

- SQLite schema 升级到 4，增加本地 XML 导入批次表和导入文件明细表。
- 每次导入保存目录、完成时间、写入/更新数量，以及全部跳过文件和失败文件；报文本身继续使用原有事务和幂等主键。
- 应用启动时读取最近一次导入批次；设置页显示最近导入时间、目录统计和最多 100 条跳过/失败明细。
- 导入完成后立即更新设置页明细；重启应用后仍能恢复最近一次导入信息。
- 增加 schema 4 导入历史持久化/重载测试，以及设置页摘要和明细投影测试。

### 本版本未包含

- 未提供所有历史批次的分页查看、删除批次或导入撤销。
- 未将导入明细单独放入事件详情页；当前只在设置页查看。

### 验证结果

- `dotnet test tests\\EarthquakeShow.Infrastructure.Tests\\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：79 项通过。
- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：150 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：全量通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore`：0 个警告、0 个错误；未执行 Git commit。

版本总结：持久化本地 JMA XML 导入批次和文件明细，并在设置页恢复最近一次导入结果。

提交日志：`0.66.19: 持久化本地XML导入明细`

## 0.66.18 - 2026-08-26

### 已实现

- 在现有“设置 → 数据源”页面增加“选择目录并导入”入口，使用 .NET 8 WPF 原生 `OpenFolderDialog` 选择本地 JMA XML 目录。
- 设置页显示导入中的状态，以及成功写入/更新、跳过和失败文件数量；导入期间按钮自动禁用，结束后恢复可用。
- `MainWindowViewModel` 使用现有站点/区域目录创建本地 XML 导入器，调用仓储导入入口；仓储发布的事件快照会自动刷新事件列表、详情和地图。
- 增加设置 ViewModel 导入状态和统计显示测试；保留现有后端与 WPF 回归测试。

### 本版本未包含

- 未把跳过/失败文件明细写入 SQLite；当前只在设置页显示数量。
- 未增加导入历史记录、撤销导入或自动扫描固定目录。

### 验证结果

- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：149 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：全量通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore`：0 个警告、0 个错误；未执行 Git commit。

版本总结：在设置页增加本地 JMA XML 目录选择和导入结果提示，导入后自动刷新页面数据。

提交日志：`0.66.18: 增加本地JMA XML设置导入入口`

## 0.66.17 - 2026-08-26

### 已实现

- 在 `SqliteEarthquakeEventRepository` 增加 `ImportLocalXmlAsync`，将本地 XML 导入器的成功报文交给现有 SQLite 报文事务保存。
- 导入过程复用 `SaveReportsAsync` 的主键冲突更新、事件重新聚合和 `EventsChanged` 发布规则；同一 `EventId + SourceId + SourceMessageId` 重复导入不会产生重复报文。
- 导入结果增加 `SavedReportCount`，明确表示本次成功写入或更新的报文数；跳过文件和解析失败仍保留在原结果中。
- 增加数据库导入、重复导入幂等和“有效文件与损坏文件并存”三项回归测试。

### 本版本未包含

- 未增加 UI 导入按钮、目录选择器或设置项；当前只能由后端调用入口执行。
- 未把失败/跳过明细写入 SQLite；调用方需要自行展示本次导入结果。

### 验证结果

- `dotnet test tests\\EarthquakeShow.Infrastructure.Tests\\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：78 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：全量通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore`：0 个警告、0 个错误；未执行 Git commit。

版本总结：将本地 JMA XML 解析结果接入 SQLite 事务并保证重复导入幂等。

提交日志：`0.66.17: 接入本地JMA XML数据库导入`

## 0.66.16 - 2026-08-26

### 已实现

- 增加 `JmaXmlLocalFileImporter`，递归读取指定目录中的 `.xml` 文件，只解析文件名包含 `_VXSE51_`、`_VXSE52_`、`_VXSE53_` 的 JMA 地震报文。
- 本地文件复用 Core 的 `JmaXmlParser`、站点目录和区域目录；来源 ID 继续使用 `jma-xml`，文件名作为 `SourceMessageId`，本地 URI 和完整 XML 保留在 `SourceReference` 中。
- 单个文件读取或解析失败不会阻断其他文件；结果明确区分成功报文、跳过文件和失败文件。暂不自动写入 SQLite。
- 增加有效 XML、非地震 XML、损坏 XML、空目录和目录不存在的 Infrastructure 单元测试。

### 本版本未包含

- 未增加 UI 导入按钮或设置项。
- 未将导入结果写入 SQLite；后续步骤需要设计事务导入和重复报文处理提示。

### 验证结果

- `dotnet test tests\\EarthquakeShow.Infrastructure.Tests\\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：75 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 106 项、Infrastructure 75 项、App 148 项通过，共 329 项。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore`：0 个警告、0 个错误；未执行 Git commit。

版本总结：增加可复用的本地 JMA XML 批量解析后端，为历史报文补录准备统一来源身份。

提交日志：`0.66.16: 增加本地JMA XML导入后端`

## 0.66.15 - 2026-08-26

### 已实现

- 增加 `tools\audit_jma_xml_feed.ps1`，按运行时相同的文件名时间和 `VXSE51/52/53` 筛选规则审计 JMA 短 Feed、长期 Feed 的条目数、类型分布、最早/最新时间和指定 `since` 的命中数。
- 使用官方 Feed 完成一次真实只读核验：短 Feed 有 8 条地震报文，最早 `2026-08-25T23:36:00Z`；长期 Feed 有 93 条，最早 `2026-08-19T11:47:27Z`。
- 核验 `since=2026-08-21T00:00:00+09:00` 时，短 Feed 命中 8 条且覆盖不足，长期 Feed 命中 75 条且覆盖起点正常；确认 XML 能补回近期离线报文，但不能恢复早于长期 Feed 最早条目的历史缺口。
- 将审计命令和覆盖边界写入工程实现、数据契约和 UI 推进文档，作为后续判断是否需要本地 XML 导入的依据。

### 本版本未包含

- 未改变正式应用的 XML 抓取、并行刷新、退避、事件合并或 SQLite 写入逻辑。
- 未实现历史 XML 批量导入；超出长期 Feed 窗口的报文仍需要用户提供 XML 文件或有授权的历史归档。

### 验证结果

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\audit_jma_xml_feed.ps1 -Since 2026-08-21T00:00:00+09:00`：官方短/长期 Feed 读取成功，输出与独立解析结果一致。
- Core：106 项通过；Infrastructure：70 项通过；App：148 项通过；全量测试共 324 项通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：增加 JMA XML 覆盖窗口审计工具并确认长期 Feed 的实际历史边界。

提交日志：`0.66.15: 增加JMA XML覆盖审计工具`

## 0.66.14 - 2026-08-26

### 已实现

- 将 XML 来源的 `Delayed` 覆盖诊断投影到地震页面底部状态栏，显示“网络：XML覆盖延迟”，不再只显示笼统的“已连接/部分可用”。
- 对实时来源的限流和解析失败分别显示“网络：来源限流”和“网络：来源解析失败”，方便判断是上游限制、报文格式问题还是离线。
- 保持现有 XML 短 Feed/长期 Feed 回补、来源并行抓取、退避和数据库写入规则不变。
- 增加 App 单元测试，覆盖 XML 覆盖延迟和来源解析失败的状态投影。

### 本版本未包含

- 未改变 XML Feed 的实际覆盖范围；“XML覆盖延迟”仍表示当前 Feed 最早条目晚于缓存增量起点，不能保证上游已提供全部离线报文。
- 未新增本地 XML 批量导入；若需要补回超出 JMA 长期 Feed 窗口的历史报文，仍需要用户提供 XML 文件或接入有授权的历史归档。

### 验证结果

- App：148 项通过，覆盖 XML 覆盖延迟、来源解析失败和既有页面状态。
- Infrastructure：70 项通过；Core：106 项通过；全量测试共 324 项通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：在页面状态栏显示 XML 覆盖和来源故障诊断，便于确认 XML 是否正在补报。

提交日志：`0.66.14: 增加XML覆盖诊断状态`

## 0.66.13 - 2026-08-26

### 已实现

- 在线 HTTP 来源自动检查间隔由 60 秒降至 5 秒。
- JMA XML 与 P2PQuake HTTP 来源改为并行抓取，整轮耗时不再由两个来源串行累加。
- P2PQuake WebSocket 保持收到报文即解析、入库和刷新页面；HTTP 失败或限流时继续使用指数退避。
- JMA Feed 覆盖不足的 `Delayed` 状态仍保持 5 秒检查，不把可恢复的覆盖延迟误判为网络故障。

### 本版本未包含

- 未强制把 JMA XML 上游 Feed 变成 1 秒请求；JMA Feed 的实际发布频率仍由上游决定。
- 未改变来源优先级、事件合并或报文解析规则。

### 验证结果

- App：146 项通过，覆盖默认 5 秒刷新间隔和 `Delayed` 状态调度。
- Infrastructure：70 项通过，覆盖两个 HTTP 来源并行抓取。
- Core：106 项通过；全量测试共 322 项通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：降低在线情报检查延迟，并消除多来源串行抓取造成的额外等待。

提交日志：`0.66.13: 加快情报刷新并行抓取`

## 0.66.12 - 2026-08-26

### 已实现

- 收到新增报文并自动选择最新情报时，左侧事件列表通过 `ScrollIntoView` 滚动到对应事件项。
- 主窗口收到同一导航通知后自动打开详情栏并切换到“摘要”页；用户手动切换事件或报文不会被强制切回摘要。

### 本版本未包含

- 未改变新增报文的事件合并、发布时间排序和 XML 优先级规则。

### 验证结果

- App 页面 VM 单元测试覆盖新增报文触发一次导航通知、无新增报文不触发通知。
- Core：106 项通过；Infrastructure：69 项通过；App：144 项通过；全量测试共 319 项通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：完善收到新情报后的事件列表和详情页自动导航。

提交日志：`0.66.12: 完善新情报自动定位和详情跳转`

## 0.66.4 - 2026-08-26

### 已实现

- 调查并接入 P2PQuake JSON API v2 的 `domesticTsunami`/`foreignTsunami` 完整枚举。
- 国内海啸状态优先归一化显示；`domesticTsunami=None`、`foreignTsunami=Unknown` 现在显示“津波の心配なし”。
- 海外海啸可能性按官方区域语义补充显示，不覆盖日本国内海啸状态；HTTP 与 WebSocket 共用该解析逻辑。

### 本版本未包含

- 未把海外海啸可能性当作日本国内海啸警报等级，也未改变独立 JMA 海啸报文来源和模型。

### 验证结果

- P2PQuake 来源测试：18 项通过，覆盖国内 `None`、国内 `Checking` 和海外区域可能性。
- Release 构建及全量测试在本版本改动完成后执行。
- 全量测试：Core 106 项、Infrastructure 66 项、App 138 项，共 310 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：修复 P2PQuake 常见 `None + Unknown` 摘要误显示，并完整保留国内/海外字段语义。

提交日志：`0.66.4: 规范化 P2P 海啸状态摘要`

## 0.66.5 - 2026-08-26

### 已实现

- 按 P2PQuake JSON API v2 官方定义确认 `domesticTsunami=Warning` 为“津波预报（种类不明）”，界面统一显示“津波预报”，不误报为津波警报或大津波警报。
- 将 `domesticTsunami=Checking` 固定显示为“津波 调查中”，并保留海外状态的独立补充语义。
- 将国内 `Unknown` 也归入“津波 调查中”；海外 `Unknown` 仍不覆盖国内状态。
- SQLite 读取 P2PQuake 历史报文时，从 `SourcePayload` 重新解析 `domesticTsunami/foreignTsunami`，修正旧缓存中保存的过期海啸摘要。

### 本版本未包含

- 未改变 P2PQuake 原始 JSON 保存内容，也未将海外海啸可能性提升为日本国内警报等级。

### 验证结果

- Infrastructure 测试：69 项通过，新增 `Warning`、国内 `Unknown` 映射和旧 SQLite 缓存重解析回归测试。

版本总结：修复 P2PQuake 海啸状态显示和历史缓存兼容性。

提交日志：`0.66.5: 修复 P2P 海啸状态和缓存显示`

## 0.66.6 - 2026-08-26

### 已实现

- 选中都府道县时，按都府道县代码高亮其下属区域面；选中区域或市町村时，按稳定代码高亮对应面。
- 选中观测点时，在对应站点标记外绘制双层圆环；所有高亮均为外层半透明粗描边加内层高亮细描边，不覆盖原有震度填色。
- 高亮状态由详情观测树统一驱动，并在地图中精度几何替换后自动复用，不增加整套高精度几何副本。

### 本版本未包含

- 未加入持续动画、模糊特效或多选高亮；当前只保留一个选中对象。

### 验证结果

- Core：106 项通过。
- Infrastructure：69 项通过。
- App：139 项通过，包含四层选中高亮回归测试。
- 全量测试：314 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：为四层观测树选中对象增加低开销双层描边高亮。

提交日志：`0.66.6: 增加地图选中区域发光高亮`

## 0.66.7 - 2026-08-26

### 已实现

- 将选中高亮改为高对比外层深色/浅色描边加青色内层描边，避免震度 3 等黄色区域上使用黄色高亮导致不可见。
- 选中观测点时定位到观测点；选中区域、市町村或都道府县时根据完整几何包络计算中心和自动缩放，确保对象完整显示。
- 区域高亮层重新保留该区域原本的震度填色，市町村保持现有面表现并叠加高亮描边。
- 观测树同一节点再次点击时取消选择并移除高亮；地图拖动或缩放不会清除当前高亮。

### 本版本未包含

- 未加入多选、持续动画或模糊特效；仍保持单个选中对象。

### 验证结果

- Core：106 项通过。
- Infrastructure：69 项通过。
- App：139 项通过，包含选中对象切换和取消回归测试。
- 全量测试：314 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：完善地图选中高亮的可见性、定位、缩放和取消交互。

提交日志：`0.66.7: 完善地图选中高亮交互`

## 0.66.8 - 2026-08-26

### 已实现

- 修复观测树重复点击时对文本内容元素调用视觉树 API 可能导致的异常，改用 `ItemsControl.ContainerFromElement` 获取树节点容器。
- 对空轮廓、未加载轮廓和不完整轮廓增加高亮绘制保护，无法形成有效多边形时跳过绘制。
- 区域高亮内层描边改为当前区域对应的震度颜色，外层继续使用高对比描边，避免高亮遮蔽震度语义。

### 本版本未包含

- 未改变地图轮廓资源加载策略，也未加入多选或动画高亮。

### 验证结果

- Core：106 项通过。
- Infrastructure：69 项通过。
- App：140 项通过，包含高亮边界与重复选择回归测试。
- 全量测试：315 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：修复观测树高亮闪退风险并恢复区域震度颜色表达。

提交日志：`0.66.8: 修复地图高亮异常和颜色显示`

## 0.66.9 - 2026-08-26

### 已实现

- 观测树点击查找同时支持 Visual 和文本内容元素，修复重复点击同一节点无法取消高亮的问题。
- 区域和市町村定位优先使用当前几何，当前精度未加载对应对象时回退到概览几何；选中高亮也能从概览几何重建，避免位置未知节点在拖动后才延迟自动定位。
- 观测点栏启用固定的内容拉伸布局，与时间线栏一致占满详情栏，减少内容测量变化导致的卡顿和抖动。

### 本版本未包含

- 未改变高精度地图资源格式、加载阈值和缓存回收策略。

### 验证结果

- Core：106 项通过。
- Infrastructure：69 项通过。
- App：142 项通过，包含重复切换和概览几何回退定位测试。
- 全量测试：317 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：修复观测树取消高亮、未知位置延迟定位和观测点栏布局抖动。

提交日志：`0.66.9: 修复观测树取消高亮和区域定位`

## 0.66.10 - 2026-08-26

### 已实现

- 将观测树重复点击处理下沉到 `TreeViewItem` 容器事件，文本、徽标和空白区域点击均能稳定执行取消高亮。
- 自动刷新和流式事件快照比较新旧报文身份，发现新增报文后按发布时间 `IssuedAt` 选择最新事件和报文；发布时间相同则按接收时间及 XML 优先级确定顺序。
- 刷新没有新增报文时保留用户当前事件和历史报文选择，避免周期刷新打断人工查看。

### 本版本未包含

- 未改变事件合并规则、报文来源优先级或手动事件列表排序规则。

### 验证结果

- Core：106 项通过。
- Infrastructure：69 项通过。
- App：143 项通过，包含 TreeView 取消高亮和新增报文自动跳转回归测试。
- 全量测试：318 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：修复重复点击取消高亮，并在收到新增情报时自动进入发布时间最新的报文。

提交日志：`0.66.10: 修复高亮取消并自动跳转最新情报`

## 0.66.11 - 2026-08-26

### 已实现

- 震度速報位于前序震源情报或震源・震度情报之后时，地图从前序报文继承最近震源坐标并继续显示震源标记。
- 详情页的“定位震源”按钮也使用同一继承规则；震源情报之前的震度速報不会凭空添加震源。

### 本版本未包含

- 未改变报文顺序、事件合并和震度区域继承规则。

### 验证结果

- Core：106 项通过。
- Infrastructure：69 项通过。
- App：144 项通过，包含震源后震度速報地图标记和详情定位回归测试。
- 全量测试：319 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：完善震源情报后的震度速報震源位置显示。

提交日志：`0.66.11: 修复震源后震度速报位置显示`

## 0.66.3 - 2026-08-26

### 已实现

- 统一加深地图、详情徽标和事件列表中的震度 2 配色。
- 图例中的五弱、五强、六弱、六强改用 `5-`、`5+`、`6-`、`6+` 显示，不再暴露内部 `5-lower` 等编码。
- 海啸摘要改用与其他摘要字段相同的标签列对齐方式，并暂时隐藏来源差异区域；底层来源差异数据和来源切换仍保留。

### 本版本未包含

- 未改变领域层震度编码、数据库值或来源差异计算逻辑。

### 验证结果

- 新增图例半级震度显示映射单元测试。
- Release 构建：0 个警告、0 个错误。
- 全量测试：Core 106 项、Infrastructure 65 项、App 138 项，共 309 项通过。

版本总结：统一震度 2 的视觉色板，修正地图图例标签，并整理详情摘要布局。

提交日志：`0.66.3: 调整震度配色与详情摘要布局`

## 0.66.2 - 2026-08-26

### 已实现

- 修复地图在当前报文身份暂时未匹配事件时间线时错误继承第一条报文的问题。
- 补充跨来源回归验证：P2P 详细观测点之后收到 XML 震源情报时，继续按震源・震度情报使用震度色区域轮廓。

### 本版本未包含

- 未改变 XML/P2P 事件归并规则、详情页报文类型文字或其他报文的地图绘法。

### 验证结果

- 地图 ViewModel 测试：32 项通过。
- 全量测试：Core 106 项、Infrastructure 65 项、App 132 项，共 303 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：修复前序报文索引回退，并确认跨来源详细观测点可以影响后续震源情报绘法。

提交日志：`0.66.2: 修复跨来源前序观测点绘法判断`

## 0.66.1 - 2026-08-26

### 已实现

- 震源情报的区域绘法改为根据同一事件此前是否已有详细观测点数据决定：只有震度速報时使用区域填色和黑/白边界；已有详细观测点时使用震源・震度情报的震度色区域轮廓。

### 本版本未包含

- 未改变详情页当前报文类型文字、报文时间线或事件合并规则。
- 未改变震度速報本身、震源・震度情报本身以及远地事件的既有绘图规则。

### 验证结果

- 新增“震度速報 → 震源情报”和“震度速報 → 震源・震度情报 → 震源情报”两条地图回归测试。
- 全量测试：Core 106 项、Infrastructure 65 项、App 131 项，共 302 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：让震源情报根据此前是否已经出现详细观测点，选择正确的区域绘法。

提交日志：`0.66.1: 按前序观测点选择震源情报区域绘法`

## 0.66.0 - 2026-08-26

### 已实现

- 首次选择地震情报或点击自动定位后，地图不再无条件先请求中精度；自动适配完成的全局 `ZoomLevel` 直接决定概览、中精度或高精度，保留高精度加载期间的中精度临时兜底。
- 远地 P2PQuake 的重复更新在跨来源合并前先按报文类型、火山/地震类别、发生时间、震源坐标和震级阈值保守归并；同一远地事件的多条 P2P 更新可以与 JMA XML 一起显示为一个事件。

### 本版本未包含

- 未放宽普通近地 P2P 同阶段报文的合并规则；缺少远地类别、发生时间或坐标证据的报文仍保持独立。
- 未改变远地地图只显示震源、固定 `ZoomLevel=0.5` 和不加载日本中/高精度几何的规则。

### 验证结果

- 新增三条远地 P2P 更新与 XML 合并测试，以及时间、坐标、远地类别不匹配时保持独立测试。
- 调整地图 LOD 测试，确认自动缩放后的全局级别直接选择对应精度。
- 全量测试：Core 106 项、Infrastructure 65 项、App 130 项，共 301 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：让自动缩放精度与全局 ZoomLevel 一致，并补齐远地多次更新的跨来源归并。

提交日志：`0.66.0: 修正自动地图精度并合并远地多次更新`

## 0.65.1 - 2026-08-25

### 已实现

- 震度速報和震源情报的区域填色模式不再绘制 `Unknown` 区域面或纯未知震度边界；已知区域与未知邻区之间的边界仍按已知震度保留。
- 摘要中的“第 x / x 报”改为按当前来源统计；XML 与 P2PQuake 合并为同一事件后，各自使用本来源报文序号和总数。

### 本版本未包含

- 未改变震源・震度情报的震度色轮廓规则，也未改变底图中性行政边界。
- 未修改报文时间线排序、来源切换、事件合并或摘要字段累计逻辑。

### 验证结果

- 新增未知震度区域/边界绘制决策测试，并扩展双来源时间线测试验证来源内报次统计。
- 全量测试：Core 105 项、Infrastructure 65 项、App 130 项，共 300 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：隐藏区域填色模式中的未知震度轮廓，并按当前来源显示摘要报次。

提交日志：`0.65.1: 修正无震度区域轮廓和来源报次统计`

## 0.65.0 - 2026-08-25

### 已实现

- 地图按当前报文类型选择区域绘法：震度速報和继承前序区域层的震源情报使用震度色填充及黑/白对比边界；震源・震度情报恢复不填充区域面、只按震度色绘制区域轮廓。
- 时间线工具栏增加来源切换按钮；时间线条目、累计变化和上一报/下一报/返回最新导航均限定在当前来源内，切换后选择目标来源的最新报文。
- 事件列表默认改为按发生时间降序；发生时间未知的事件排在已知事件之后，并在未知组内按发布时间降序稳定排列。“最新发布”和“最大震度”排序仍可手动选择。

### 本版本未包含

- 未改变事件时间范围筛选规则；“最近 24 小时/7 天/30 天”仍按最新发布时间判断。
- 未修改 XML/P2PQuake 事件合并阈值、来源优先级、地图 LOD 或高精度缓存策略。
- 震源・震度情报的市町村填色、观测点和震源图层保持原有显示方式。

### 验证结果

- 新增或扩展报文类型绘图、双来源时间线切换与来源内导航、默认发生时间排序及未知发生时间排序测试。
- 全量测试：Core 105 项、Infrastructure 65 项、App 129 项，共 299 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：按报文类型恢复区域绘图语义，增加时间线来源切换，并默认按地震发生时间排列事件。

提交日志：`0.65.0: 按报文类型绘制区域并支持时间线来源切换`

## 0.64.0 - 2026-08-25

### 已实现

- 修复同一地震的 P2PQuake `ScalePrompt`、`Destination`、`DetailScale` 使用不同消息 ID 而无法全部并入 JMA XML 事件的问题；先按互补报文阶段、发生时间、发布时间、震度、坐标和震级保守归并 P2P，再执行跨来源匹配，JMA XML 继续作为默认首选来源。
- 正确解析 P2PQuake `points[].isArea=true`：震度速報只生成“都道府县 → 区域”，命中关系目录时使用正式 JMA 代码，不再伪造 `unknown` 市町村和观测点。
- 地图开始绘制区域震度面；区域填色沿用统一震度色板，边界按底色亮度选择深色或白色。后发震源情报没有震度数组时继续继承同一事件前序震度速报的区域层。
- 图例改为只显示震度 1 到当前实际最大震度的连续范围；“不明”仅在实际图层存在未知震度时显示，远地事件和无震度图层时隐藏，底色调整为更浅的白色。
- 时间线项目横向占满详情栏，震源、发布时间、接收时间和海啸说明支持换行；事件卡片底部海啸说明不再以省略号截断。

### 本版本未包含

- 未实现图例四角遮挡面积检测；连续等级缩短图例后仍固定在左下角。
- 未修改详情栏固定宽度、主窗口三栏比例、地图 LOD 阈值或高精度缓存策略。
- P2PQuake 两条都没有震源坐标的区域速报不会仅凭时间猜测合并；同阶段报文也不会互相合并，以避免邻近事件误归并。

### 验证结果

- 新增同一事件 P2P 三阶段与 JMA XML 合并、无坐标 P2P 不猜测关联、同阶段邻近事件不误合并、`isArea` 两层解析、连续图例以及震源情报继承区域层测试。
- 全量测试：Core 105 项、Infrastructure 65 项、App 123 项，共 293 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：修正 P2P 多阶段与 XML 的事件归并和区域层级，并完善区域地图、图例及详情换行。

提交日志：`0.64.0: 修正多来源归并并完善区域地图与详情布局`

## 0.63.1 - 2026-08-25

### 已实现

- 优化高精度地图拖动：新增固定视口和可平移内容层，MouseMove 期间复用已生成的 WPF 图形并只更新一个 `TranslateTransform`，松手后再提交中心并重建一次地图。
- 修复远地地图拖动或缩放崩溃：投影反算对经度进行日期变更线环绕，对纬度限制在有效范围；正向投影使用最短经度差。
- 远地事件固定使用 Overview，不再根据缩放级别加载仅覆盖日本的中精度或高精度 GeoJSON，也不再为远地视口计算日本 LOD 裁剪范围。

### 本版本未包含

- 未增加全球底图、全球行政区或远地震度图层；远地地图仍只显示可用震源。
- 未修改高精度 GeoJSON、索引格式、20% 缓冲范围或当前/上一范围缓存数量。

### 验证结果

- 先用测试复现 `193.058°` 非法经度异常和远地事件错误请求日本高精度 LOD，修复后两项通过。
- 全量测试：Core 102 项、Infrastructure 65 项、App 120 项，共 287 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：降低高精度拖动重绘开销，并修复远地地图跨日期变更线时的交互崩溃。

提交日志：`0.63.1: 优化高精度拖动并修复远地地图崩溃`

## 0.63.0 - 2026-08-25

### 已实现

- 重新修复高精度地图拖到未加载范围后回到震源的问题：拖动期间只累计像素偏移，松开时提交唯一地理中心并清零偏移；几何替换始终以替换前画布上实际显示的中心为准，异步请求携带的旧中心不再覆盖用户后续拖动。
- 增加临时 `[MapDebug]` 控制台/调试输出，覆盖鼠标交互、最终渲染中心、LOD generation、请求范围、中精度兜底、高精度缓存命中和几何替换中心，便于现场确认回弹发生阶段。
- 接入 JMA XML 与 P2PQuake 远地地震。XML 使用 `Head/Title=遠地地震に関する情報`，P2P 使用 `issue.type=Foreign`；自由评论含“噴火”时归类为远地火山喷发。
- P2PQuake 的 `depth=-1`、`magnitude=-1` 和越界坐标哨兵值映射为未知，不再让单条震度速報或远地报文导致整批解析失败。
- 远地地震和远地火山喷发进入事件列表；XML/P2P 同时存在时继续按时间和坐标合并并优先显示 XML。地图只显示可用震源，不绘制远地震度区域、边界、市町村或观测点，并自动使用 `ZoomLevel=0.5`。
- SQLite 报文负载新增可选 `DistantEarthquakeKind` 字段，旧缓存缺失该字段时保持兼容。

### 本版本未包含

- 未接入全球行政区或远地震度底图；震源以外的远地地图图层暂不绘制。
- JMAXML/P2PQuake 没有独立的火山事件枚举，本版本依据官方自由评论中的“噴火”判定，原始 XML/JSON 仍完整保留。
- `[MapDebug]` 是临时诊断输出，问题确认稳定后可单独删除。

### 验证结果

- 全量测试：Core 102 项、Infrastructure 65 项、App 118 项，共 285 项通过。
- 新增真实结构远地火山与普通远地地震 XML/P2P、未知哨兵值、跨来源合并、SQLite 往返、列表文本、远地震源图层和持久地图中心回归测试。
- Release 构建：0 个警告、0 个错误。
- 真实来源验证：官方 XML `20260825113507_0_VXSE53_010000.xml` 与 P2PQuake `6a8d7e0c64680b00075e0ee5` 均写入 SQLite，类型为远地火山喷发，震级和深度为未知；两者时间和坐标满足跨来源合并条件。
- `git diff --check` 通过。

版本总结：接入远地地震/火山事件，并用单一已提交中心和可观察日志重新修复高精度重载回震源。

提交日志：`0.63.0: 接入远地地震并修复高精度地图中心`

## 0.62.5 - 2026-08-25

### 已实现

- 修复高精度重载时中心仍回到震源的问题：地图视图新增持久视口中心，拖动和滚轮会实时更新，LOD 替换始终优先恢复该中心。
- 仅在切换事件、复位或主动定位时清除持久视口中心；中精度兜底和高精度替换不会清除它。
- 增加持久中心优先级回归测试，并保留高精度重载顺序和缓存命中测试。

### 本版本未包含

- 未改变高精度资源格式、缓存范围、全局缩放范围或数据源逻辑。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 118 项，共 279 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：将地图中心从瞬时平移量提升为持久视口状态，修复高精度重载回震源。

提交日志：`0.62.5: 修复高精度重载回震源`

## 0.62.4 - 2026-08-25

### 已实现

- 修复高精度异步重载中旧请求抢先提交中精度、导致新请求中心被覆盖的问题；提交兜底前后均校验加载 generation 和取消状态。
- 新加载请求显式中心优先于旧 pending 中心，几何替换完成后继续使用最近一次拖动位置，不再回到震源。
- 增加旧 pending 中心被新请求中心覆盖的回归测试。

### 本版本未包含

- 未改变高精度缓存策略、全局缩放范围、数据源和 SQLite 契约。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 118 项，共 279 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：修复高精度加载完成后地图中心仍被旧状态覆盖的问题。

提交日志：`0.62.4: 修复高精度加载后的地图中心保持`

## 0.62.3 - 2026-08-25

### 已实现

- 修复高精度越界时中精度兜底不可观察的问题：切换高精度前先提交中精度几何，并让出一次 UI 调度周期；状态栏显示“中精度兜底，正在加载高精度”。
- 将加载请求开始时的地理中心显式传递到几何替换事件，替代仅依赖替换时视图反算中心的时序策略；高精度加载完成后保持拖动后的当前中心。
- 为上一范围高精度恢复增加可观察状态“高精度 · 缓存”，并用回归测试确认回拖命中缓存而不是重新加载。

### 本版本未包含

- 未改变高精度资源索引格式、物理瓦片拆分、全局 `ZoomLevel` 定义、`max_small=0.5`、`max_big=24` 或高精度阈值 `12`。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 117 项，共 278 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：让中精度兜底、中心保持和上一范围缓存命中变成实际可见且可验证的运行时行为。

提交日志：`0.62.3: 修复高精度重载兜底与中心保持`

## 0.62.2 - 2026-08-25

### 已实现

- 高精度视野越界时先切换到已缓存的中精度几何作为临时底图，再异步加载新视野高精度区域、市町村和轮廓；加载成功后原子替换并保持当前地理中心。
- 增加高精度请求 generation 校验，旧请求即使晚完成也不能覆盖最新视野；最多保留当前和上一高精度范围，回拖到上一范围时直接恢复缓存，避免重复解析。
- 只在确认本次请求会发生 LOD 切换或高精度视野重载时保存中心，普通滚轮缩放不会重复应用平移量。
- 增加高精度越界判定、旧范围恢复和中/高精度替换回归测试。

### 本版本未包含

- 未拆分物理瓦片；当前采用索引按视野读取，并保留当前/上一高精度几何，后续根据实际内存指标再评估 LRU 瓦片缓存。
- 未改变全局 `ZoomLevel` 定义、`max_small=0.5`、`max_big=24` 或高精度阈值 `12`。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 117 项，共 278 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：实现高精度视野实时重载、中精度临时兜底和地图中心稳定保持。

提交日志：`0.62.2: 实现高精度地图实时加载与中心保持`

## 0.62.1 - 2026-08-25

### 已实现

- 修复事件跟随状态持续强制使用中精度的问题；按钮缩放和其他手动操作会退出跟随，达到高精度阈值后可加载高精度区域、市町村和轮廓资源。
- 修复中精度图层重建时重复 `FocusedCoordinate` 通知清空平移量的问题，并修正跟随状态判断的条件优先级，手动拖动后保持当前地图中心。
- 修复自动刷新退避混入 `p2pquake-ws` 流式状态的问题；HTTP 实时源继续按分钟轮询，单轮未预期异常只标记失败并继续后续刷新。
- 增加高精度选择、重复焦点通知和 HTTP 来源过滤回归测试。

### 本版本未包含

- 未改变全局 `ZoomLevel` 定义、`max_small=0.5`、`max_big=24` 或高精度阈值 `12`。
- 未删除临时 `ZoomLevel` UI 调试显示。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 115 项，共 276 项通过。
- Release 构建：0 个警告、0 个错误。
- `git diff --check` 通过。

版本总结：恢复高精度 LOD 入口、可靠 HTTP 自动刷新并修复中精度拖动回弹。

提交日志：`0.62.1: 修复自动刷新、地图高精度加载和中精度拖动`

## 0.62.0 - 2026-08-25

### 已实现

- 将 `ZoomLevel` 改为全局缩放级别：`1.0` 固定表示全国概览自动适配，事件自动适配按事件比例相对全国比例换算，不再出现全国和事件都显示 `1.0` 的矛盾。
- 地图投影统一使用启动时全国概览范围作为缩放基准；中/高精度局部几何替换不会改变 `ZoomLevel` 的全局含义。
- 放大和缩小改为全局级别 `+1/-1`，每级实际像素比例约为 `1.25x`；保留 `max_small=0.5` 和 `max_big=24` 的范围限制。
- 增加全局缩放基准、比例换算和级别递进单元测试。

### 本版本未包含

- 未改变地图中心、平移、事件来源优先级、LOD 资源格式和历史报文数据。
- 临时 `ZoomLevel` UI 调试显示仍保留，后续可单独移除。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 112 项，共 273 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：统一地图全局缩放级别，消除全国视图和事件视图的数值歧义。

提交日志：`0.62.0: 统一地图全局缩放级别`

## 0.61.1 - 2026-08-25

### 已实现

- 修复手动滚轮关闭跟随时误保留 `FocusMode=JapanOverview` 的问题，事件视图不再在第一次滚轮时跳回日本全图。
- 修复切换事件或来源报文时只重置倍率、不恢复事件焦点的问题；自动适配现在同时恢复 `FocusMode=SelectedEvent` 和跟随状态。
- 新增 `GeometryChanging`，在概览/中精度/高精度几何替换前按当前投影、缩放和像素平移保存画布中心。
- 为两个高精度 GeoJSON 生成 `.index.json` Feature 偏移和经纬度包络索引；加载高精度地图时只读取当前视野及缓冲范围内的 Feature，并校验源文件长度。
- 新增手动视图状态、自动适配状态、几何替换时序和高精度索引过滤回归测试。

### 本版本未包含

- 未改变 `max_small=0.5`、`max_big=24`、中/高精度阈值或地震事件合并规则。
- 未将高精度资源拆分为瓦片；索引文件失配时仍保留完整解析回退路径。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 110 项，共 271 项通过。
- Release 构建：0 个警告、0 个错误。
- 高精度局部视野加载：索引启用后约 `139 ms`，区域 9 个、市町村 52 个；索引生成前同一视野约 `1594 ms`。
- `git diff --check` 通过。

版本总结：修复事件地图手动缩放、报文切换、LOD 中心时序和高精度加载性能问题。

提交日志：`0.61.1: 修复事件地图状态与高精度加载`

## 0.61.0 - 2026-08-25

### 已实现

- 增加显式自动适配基线：地图加载和每次切换地震情报时恢复 `ZoomLevel=1` 的事件包络适配视野，滚轮缩放从该基线继续，不再从 `max_small=0.5` 重新开始。
- 增加 `ViewedReportKey` 变化通知，报文或事件身份变化时自动重置手动焦点、画布偏移并重新检查 LOD。
- 修复 Overview→Medium/High 几何替换的中心保存：`DetailLevel` 和 `GeometrySource` 任一变化都会在替换前保存当前投影中心，替换后继续使用该中心。
- 将高精度触发阈值从工作区原值 `6` 调整为 `12`，降低常用缩放下高精度大文件解析的频率；保留按视野和缓冲范围过滤的高精度加载逻辑。
- 增加自动适配基线回归测试，并保留地图中心和 LOD 切换测试。

### 本版本未包含

- 未改变 `max_small=0.5`、`max_big=24` 的手动缩放上下限。
- 未改变地震事件合并、报文来源优先级、SQLite schema 或地图投影坐标系。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 107 项，共 268 项通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：修复自动适配后的滚轮起点、报文切换自动适配、LOD 中心跳动和高精度过早加载。

提交日志：`0.61.0: 修复地图自动适配与LOD切换中心`

## 0.61.2 - 2026-08-25

### 已实现

- 修复高精度地图手动平移越过已加载视野后，连续 LOD 几何替换覆盖待恢复中心、地图回到震源位置的问题；一次渲染完成前只保留第一次捕获的画布中心。
- 在地图状态区增加临时 `ZoomLevel` 实时显示，随缩放属性通知更新。
- 增加连续几何替换中心保护的单元测试。

### 本版本未包含

- 临时 `ZoomLevel` 调试显示不会作为长期 UI 契约，后续可单独移除。
- 未改变地图缩放上下限、LOD 阈值、地图资源格式或事件来源逻辑。

### 验证结果

- 地图投影回归测试：2 项通过。
- 全量测试：Core 99 项、Infrastructure 62 项、App 111 项，共 272 项通过。
- Release 构建：0 个警告、0 个错误。

版本总结：修复高精度地图拖动回弹，并临时显示实时缩放值。

提交日志：`0.61.2: 修复高精度地图拖动回弹并显示缩放值`

## 0.61.1 - 2026-08-25

### 已实现

- 修复手动滚轮关闭跟随时误保留 `FocusMode=JapanOverview` 的问题，事件视图不再在第一次滚轮时跳回日本全图。
- 修复切换事件或来源报文时只重置倍率、不恢复事件焦点的问题；自动适配现在同时恢复 `FocusMode=SelectedEvent` 和跟随状态。
- 新增 `GeometryChanging`，在概览/中精度/高精度几何替换前按当前投影、缩放和像素平移保存画布中心。
- 为两个高精度 GeoJSON 生成 `.index.json` Feature 偏移和经纬度包络索引；加载高精度地图时只读取当前视野及缓冲范围内的 Feature，并校验源文件长度。
- 新增手动视图状态、自动适配状态、几何替换时序和高精度索引过滤回归测试。

### 本版本未包含

- 未改变 `max_small=0.5`、`max_big=24`、中/高精度阈值或地震事件合并规则。
- 未将高精度资源拆分为瓦片；索引文件失配时仍保留完整解析回退路径。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 110 项，共 271 项通过。
- Release 构建：0 个警告、0 个错误。
- 高精度局部视野加载：索引启用后约 `139 ms`，区域 9 个、市町村 52 个；索引生成前同一视野约 `1594 ms`。
- `git diff --check` 通过。

版本总结：修复事件地图手动缩放、报文切换、LOD 中心时序和高精度加载性能问题。

提交日志：`0.61.1: 修复事件地图状态与高精度加载`

## 0.61.0 - 2026-08-25

### 已实现

- 增加显式自动适配基线：地图加载和每次切换地震情报时恢复 `ZoomLevel=1` 的事件包络适配视野，滚轮缩放从该基线继续，不再从 `max_small=0.5` 重新开始。
- 增加 `ViewedReportKey` 变化通知，报文或事件身份变化时自动重置手动焦点、画布偏移并重新检查 LOD。
- 修复 Overview→Medium/High 几何替换的中心保存：`DetailLevel` 和 `GeometrySource` 任一变化都会在替换前保存当前投影中心，替换后继续使用该中心。
- 将高精度触发阈值从工作区原值 `6` 调整为 `12`，降低常用缩放下高精度大文件解析的频率；保留按视野和缓冲范围过滤的高精度加载逻辑。
- 增加自动适配基线回归测试，并保留地图中心和 LOD 切换测试。

### 本版本未包含

- 未改变 `max_small=0.5`、`max_big=24` 的手动缩放上下限。
- 未改变地震事件合并、报文来源优先级、SQLite schema 或地图投影坐标系。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 107 项，共 268 项通过。
- Release 构建：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：修复自动适配后的滚轮起点、报文切换自动适配、LOD 中心跳动和高精度过早加载。

提交日志：`0.61.0: 修复地图自动适配与LOD切换中心`

## 0.60.0 - 2026-08-25

### 已实现

- 将中精度与高精度分界从 `ZoomLevel > 8` 放宽到 `ZoomLevel > 12`，让中精度覆盖更宽的常用缩放范围。
- 高精度区域、市町村和区域轮廓统一按同一视野范围加载；高精度边界由命中的高精度区域 Polygon 轮廓生成，不再固定复用中精度边界。
- 高精度资源按当前画布包络加 20% 缓冲范围过滤，只保留视野及周边小范围几何；窗口缩放或平移越过已加载包络时重新加载，避免一次性把全国高精度资源转成运行时对象。
- GeoJSON 文件改为从文件流解析，避免加载高精度资源时额外保留完整 UTF-16 文本副本；过滤后只保留命中的几何对象。
- 增加 Polygon/LineString 视野过滤、高精度轮廓生成和高精度切换回归测试。

### 本版本未包含

- 没有生成独立的高精度拓扑相邻边界文件；高精度区域轮廓由高精度区域 Polygon 派生，震度边界仍按当前区域代码投影。
- 未改变地图投影坐标系、缩放上下限、事件数据、SQLite schema 或来源合并规则。

### 验证结果

- App 测试：106 项通过；全量测试：Core 99 项、Infrastructure 62 项、App 106 项，共 267 项通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore`：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：扩大中精度适用范围，按视野裁剪高精度地图，并让区域轮廓同步切换以降低内存峰值。

提交日志：`0.60.0: 按视野加载高精度地图并同步区域轮廓`

## 0.59.0 - 2026-08-25

### 已实现

- 地图缩放统一限制为 `max_small=0.5` 到 `max_big=24` 倍，放大按钮、缩小按钮、滚轮、定位和其他自动调整入口都通过同一范围钳制；保留 `MaximumZoomLevel` 兼容常量并将其指向新的最大值。
- 地图 LOD 扩展为概览、中精度和高精度三档：`ZoomLevel > 2` 加载 `0.003°` 中精度资源，`ZoomLevel > 8` 按需加载现有高精度区域/市町村资源；高精度没有独立边界文件时沿用中精度拓扑边界，加载失败保留当前可见图层。
- 中精度或高精度几何替换时保存当前画布中心经纬度，重新投影时恢复该中心，避免精度变化把地图强制移动到新资源边界中心。
- 增加高精度 LOD 切换和新的缩放上下限回归测试，并同步更新地图资源和工程实现文档。

### 本版本未包含

- 未新增高精度区域边界资源；高精度区域边界继续使用已审计的中精度拓扑边界。
- 未改变地图投影坐标系、事件数据、SQLite schema 或来源合并规则。

### 验证结果

- App 地图测试：103 项通过；全量测试：Core 99 项、Infrastructure 62 项、App 103 项，共 264 项通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore`：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：统一地图缩放边界，接入高精度按需 LOD，并保持精度切换时的地图中心。

提交日志：`0.59.0: 统一地图缩放边界并接入高精度LOD`

## 0.58.3 - 2026-08-25

### 已实现

- 修复 JMA XML Feed 文件名时间的时区解析：文件名前 14 位按 UTC 解释，再与报文 `IssuedAt` 的 `DateTimeOffset` 比较，避免 9 小时偏差把新 XML 误判为早于增量起点。
- 使用 JMA 官方长期 Feed 对账并补入 10 个缺失详情 XML；8 月 23–25 日 24 个支持的 `VXSE51/52/53` Feed 条目现已全部进入 SQLite 缓存。
- 增加 UTC 文件名与 JST 报文发布时间的增量回归测试，保留 XML/P2P 来源隔离和幂等去重行为。

### 本版本未包含

- 只存在于更早历史 Feed 快照、但当前工作区没有详情原文的 XML 未伪造补入；JMA 长期 Feed 没有历史分页，需要另外取得官方详情 XML 才能补齐。

### 验证结果

- 全量测试：Core 99 项、Infrastructure 62 项、App 101 项通过，合计 262 项通过。
- 本机缓存核对：XML 报文由 59 条增加到 69 条；8 月 23–25 日目标 Feed 条目 24/24 已缓存。
- `dotnet build EarthquakeShow.sln --no-restore`：0 个警告、0 个错误；`git diff --check` 通过。

版本总结：修复 JMA XML 文件名 UTC/JST 偏差并补齐 8 月 23–25 日缺失 XML。

提交日志：`0.58.3: 修复XML文件名时区并补齐缺失地震情报`

## 0.58.2 - 2026-08-25

### 已实现

- 修复多来源刷新隔离：JMA XML 请求超时或非用户取消不会中断后续 P2PQuake 刷新，来源状态单独记录为断开，已有缓存继续保留。
- 保持 XML 增量起点按 `source_id` 计算，P2PQuake 的最新报文不会推进或完成 XML 的同步判断。
- XML Feed 解析兼容缺少 `type="application/xml"` 的绝对链接；短 Feed 为空或覆盖不足时仍尝试长期 `eqvol_l.xml` 回补，单条详情超时不会丢弃同批成功报文。
- 新增 XML 超时、Feed 链接兼容、来源独立增量起点和 XML 默认优先展示的单元测试。

### 本版本未包含

- 本机 `tmp/eqvol.xml` 中的 `20260824035723_0_VXSE53_010000.xml` 没有对应详情原文，当前网络权限也无法下载，因此未伪造写入缓存；该条已经早于当前 XML 增量起点，需在可联网环境重新抓取详情或提供原始 XML 后幂等补入。

### 验证结果

- 定向 Infrastructure 测试：5 项通过；Core XML 优先测试：1 项通过；App 默认选择测试：1 项通过。
- 本机缓存只读核对：42 个长期 Feed 回补详情全部已入库；另发现 1 个 Feed 条目缺少本地详情原文。

版本总结：隔离多来源刷新失败，修复 XML Feed 回补边界并落实 XML 默认优先展示。

提交日志：`0.58.2: 修复多来源XML增量漏报并落实XML优先级`

## 0.58.0 - 2026-08-25

### 已实现

- P2PQuake REST 和 WebSocket 报文共用 JMA 观测点目录，按去除都道府县前缀后的正式观测点名称补齐经纬度；详情和地图不再把可匹配的 P2P 观测点显示为“位置未知”。
- 地图跟随工具由复选框改为一次性按钮；点击后重新启用跟随并以震源坐标为中心，手动平移或缩放仍会退出跟随。
- 选中事件时根据震源、已解析观测点和区域/市町村几何计算事件包络，使用约 10% 视口留白自适应缩放；无震源时保留区域包络中心回退，并对点事件设置最小包络，避免无限放大。
- 增加 P2P 坐标解析、震源优先定位和事件包络回归测试；完成 Leaflet、Mapbox GL JS、OpenLayers 官方 `fit`/`fitBounds` 方案调研。

### 本版本未包含

- 不改变事件合并、来源切换和 SQLite 数据契约；没有正式目录坐标的 P2P 观测点仍会显示“位置未知”。

### 验证结果

- `dotnet test EarthquakeShow.sln --no-restore`：Core 98 项、Infrastructure 57 项、App 99 项通过，合计 254 项通过。
- `dotnet build EarthquakeShow.sln --no-restore`：通过，0 个警告、0 个错误。
- `git diff --check`：通过。

版本总结：补齐 P2P 观测点坐标，并将地图跟随改为震源中心的自适应事件视野按钮。

提交日志：`0.58.0: 修复P2P观测点坐标并优化震源跟随地图`

## 0.58.1 - 2026-08-25

### 已实现

- 修复震源不在震度区域包络中心时的地图裁切：事件视野改为以震源为中心重建对称包络，保证区域和观测点完整落在可见范围内，并收紧视口留白以提高地图占用率。
- 修复自动跟随仍使用概览几何的问题：跟随和初始事件视野会主动请求中精度地图资源，普通手动缩放仍按原有阈值切换 LOD。
- 增加事件包络完整性和基础缩放自动加载中精度的回归测试。

### 本版本未包含

- 不新增高于现有中精度资源的第三档运行时地图资源；高精度资源仍按既有规划保留为后续工作。

### 验证结果

- `dotnet test EarthquakeShow.sln --no-build --verbosity minimal`：Core 98 项、Infrastructure 57 项、App 100 项通过，合计 255 项通过。
- `dotnet build EarthquakeShow.sln --no-restore`：通过，0 个警告、0 个错误。
- `git diff --check`：通过。

版本总结：修复震源跟随视野裁切并让自动视野使用中精度地图。

提交日志：`0.58.1: 修复事件视野裁切并提升自动跟随地图精度`

## 0.57.0 - 2026-08-25

### 已实现

- 新增 `jma-intensity-regions.json` 四层关系目录，保存都道府县、JMA 震度区域、市町村及其父级代码，并由生成脚本记录未匹配诊断。
- XML 区域按目录中的区域名补齐都道府县代码/名称；详情观测树调整为“都道府县 → 区域 → 市町村 → 观测点”四层。
- P2PQuake 使用都道府县和地址市町村别名查询正式 JMA 市町村/区域代码；无法命中时保留来源内回退键和原始观测点。
- P2PQuake 正式市町村代码可直接命中现有市町村 GeoJSON，地图按 XML 相同的市町村面、区域面、震源和观测点图层显示。
- 增加 XML 都道府县推断、P2P 正式代码、四层树和 P2P 地图图层单元测试。

### 本版本未包含

- 目录中没有正式代码的离岛或无效 GIS 记录不会伪造市町村代码；这类观测保留在详情数据中，但地图可能没有对应面。

### 验证结果

- `dotnet test EarthquakeShow.sln --no-restore`：Core 98 项、Infrastructure 57 项、App 98 项通过，合计 253 项通过。
- `dotnet build EarthquakeShow.sln --no-restore`：通过，0 个警告、0 个错误。
- `git diff --check`：通过。

版本总结：建立四层震度关系目录，补齐 XML/P2P 层级并让 P2P 市町村震度复用地图。

提交日志：`0.57.0: 建立四层震度关系并显示P2P地图`

## 0.56.0 - 2026-08-25

### 已实现

- SQLite schema 升级到 v3，启动迁移会删除历史 `jma-json` 地震报文和来源状态；后续读取也会保持该来源不再回流。
- p2pquake 观测点解析增加都道府县、从地址前缀提取的市町村和区域/市町村最大震度聚合，详情树可以按“区域 → 市町村 → 观测点”显示。
- `p2pquake` 与 `jma-xml` 在发生时间、震源坐标和震级满足现有关联阈值时合并为一个 `EarthquakeEvent`，保留双方报文时间线并优先使用 JMA XML 事件 ID。
- 详情页新增来源切换按钮，可在已合并事件的 XML 与 P2P 报文之间切换；移除“可能关联来源”展示区块。
- 增加跨源合并、市町村解析、历史 JSON 清理和来源切换单元测试，并修复地震/海啸 SQLite schema key 互相影响的问题。

### 本版本未包含

- p2pquake 没有官方市町村代码和观测点坐标；当前市町村代码是来源内稳定派生键，观测点仍显示为无坐标。
- 不能可靠匹配的 XML/P2P 报文仍保留为独立事件，不使用模糊名称强制合并。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 97 项、Infrastructure 56 项、App 97 项通过，合计 250 项通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，0 个警告、0 个错误。
- `git diff --check`：通过。

版本总结：清理历史 JMA JSON，合并 XML/P2P 同一事件，并补全 P2P 市町村观测层级。

提交日志：`0.56.0: 清理JMA JSON并合并XML与P2P地震事件`

## 0.55.3 - 2026-08-24

### 已实现

- 停止生产环境对 JMA `list.json`（`jma-json`）的实时请求和新报文写入，避免摘要源产生过多或重复事件。
- 保留 `JmaJsonEarthquakeSource` 解析器、固定样本和 SQLite 中已有的历史 `jma-json` 报文；本版本不删除历史缓存。
- 实时地震来源调整为 JMA XML（`jma-xml`）和 P2PQuake（`p2pquake`），XML 继续负责 `eqvol.xml` 实时 Feed、`eqvol_l.xml` 增量回补以及 `VXSE51/52/53` 详情。
- 修复移除 JSON 后自动刷新条件仍引用旧 JSON 字段的问题，确保 XML/P2P 和海啸来源继续启动刷新循环。
- 增加 App 回归测试，验证生产来源列表不包含 `jma-json`。

### 本版本未包含

- 不自动删除数据库中已经保存的 `jma-json` 报文；页面仍可能显示这些历史摘要，清理历史数据需要单独的迁移设计。
- XML 尚未覆盖 JMA `list.json` 的全部摘要范围；XML Feed 实际可用范围和缺失字段仍需后续逐项核验。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 54 项、App 97 项通过，合计 246 项通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore`：通过。
- `git diff --check`：通过。

版本总结：禁用 JMA JSON 实时接收，改由 JMA XML 主来源继续补全地震信息。

提交日志：`0.55.3: 禁用JMA JSON并以XML作为地震主来源`

## 0.55.0 - 2026-08-24

### 已实现

- 接入 JMA `20240520_AreaTsunami_GIS.zip`，转换为随应用发布的海啸预报区沿岸线 GeoJSON。
- 页面新增海啸预报区地图：按 `ForecastArea.Code` 匹配沿岸线，未收到该区域报文时使用中性海岸线，收到等级后按海啸等级着色。
- 地图资源按预报区代码聚合为 67 个图层，使用约 0.01° 简化参数，运行时按代码分组为 WPF `StreamGeometry`，避免为每条原始线段创建独立视觉元素。
- 地图明确区分官方沿岸线和观测点：当前 JMAXML 海啸观测站只有名称/代码而没有经纬度，因此暂不绘制观测点，不使用地震站点目录猜测位置。
- 增加 GeoJSON `LineString/MultiLineString` 解析测试，验证预报区代码和经纬度包络。

### 本版本未包含

- 尚未接入带官方海啸观测站代码和坐标的站点目录，因此沿岸观测和近海观测仍只在列表中显示。
- 尚未实现海啸地图的鼠标缩放、平移、站点点击和解除区域筛选。

### 验证结果

- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 96 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 51 项、App 96 项通过，合计 242 项通过。
- `git diff --check`：通过。

版本总结：接入 JMA 海啸预报区沿岸线地图，并明确观测点坐标缺口。

提交日志：`0.55.0: 接入海啸预报区沿岸线地图`

## 0.55.1 - 2026-08-24

### 已实现

- 修复海啸按钮进入页面后地图不显示的问题：海啸地图移出“必须选中报文”的详情容器，地图只由沿岸线资源是否可用控制；没有选中报文时仍显示中性 JMA 预报区沿岸线。
- 修复 JMA `list.json` 中 `ser` 字段在字符串和数字之间混用导致整批列表解析失败的问题；目标 `20260824125441` 报文现在可被正常解析。
- 修复 JMA XML 增量回补先截取 Feed 条目导致离线期间报文被遗漏的问题；增量模式先读取完整 Feed，再按缓存时间筛选，必要时合并长期 Feed。
- 区域和市町村轮廓使用圆角连接和圆端点，降低中精度简化几何在锐角处产生“刺”的视觉效果，不增加坐标数量或运行时地图内存。
- 调查官方海啸站点坐标目录：JMA [JMAXML 技术资料](https://xml.kishou.go.jp/tec_material.html) 发布的代码表压缩包包含 `20250630_PointTidalLevel.xls`，它是当前最可信的潮位/海啸站点目录候选；本版本只记录来源和核验边界，不把未确认字段的旧式 XLS 直接作为运行时资源。

### 本版本未包含

- 尚未把 `20250630_PointTidalLevel.xls` 转换为运行时站点目录；在确认站点代码、名称、纬度、经度、坐标基准及是否覆盖报文中的 `潮位観測点` 代码前，不显示海啸观测点地图标记。
- 海啸地图仍未实现站点点选、预报区命中筛选和独立的地图缩放/平移交互。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：串行执行，避免运行中的 WPF 进程锁定 App 输出 DLL。
- `git diff --check`：通过。

版本总结：修复海啸地图显示和 JMA 增量漏报，改善中精度轮廓渲染，并完成官方海啸站点目录来源调查。

提交日志：`0.55.1: 修复海啸地图与增量漏报并调查站点目录`

## 0.55.2 - 2026-08-24

### 已实现

- 修复 JMA `cod` 使用 `DDMM.m / DDDMM.m` 度分格式时的坐标解析：当原始绝对值超过纬度/经度十进制度范围时转换为十进制度，并继续执行最终范围校验。
- 增加回归测试，验证茨城县南部度分坐标不会导致整批 `list.json` 丢弃，且 2026-08-24 12:54:41 事件可以同时解析。
- 将用户下载的 JMA 官方 `20250630_PointTidalLevel.xls` 提取为 `jma-tsunami-tidal-points.json`，保留站点代码、机构、名称、地址、观测方式和可空坐标；106 条记录具备完整坐标，缺坐标记录不被伪造。
- 完成代码边界审计：该表的 `PointTidalLevel` 编码与现有 `VTSE` 样例的 `Station/Code` 不同，190 个样例站点代码与该目录交集为 0，因此本版本不把目录当作海啸站点地图的直接坐标源。
- 本机缓存已验证包含 `event_id=20260824125441` 的 `jma-json` 报文，事件列表数据已具备该事件的摘要记录。

### 本版本未包含

- 尚未把海啸潮位站目录接入海啸报文模型、地图点图层或点选定位；当前目录编码与 `VTSE Station.Code` 不兼容，后续必须获取匹配编码的官方目录。
- JMA `list.json` 摘要本身不包含完整观测点震度明细；目标事件的完整站点明细仍需官方详情 JSON/XML 报文，不能用摘要字段补造。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 54 项、App 96 项通过，合计 245 项通过。
- 官方站点目录审计：311 条记录、311 个唯一代码、106 条完整坐标、205 条缺少坐标、0 条坐标越界。
- 本机 SQLite 查询确认 `earthquake_reports` 存在 `20260824125441`，事件汇总表也存在相同事件 ID。
- `git diff --check`：通过。

版本总结：修复 JMA 度分坐标导致的地震报文遗漏，并提取官方海啸潮位站点目录资源。

提交日志：`0.55.2: 修复JMA度分坐标遗漏并提取海啸站点目录`

## 0.54.0 - 2026-08-24

### 已实现

- 将震度 6 弱调整为 `#B51255`，同步地图图例、事件列表、摘要和时间线徽标；与 6 强紫色保持区分。
- 海啸页新增“与上一报差异”区域，按同一 `EventId` 的相邻报文比较报文代码、状态、场景、最高等级、标题、信息项、预报区、沿岸观测和近海推定数量。
- 首报显示“首报，没有上一报可比较”，没有变化时显示“与上一报无字段差异”。
- 取消报文的等级差异明确显示为“解除”，不把取消报文中的旧等级当作当前警报。
- 增加 ViewModel 测试，验证取消报文状态和等级差异投影。

### 本版本未包含

- 尚未实现海啸预报区地图图层、解除区域地图显示和地图交互。

### 验证结果

- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 95 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 51 项、App 95 项通过，合计 241 项通过。
- `git diff --check`：通过。

版本总结：加深震度 6- 配色，并增加海啸报文逐字段差异比较。

提交日志：`0.54.0: 加深震度6-配色并增加海啸报文差异比较`

## 0.53.0 - 2026-08-24

### 已实现

- 修复海啸页布局覆盖左侧导航的问题：主工作区保留固定导航列，海啸内容只覆盖右侧内容列，地震/海啸按钮可以往返切换。
- 将震度 4 调整为 `#E89A3C`、震度 5 弱调整为 `#E85E3F`，并同步地图图例、事件列表、摘要和时间线徽标；震度 5 强保持 `#F04438`，三档保持明显区分。
- 海啸页新增同一 `EventId` 的报文时间线，按发布时间升序显示发布、订正和解除过程。
- 取消报文在时间线中显示为“解除”，不继续沿用旧报文的警报等级，避免误显示为仍在警报。
- 增加 ViewModel 回归测试，验证跨事件过滤、时间排序和解除节点投影。

### 本版本未包含

- 尚未实现海啸报文间逐字段差异比较。
- 尚未实现海啸预报区地图图层和解除区域地图显示。

### 验证结果

- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 95 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 51 项、App 95 项通过，合计 241 项通过。
- `git diff --check`：通过。

版本总结：修复海啸导航遮挡、调整震度 4/5- 配色，并增加海啸报文时间线与解除节点。

提交日志：`0.53.0: 修复海啸导航并增加报文时间线`

## 0.52.0 - 2026-08-24

### 已实现

- 海啸查询页新增“信息项”展开，显示当前等级、代码、上一状态和涉及区域。
- 新增“原始 XML”折叠区域，支持只读查看和复制完整 `SourcePayload`，复制后显示操作结果。
- 修复海啸导航按钮点击后页面不显示的问题：可见性触发器改为从 `Window.DataContext.IsTsunamiPageVisible` 读取主窗口状态。
- 新增 WPF 点击回归测试，实际触发海啸导航按钮并确认查询页变为可见。
- 新增 ViewModel 测试，验证信息项区域投影、原始 XML 可复制状态和复制反馈。

### 本版本未包含

- 尚未制作海啸报文时间线、解除过程和报文间差异比较。
- 原始 XML 当前只读显示和复制，不提供编辑、格式化或 XML Schema 校验。

### 验证结果

- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 94 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 51 项、App 94 项通过，合计 240 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过；`git diff --check`：通过。

版本总结：增加海啸信息项和原始 XML 查看，并修复海啸导航显示问题。

提交日志：`0.52.0: 增加海啸原始报文查看并修复导航`

## 0.51.0 - 2026-08-24

### 已实现

- 查询页当前报文新增预报区列表，显示区域、代码、等级、首波到达时间和最大高度。
- 新增沿岸观测列表，显示区域、观测站、站点代码、首波时间、最大高度和观测初报。
- 新增近海推定列表，显示区域、代码、首波时间和最大高度。
- 复用 `JmaTsunamiClassifier` 对 `KindName/KindCode` 分类：无海啸影响使用灰色，若干海面变动使用蓝色，津波注意报使用黄色，津波警报使用红色，大津波警报使用紫色，未知/调查中使用中性样式。
- 所有时间、高度、到达条件和观测初报都遵守可空契约；缺失值显示“未提供”，不会从其他字段推断等级或数值。
- 增加 App 测试验证注意报等级、预报区高度、到达条件、沿岸初报和近海推定字段投影。

### 本版本未包含

- 尚未加入海啸报文原始 XML 查看、完整结构化字段树和海啸地图图层。
- 当前颜色仅表示报文中已解析的海啸等级，不代表地图区域风险范围。

### 验证结果

- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 94 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 51 项、App 94 项通过，合计 240 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过；`git diff --check`：通过。

版本总结：细化海啸报文区域、观测和推定信息，并加入等级配色。

提交日志：`0.51.0: 细化海啸报文详情和等级配色`

## 0.50.0 - 2026-08-24

### 已实现

- 新增 `TsunamiPageView`，提供独立的 JMA 海啸报文查询页，不覆盖原地震情报页。
- 左侧导航新增“海啸”入口，可在地震页和海啸页之间切换；切换只改变页面可见性，不重置地震事件选择。
- 海啸页左侧显示报文列表和发布时间，右侧显示当前报文摘要、状态/场景、事件身份、接收时间和结构数量。
- 页面刷新按钮复用 `TsunamiPageViewModel.RefreshAsync()`，继续使用既有 SQLite 仓储和来源刷新链路。
- 空缓存、读取错误和报文刷新状态分别显示对应空状态，不伪造海啸报文或等级。
- 增加 WPF 页面加载测试和导航切换回归测试。

### 本版本未包含

- 尚未展示预报区明细、沿岸站点明细、近海观测和推定区域列表。
- 尚未制作海啸地图图层、海啸等级地图配色、详情树或报文原始 XML 展示。

### 验证结果

- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 93 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 51 项、App 93 项通过，合计 239 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过；`git diff --check`：通过。

版本总结：新增独立海啸报文查询页和地震/海啸页面切换。

提交日志：`0.50.0: 新增海啸报文查询页`

## 0.49.0 - 2026-08-24

### 已实现

- 新增 `TsunamiPageState` 和 `TsunamiPageViewModel`，建立海啸专页的数据状态边界。
- 从 `ITsunamiReportRepository` 读取海啸报文，按 `IssuedAt`、`ReceivedAt` 和来源身份稳定降序排序。
- 支持按 `EventId + SourceId + SourceMessageId` 选择报文；重新加载后如果报文仍存在则保留选择，否则选择最新报文。
- 提供只读加载和刷新操作，区分 `NotLoaded/Loading/Ready/Error`，保存来源状态、离线标记和错误信息。
- `MainWindowViewModel` 启动时加载海啸页状态，海啸来源刷新成功后重新读取仓储快照；新增 3 个 ViewModel 测试和主窗口集成断言。

### 本版本未包含

- 尚未制作海啸专页 XAML、导航入口、地图图层或报文详情布局。
- 页面状态的读取错误不会修改来源连接状态；来源是否在线仍由 `SourceStatuses` 单独表达。

### 验证结果

- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 92 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 51 项、App 92 项通过，合计 238 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过；`git diff --check`：通过。

版本总结：建立海啸报文专页的只读状态和查询基础。

提交日志：`0.49.0: 建立海啸报文页状态层`

## 0.48.0 - 2026-08-24

### 已实现

- 将 `MainWindowViewModel.TsunamiSourceStatuses` 接入主窗口底部状态栏，新增 `TsunamiSourceStatusText` 展示独立海啸来源状态。
- 状态栏支持在线、延迟、限流、解析失败、离线、状态未知和未启用文案；多个来源时按错误优先级显示更需要关注的状态。
- 保持地图、地震来源、WebSocket、缓存和自动刷新信息的原有布局与绑定，不新增页面，不改变海啸报文持久化和地震事件合并规则。
- 增加 App 回归断言，确认注入海啸源刷新后状态栏文本为“海啸：在线”。

### 本版本未包含

- 尚未制作独立海啸报文查询页、海啸地图图层或海啸详情树。
- 尚未为海啸状态增加独立颜色主题或 XAML 状态图标；当前使用与其他底部状态一致的低干扰文字样式。

### 验证结果

- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 89 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 51 项、App 89 项通过，合计 235 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过；`git diff --check`：通过。

版本总结：在主窗口状态栏展示海啸来源连接状态。

提交日志：`0.48.0: 展示海啸来源状态`

## 0.47.0 - 2026-08-24

### 已实现

- `MainWindowViewModel` 启动时初始化独立海啸 SQLite 仓储，并在首次刷新中调用 `JmaTsunamiXmlSource`。
- 自动刷新循环复用同一海啸刷新路径；仓储按 `jma-xml-tsunami` 来源已保存报文的最大 `IssuedAt` 请求增量数据，短 Feed 覆盖不足时继续由长期 Feed 回补。
- 新增 `TsunamiSourceStatuses` ViewModel 状态投影，启动离线时保留 `LastReceivedAt`，刷新成功或失败后同步状态。
- 海啸报文写入 `tsunami_reports`，不会传给 `EarthquakeEventMerger`，因此不会改变现有地震事件列表和地震详情时间线。
- 关闭窗口时等待初始化、刷新循环和海啸仓储释放；新增应用级回归测试验证海啸报文独立落库和关闭后无后台刷新残留。

### 本版本未包含

- 尚未制作海啸专页、海啸地图图层或海啸状态栏 XAML 展示；当前只公开 ViewModel 状态，供后续页面使用。
- 尚未接入 JMA 官网 JSON 海啸补充源或 P2PQuake 海啸接口，也不进行海啸报文与地震事件的自动合并。

### 验证结果

- `dotnet test tests\\EarthquakeShow.Core.Tests\\EarthquakeShow.Core.Tests.csproj --configuration Release --no-restore`：Core 95 项通过。
- `dotnet test tests\\EarthquakeShow.Infrastructure.Tests\\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：Infrastructure 51 项通过。
- `dotnet test tests\\EarthquakeShow.App.Tests\\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 89 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 95 项、Infrastructure 51 项、App 89 项通过，合计 235 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过；`git diff --check`：通过。

版本总结：接入海啸来源的应用启动刷新和独立状态持久化。

提交日志：`0.47.0: 接入海啸来源启动刷新和状态持久化`

## 0.46.0 - 2026-08-24

### 已实现

- 新增 `ITsunamiReportRepository` 和 `SqliteTsunamiReportRepository`，独立保存 JMA `VTSE41/51/52` 海啸报文，不把它们混入地震事件仓储。
- 新增 `tsunami_reports` SQLite 表，使用 `event_id + source_id + source_message_id` 去重；标准查询列保存报文身份和时间，完整字段及原始 XML 保存在显式 DTO JSON 中。
- SQLite schema 从 v1 升级到 v2；旧 v1 缓存启动时自动创建海啸表并更新版本，未知 schema 仍按既有策略拒绝读取。
- 支持读取全部海啸报文、按 `EventId` 查询和幂等 upsert；官方三份训练样例的预计高度、沿岸站点、近海观测和推定区域均有持久化回归测试。

### 本版本未包含

- `JmaTsunamiXmlSource` 尚未接入应用启动/自动刷新生命周期，当前需要调用仓储接口显式保存来源返回的报文。
- 尚未持久化海啸来源状态、实现海啸事件合并或制作海啸专页；现有地震事件列表和地震详情页行为不变。
- JMA 官网海啸 JSON 和 P2PQuake 海啸接口仍未接入。

### 验证结果

- `dotnet test tests\EarthquakeShow.Core.Tests\EarthquakeShow.Core.Tests.csproj --configuration Release --no-restore`：Core 95 项通过。
- `dotnet test tests\EarthquakeShow.Infrastructure.Tests\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：Infrastructure 50 项通过。
- `dotnet test tests\EarthquakeShow.App.Tests\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 88 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过；`git diff --check`：通过。

提交日志建议：`0.46.0: 接入 JMA 海啸报文 SQLite 持久化`

## 0.45.0 - 2026-08-24

### 已实现

- 新增 `IRealtimeTsunamiSource`、`IIncrementalTsunamiSource` 和 `TsunamiSourceFetchResult`，为海啸来源定义独立于地震报文的读取边界。
- 新增 `JmaTsunamiXmlSource`，读取 JMA 高频 `eqvol.xml` 和长期 `eqvol_l.xml`，筛选 `VTSE41`、`VTSE51`、`VTSE52`，下载 XML 原文并调用 `JmaTsunamiXmlParser`。
- 支持按缓存时间点过滤旧条目；短 Feed 覆盖不足时合并长期 Feed，按来源消息 ID 去重，并保留限流、网络、解析失败状态。
- 增加 Feed 过滤、来源身份、报告失败和长期 Feed 回补测试。
- 固定 JMA 官方样例包中的 `VTSE41`、`VTSE51`、`VTSE52` 训练 XML，验收预计到达、预计高度、沿岸站点、近海观测站和推定区域字段，并纳入离线数据校验。

### 本版本未包含

- 尚未接入 SQLite、WPF 或事件合并；当前适配器不会让海啸报文出现在事件列表中。
- 真实样例只用于 Core 解析和固定数据回归，不会作为实时事件展示。
- 尚未把已解析的预报区、观测站和推定区域接入 SQLite 或海啸页面。
- JMA 官网海啸 JSON 和 P2PQuake 海啸接口仍未接入。

### 验证结果

- `dotnet test tests\EarthquakeShow.Core.Tests\EarthquakeShow.Core.Tests.csproj --configuration Release --no-restore`：Core 95 项通过。
- `dotnet test tests\EarthquakeShow.Infrastructure.Tests\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：Infrastructure 47 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 95 项、Infrastructure 47 项、App 88 项通过，合计 230 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。
- `git diff --check`：通过；三份官方样例原文已固定并记录 SHA256。

提交日志建议：`0.45.0: 新增 JMA 海啸 XML Feed 适配器并固定官方样例`

## 0.44.0 - 2026-08-24

### 已实现

- 新增 `JmaTsunamiReport`、`JmaTsunamiInformationItem` 和 `JmaTsunamiArea`，独立保存 `VTSE*` 海啸报文的当前 `Kind/Code`、上一状态 `LastKind/Code`、报文元数据和预报区代码。
- 新增 `JmaTsunamiXmlParser`，按 `Head/Headline/Information/Item` 读取当前/上一状态和预报区，拒绝非 `VTSE*` 报文，并保留未知代码原值。
- 增加最小结构化海啸 XML 测试和非 `VTSE*` 拒绝测试。

### 本版本未包含

- 尚未接入 JMA Feed、官网海啸 JSON 或 SQLite；本版本不会在事件列表中显示新的海啸事件。
- 尚未核实完整 `TsunamiWarning` 数值映射、预计到达/高度/观测字段，也未制作海啸专页。
- 本轮仅完成接口调研和文档登记，没有改变运行时行为；未将未验证的 JSON 字段或海啸代码写入实现契约。

### 验证结果

- `dotnet test tests\\EarthquakeShow.Core.Tests\\EarthquakeShow.Core.Tests.csproj --configuration Release --no-restore`：Core 92 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 92 项、Infrastructure 44 项、App 88 项通过，合计 224 项；完整重建需先关闭正在运行的旧版 `EarthquakeShow.App`。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.44.0: 新增 VTSE 海啸报文结构化解析`

### 调研记录（不单独升版本）

- JMA 官方 XML 页面确认公开接口是 PULL 型 Atom Feed：高频 `eqvol.xml` 每分钟更新，长期 `eqvol_l.xml` 每小时更新并保留数天。
- 核对海啸报文类型：`VTSE41` 为津波警報・注意報・予報，`VTSE51` 为津波情報，`VTSE52` 为沖合の津波観測に関する情報。
- 确认 JMA 官网海啸 JSON 列表/详情地址存在，但该 JSON 没有公开版本化契约；暂不作为唯一生产数据源。
- 后续步骤：获取并固定真实 `VTSE41/51/52` 样例，验收预计到达/高度/观测字段后扩展 SQLite 契约，再制作海啸页面；来源适配器已在 `0.45.0` 完成第一切片。

## 0.43.2 - 2026-08-24

### 已实现

- 修复 JMA XML 使用 ASCII `5-`、`5+`、`6-`、`6+` 表示震度时被解析为 `Unknown` 的问题；这些值分别映射为 5 弱、5 强、6 弱、6 强。
- 读取已有 SQLite 缓存时，如果保存的 JMA 原始 XML 含上述记法，自动重新派生震度；无需重新下载历史报文即可修正 2026-08-23 茨城县南部事件中的 `小美玉市上玉里＊` 为 5 弱。
- 增加 XML 解析和 SQLite 旧缓存兼容回归测试。

### 本版本未包含

- 独立 `VTSE*` 海啸报文仍未接入；海啸专页将在获得真实报文样例并完成结构化模型后实现。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 90 项、Infrastructure 44 项、App 88 项通过，合计 222 项通过；由于旧版 `EarthquakeShow.App` 仍在运行，本轮未覆盖完整重建步骤。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.43.2: 修复 JMA XML 5弱震度解析`

## 0.43.1 - 2026-08-24

### 已实现

- 按 JMA 关联代码表中已确认的固定文本补充海啸边界：`津波なし` 显示为无海啸影响，`津波予報（若干の海面変動）` 显示为若干海面变动。
- 解除文本在等级判断前单独处理，保持“调查中”，避免“津波注意報解除”或警报解除被误判为当前仍在警报。
- 增加上述三类文本边界的单元测试。

### 本版本未包含

- 完整 `TsunamiWarning` 数值代码表仍未固化；不能从代码编号顺序推断等级。
- 独立 `VTSE*` 海啸报文及其预报区、观测数据仍未接入。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 86 项、Infrastructure 43 项、App 88 项通过，合计 217 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.43.1: 修正官方海啸文本边界分类`

## 0.43.0 - 2026-08-24

### 已实现

- 按 JMA 官方 JMAXML 代码管理表确认地震报文的海啸字段位置：`Body/Comments/ForecastComment` 下同时有 `Text` 和 `Code`。
- `EarthquakeReport` 新增 `TsunamiCommentCode`，XML 解析器保存与含“津波”的评论对应的代码，SQLite JSON 缓存同步持久化且兼容旧缓存。
- 新增 `TsunamiLevel` 和 `JmaTsunamiClassifier`；官方样例代码 `0215` 映射为 `NoConcern`，未知代码保留原始值并按明确文本或“津波 调查中”降级。
- 摘要和时间线使用结构化等级分类，通用模板仍不会因为括号内出现“大津波警报”而误判为大津波警报。
- 增加 JMAXML 代码保存、`0215` 分类和未知代码降级测试。

### 本版本未包含

- JMA 关联“地震火山相关代码表”中全部 `TsunamiWarning` 数值映射尚未全部固化；当前仅对已验证的 `0215` 做代码优先映射。
- 独立 `VTSE*` 海啸警报/注意报/解除报文尚未接入；其 `Kind/Code`、预报区和观测信息留待下一步。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 83 项、Infrastructure 43 项、App 88 项通过，合计 214 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.43.0: 接入 JMAXML 海啸代码基础解析`

## 0.42.0 - 2026-08-24

### 已实现

- 核对 JMA 官方 PULL 规范：短 `eqvol.xml` 只保留至少约 10 分钟，长期 `eqvol_l.xml` 每小时更新并保留数天地震火山入电。
- JMA XML 增量刷新在短 Feed 覆盖不足时自动请求并合并长期 Feed，保持实时刷新使用短 Feed，避免所有刷新都等待小时级 Feed。
- 从 JMA 官方长期 Feed 下载并用项目现有解析器校验，向本机 SQLite 幂等补入 2026-08-21 至 2026-08-24 的 42 条 `jma-xml` 报文。
- 增加长期 Feed 回退、跨 Feed 去重和覆盖诊断回归测试。

### 本版本未包含

- JMAXML `ForecastComment.Code` 的完整官方代码表和独立海啸报文仍未接入；海啸等级仍以明确文本为准。
- JMA 长期 Feed 也只有有限保留窗口，无法覆盖更早的历史缺口。

### 验证结果

- `dotnet test tests\EarthquakeShow.Core.Tests\EarthquakeShow.Core.Tests.csproj --configuration Release --no-restore`：Core 79 项通过。
- `dotnet test tests\EarthquakeShow.Infrastructure.Tests\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：Infrastructure 43 项通过。
- `dotnet test tests\EarthquakeShow.App.Tests\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 88 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：完整 Release 测试通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.42.0: 接入 JMA 长期 Feed 历史回补`

## 0.41.0 - 2026-08-24

### 已实现

- 启动后的首次实时刷新会读取缓存中各来源最新 `IssuedAt`；支持增量接口的数据源只请求该时间点及之后的 Feed 条目。
- JMA XML Feed 从文件名时间戳解析条目发布时间，过滤缓存时间点之前的条目，并在来源状态中记录 Feed 总条数、增量命中条数和覆盖起点。
- Feed 条目不足以覆盖缓存时间点时保留诊断信息，不伪造“已补齐全部离线期间报文”；来源消息仍按原身份写入 SQLite，由事件合并器去重。
- 增加 JMA XML 增量过滤和 SQLite 仓储传递最新缓存发布时间的回归测试。
- 修复未知但非空的海啸文本回退为“无心配”的问题；无法从文本确定等级时现在保持“津波 调查中”。

### 本版本未包含

- JMAXML `ForecastComment.Code` 的完整官方代码表和独立海啸报文仍未接入；当前海啸等级识别仍以明确的 `Text` 文本为准。
- JMA Feed 只保留上游当前可见的有限条目；离线时间超过 Feed 覆盖范围时，仍需后续接入 JMA 历史归档或用户提供的 XML 批量目录。

### 验证结果

- `dotnet test tests\EarthquakeShow.Core.Tests\EarthquakeShow.Core.Tests.csproj --configuration Release --no-restore`：Core 79 项通过。
- `dotnet test tests\EarthquakeShow.Infrastructure.Tests\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：Infrastructure 42 项通过。
- `dotnet test tests\EarthquakeShow.App.Tests\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore`：App 88 项通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 79、Infrastructure 42、App 88，共 209 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.41.0: 实现 JMA 离线增量回补`

## 0.40.0 - 2026-08-24

### 已实现

- 修复同一 JMA 地震先后使用临时 EventID 发布 `VXSE51`、`VXSE52`/`VXSE53` 时被拆成多个事件的问题。
- 仅对 `jma-xml` 来源执行临时身份归并：EventID 时间差不超过 60 秒、报文发布时间差不超过 180 秒、报文类型匹配且最大震度不冲突时才合并。
- 使用包含震源信息的正式 EventID 作为页面事件 ID；原始 `EarthquakeReport.EventId`、XML 负载和 SQLite 报文主键保持不变。
- 增加 `VXSE51 + VXSE52`、`VXSE51 + VXSE53`、时间窗口和震度冲突回归测试。
- 核对 JMAXML 海啸字段：当前地震 XML 的 `Body/Comments/ForecastComment` 包含 `<Text>` 和 `<Code>`；解析器目前只保存含“津波”的文本，未将代码映射为海啸等级。

### 本版本未包含

- 尚未接入独立 JMA 海啸报文或 `ForecastComment.Code` 的完整官方代码表，因此不能仅凭当前 XML 文本准确区分“津波注意报”和“津波警报”。
- 启动时离线增量回补在 `0.41.0` 实现；本版本不包含该功能。

### 验证结果

- `dotnet test tests\EarthquakeShow.Core.Tests\EarthquakeShow.Core.Tests.csproj --configuration Release --no-restore`：Core 79 项通过。
- `dotnet test tests\EarthquakeShow.App.Tests\EarthquakeShow.App.Tests.csproj --configuration Release --no-restore --no-build`：App 87 项通过。
- Infrastructure 40 项通过；全套测试重新构建时因正在运行的 `EarthquakeShow.App` 锁定 Release DLL，未能完成最终汇总。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.40.0: 归并 JMA 临时事件身份`

## 0.39.1 - 2026-08-24

### 已实现

- 修复 JMA 海啸通用模板“津波警报等（大津波警报・津波警报あるいは津波注意報）”被错误识别为“大津波警报”的问题。
- 对无法从通用模板确定具体等级的报文显示“津波 调查中”；明确包含具体等级的海啸文本继续按原规则显示。
- 增加 App 回归测试，确认熊本样例不会因括号中的枚举文字触发大津波警报状态。

### 本版本未包含

- 尚未接入 JMA `ForecastComment.Code` 或独立海啸报文，因此暂时不能仅凭当前熊本样例准确推断“津波注意报”；原始海啸文本保持不变。
- 尚未实现 JMA 临时 EventID 归并和启动离线期间增量回补。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 75、Infrastructure 40、App 87，共 202 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.39.1: 修复海啸通用模板误判`

## 0.39.0 - 2026-08-24

### 已实现

- 新增地图多级 LOD 的第一阶段：启动继续使用 `0.015°` 概览几何，`ZoomLevel > 2` 时异步读取同一 JMA GIS 源生成的 `0.003°` 中精度区域、市町村和拓扑边界几何。
- 中精度资源在后台读取完成后一次性替换三类地图几何，缩回 `ZoomLevel <= 2` 时恢复概览；加载期间和加载失败时保留上一层可见地图。
- 地图窗口加载、放大/缩小按钮、鼠标滚轮和事件定位均会检查当前 LOD；关闭地图 ViewModel 时取消未完成读取，避免后台任务继续持有资源。
- 修复 `tools/convert_jma_gis.py` 闭合环简化在结果少于三个点时回退整环的问题，并增加简化单调性自测。

### 本版本未包含

- 高精度 `0.0005°` 全量资源暂不接入运行时；后续需要按区域或离线瓦片分块后再评估。
- 尚未完成同一 Release 配置下的真实工作集峰值、LOD 切换耗时和人工截图基准；本版本的内存数字仍是资源级估算。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 75、Infrastructure 40、App 86，共 201 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。
- `python -B -X utf8 tools\convert_jma_gis.py --self-test`：3 项简化器自测通过。
- 中精度资源合计约 4.66 MB、约 170,936 个坐标；均由同一 JMA GIS 源生成，元数据保留版本和简化参数。

提交日志建议：`0.39.0: 接入地图中精度按需加载`

## 0.37.0 - 2026-08-21

### 已实现

- 地图支持鼠标左键拖动平移，拖动时使用地图控件捕获鼠标并显示移动光标，松开或失去捕获时正确结束拖动。
- 地图支持鼠标滚轮缩放；缩放前将指针位置反算为经纬度，缩放后调整像素平移量，使指针下的地理位置保持不动。
- 手动平移或滚轮缩放会暂停“跟随事件选择”，并保留当前事件中心作为手动视图基准；重新开启跟随、复位或定位事件会清理手动平移。
- 地图画布增加透明命中背景，空白地图区域也可以直接拖动和滚轮操作。

### 本版本未包含

- 未改变地图数据、投影坐标系、SQLite、数据契约或网络来源；仍使用低内存概览几何。
- 尚未实现摘要栏底部的报文导航和时间线宽度统一。

### 验证结果

- App 地图 ViewModel 测试验证手动操作关闭跟随、重新跟随清理焦点，以及缩放上下限和焦点保持。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 75、Infrastructure 40、App 84，共 199 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.37.0: 增加地图鼠标平移和滚轮缩放`

## 0.38.0 - 2026-08-21

### 已实现

- 摘要栏底部增加与时间线一致的“上一报”“下一报”和“返回最新”导航。
- 摘要导航直接复用 `EarthquakeDetailsViewModel` 现有报文切换逻辑；切换后摘要、时间线、地图和观测点树继续使用同一个当前报文快照。
- 首报禁用“上一报”，最新报禁用“下一报”；查看历史报文时启用“返回最新”。

### 本版本未包含

- 未改变报文身份、累计字段、SQLite、网络来源或地图震度图层规则。
- 尚未统一不同事件的时间线内容宽度；尚未切换高精度地图资源。

### 验证结果

- 复用既有 `CanGoPrevious`、`CanGoNext` 和 `CanReturnToLatest` 状态，不新增第二套导航状态或数据源。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 75、Infrastructure 40、App 84，共 199 项通过。

### 地图精度与内存调研结论

- 当前运行时区域概览、市町村概览和区域边界概览约为 `3.05 MB`、`4.37 MB` 和 `3.20 MB`，坐标数约为 `127,463`、`165,083` 和 `130,039`。
- 高精度区域和市町村资源约为 `62.94 MB`、`69.31 MB`，坐标数约为 `2,640,712`、`2,891,840`；启动时直接替换会显著增加解析峰值和 WPF 几何内存。
- 推荐后续采用“概览 + 中精度 + 高精度”多级 LOD，按缩放级别按需加载，并在替换前释放旧 `StreamGeometry`；本版本只完成调研，不生成新的运行时资源。

提交日志建议：`0.38.0: 增加摘要报文导航`

## 0.38.1 - 2026-08-21

### 已实现

- 时间线 `ListBoxItem` 和报文内容设置为横向拉伸，所有事件的时间线卡片默认占满详情栏可用宽度。
- 禁用时间线横向滚动条，避免长文本把不同事件的卡片宽度撑成不同尺寸；保留字段自身的省略和换行规则。

### 本版本未包含

- 未改变时间线字段内容、报文排序、地图、SQLite 或网络来源。
- 尚未接入中精度地图 LOD；本版本只完成候选资源生成和优化方案调研。

### 验证结果

- XAML 编译和完整测试通过，时间线导航状态与 `0.38.0` 保持兼容。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 75、Infrastructure 40、App 84，共 199 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。

### 地图优化调研摘要

- Mapbox 的公开性能指南将渲染成本归因于 source 数、layer 数和顶点数，并建议合并图层、使用矢量瓦片、移除不可见要素和减少几何顶点。
- OpenStreetMap 官方瓦片政策强调遵守缓存头、保留本地缓存和避免批量预取；这支持“按视口请求并缓存”的通用策略，但本项目仍使用离线 JMA 资源。
- Google Maps JavaScript API 的公开文档将栅格/矢量渲染、缩放级别、视口和标记碰撞作为独立能力；本项目可借鉴按视口和缩放级别管理图层的思想，不引入网页运行时。
- 当前概览资源合计约 `422,585` 个坐标。按坐标数组、WPF `StreamGeometry` 和解析峰值的保守估算，概览地图新增内存约 `25–55 MB`；区域和市町村高精度资源合计约 `5,532,552` 个坐标，若同时解析和绘制，地图相关新增内存可能达到约 `180–420 MB`。该区间是工程估算，不替代 Release 工作集实测。

提交日志建议：`0.38.1: 统一时间线宽度`

## 0.36.0 - 2026-08-21

### 已实现

- 将地图 `ZoomLevel` 上限从 `4` 提高到 `12`，保留每次 `1.25` 倍的渐进缩放步长。
- 缩放上限使用 ViewModel 常量统一约束；放大、缩小、复位、事件定位和跟随选择的既有语义不变。
- 使用低内存概览几何支持更大显示比例，不新增地图资源和运行时依赖。

### 本版本未包含

- 尚未实现鼠标左键拖动平移、滚轮缩放和以指针位置为中心的缩放；当前缩放仍通过地图工具按钮执行。
- 未改变地图投影、SQLite、数据契约或震度绘制规则；高倍缩放下概览几何的细节质量仍需结合鼠标交互人工验证。

### 验证结果

- App 地图 ViewModel 回归测试验证可放大到 `12` 倍并在上下限处稳定截断。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 75、Infrastructure 40、App 84，共 199 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。

提交日志建议：`0.36.0: 提高地图缩放上限`

## 0.35.1 - 2026-08-21

### 已实现

- 排查应用生命周期资源：SQLite 使用关闭连接池的短连接，WebSocket 枚举器和 `ClientWebSocket` 均可取消并释放，页面 ViewModel 的事件订阅和 `DispatcherTimer` 均有对应解绑；未发现持续累积的集合或未释放数据库连接。
- 主窗口改为两阶段异步关闭：先取消初始化、HTTP 自动刷新和 WebSocket 会话，等待后台任务结束，再释放 `HttpClient`、当前会话取消源、重启信号量、时钟和页面订阅，最后完成窗口关闭。
- 修复首次渲染与立即关闭并发时，已经释放的主 ViewModel 仍被初始化而抛出 `ObjectDisposedException` 的竞态。

### 本版本未包含

- 未改变 SQLite schema、数据契约、地图、页面功能或网络协议。
- 本次完成代码级资源审计和退出回归验证；长时间运行的托管堆快照与趋势基准仍应在后续真实 Release 性能验证中记录。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 75、Infrastructure 40、App 83，共 198 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：固定测试数据校验通过。
- WPF 生命周期测试验证立即关闭窗口能够完成 `Closed`，且应用保持 `ShutdownMode=OnMainWindowClose`；WebSocket 测试验证异步释放返回时流已经停止。
- 真实 Release 进程收到标准窗口关闭消息后在 15 秒限制内退出，退出码为 `0`，未残留进程锁定构建输出 DLL。

提交日志建议：`0.35.1: 修复窗口退出时后台任务清理`

## 0.35.0 - 2026-08-21

### 已实现

- 确认本机 SQLite 原先没有截图对应的事件 `20260728162718`；已有的 `20260819071048` 是另一条 2026-08-19 熊本样例，不能作为令和八年熊本地震。
- 从 JMA 当前公开数据目录获取并保存 7 份官方详细 JSON：4 份 `VXSE51`、2 份 `VXSE53` 和 1 份 `VXSE61`，包含 2026-07-28 16:27 主震、M7.1、最大震度 7 和最终 16 km 震源要素更新。
- 新增 `JmaQuakeDetailJsonParser`，映射官方区域、市町村和观测点层级；第2报包含 72 个区域、555 个市町村和 1,248 个观测点。
- 后续仅含震源字段的 `VXSE61` 不会覆盖已收到的最大震度、区域、市町村和观测点；摘要、地图和观测树保持累计数据，震源、震级和深度使用最新值。
- 已有 SQLite 缓存启动时会按报文身份幂等补入这 7 份固定报文，事件列表可显示截图对应地震。

### 本版本未包含

- JMA XML 数据目录中的对应历史 XML 已返回 404，未把 JSON 改名或伪造为 XML；项目保留官方 JSON 原文和来源 URI。
- 未改变 SQLite schema、实时来源协议或地图资源；没有新增外部运行时依赖。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 75、Infrastructure 40、App 83，共 198 项通过。
- `python -B -X utf8 tools\validate_test_data.py`：5 份 XML、7 份 JMA JSON、站点目录、地图包络和震度定义校验通过。
- JMA 详细 JSON 文件已记录 SHA-256；原始 XML 地址验证为 HTTP 404。
- 已通过正式 SQLite 仓储初始化将目标事件写入本机数据库：`20260728162718` 共 7 条报文；数据库当前共 18 个事件、31 条报文。

提交日志建议：`0.35.0: 接入令和八年熊本地震官方详细数据`

## 0.34.3 - 2026-08-21

### 已实现

- 修复 SQLite 只读或无法写回时仓储错误回退到固定 seed、导致事件列表只剩一个样例事件的问题。
- 初始化现在优先保留已经成功读取的缓存事件；无法执行 schema/状态写回时尝试只读连接，并在内存中合并缺失的熊本固定样例。
- 原生 UI 自动化确认当前本机缓存启动后事件列表显示 `17 条`，其中包含“熊本県天草・芦北地方”。

### 本版本未包含

- 未改变 SQLite schema，也未要求用户修改或删除现有数据库。
- 当前环境的缓存目录仍可能没有写权限；应用会显示只读警告，但不再丢弃已读取事件。正式发布时仍建议将缓存目录放在用户可写目录。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 72、Infrastructure 40、App 82，共 194 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过。
- Release 构建 0 警告、0 错误；原生 UI 自动化确认列表数量和熊本事件可见。

提交日志建议：`0.34.3: 修复只读缓存导致事件列表丢失`

## 0.34.2 - 2026-08-21

### 已实现

- 修复已有 SQLite 缓存启动时看不到 2026-08-19 熊本固定样例的问题。应用初始化会将随应用发布的官方 JMA `VXSE51`、`VXSE52`、`VXSE53` 和订正报文补入缺失记录，再由现有事件归并和列表投影显示；截图对应的令和八年熊本地震在 `0.35.0` 接入。
- 固定样例补入按 `(EventId, SourceId, SourceMessageId)` 幂等去重，不会在重复启动时产生重复报文，也不会覆盖缓存中的实时来源报文。
- 增加 Infrastructure 仓储回归测试和 App 页面回归测试，分别验证已有缓存补入、重复启动去重和事件列表可见性。

### 本版本未包含

- 未新增或重新下载截图对应的真实数据；使用仓库已有的 2026-08-19 JMA 官方熊本 XML 样例。
- 未改变 SQLite schema、事件身份规则、实时数据源协议或地图绘制逻辑。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 72、Infrastructure 39、App 82，共 193 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过。
- `git diff --check`：通过。

提交日志建议：`0.34.2: 修复熊本地震样例未显示`

## 0.34.1 - 2026-08-21

### 已实现

- 修复正式区域边界与区域 Polygon 轮廓同时描边造成的局部双线、错位和遮盖：存在拓扑边界资源时，区域面只负责底图填充，区域边界统一由去重后的拓扑 LineString 绘制。
- 将区域边界线宽从 `1.4` 调整为 `1.8`，保留低震度到高震度的图层顺序，使高震度边界在残留重合处覆盖低震度边界。
- 加深震度 `1`（`#78C7D8`）和震度 `3`（`#F0C943`）颜色，并同步地图图例、摘要、时间线、观测树和事件列表徽标。

### 本版本未包含

- 未加入令和八年熊本地震的新数据；现有 SQLite 和固定 JMA XML 已包含该事件，后续只需增加页面级回归验证。
- 未实现更大缩放、鼠标拖动/滚轮缩放、摘要底部报文导航和时间线宽度统一；这些功能按 UI 推进文档拆分到后续步骤。

### 验证结果

- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 72、Infrastructure 38、App 81，共 191 项通过。
- `python -B -X utf8 tools\\validate_test_data.py`：固定测试数据校验通过。
- `git diff --check`：通过；本轮没有新增真实数据依赖。

提交日志建议：`0.34.1: 修复区域边界错位并优化低震度配色`

## 0.34.0 - 2026-08-21

### 已实现

- 移除区域最大震度面的单独填充，避免区域颜色覆盖市町村颜色；区域数据仍保留用于定位和边界震度计算。
- 市町村只有 `1` 到 `7` 的有效震度时才绘制震度填充，并使用深色边界；`Unknown` 或未收到震度时不填充，仅保留底图中性的市町村边界。
- `HasDrawableLayers` 改为依据市町村、区域边界和震源/观测点判断，只有区域边界时也能正确显示事件图层而不弹出“没有可绘制坐标”提示。
- 保持绘制顺序为“底图 → 市町村填充/边界 → 区域边界 → 震源/观测点”，区域边界继续按低震度到高震度显示。

### 本版本未包含

- 尚未完成真实 Release 启动下的工作集、首次绘制耗时和不同窗口尺寸截图基准；当前只完成代码和自动化回归验证。
- 尚未修改高精度地图按需加载、海啸图层或其他一级页面。

### 验证结果

- 新增“只有区域边界时仍属于可绘制事件图层”的 App 回归测试；App 测试 81 项通过。
- Release 构建 0 警告、0 错误；完整测试 Core 72、Infrastructure 38、App 81，共 191 项通过。
- 固定测试数据校验通过；本版本不需要新的真实数据。

提交日志建议：`0.34.0: 完成区域边界与市町村填色重构`

## 0.33.5 - 2026-08-21

### 已实现

- 将 `EarthquakeMapViewModel.BoundaryLayers` 接入 WPF 地图绘制；每个震度组使用一个冻结的 `StreamGeometry`，避免为 1,069 条边界分别创建控件。
- 区域边界绘制顺序位于市町村图层之后、震源和观测点之前，组内按 `Unknown` 到高震度、组间按已计算的震度升序添加，保证高震度边界覆盖低震度边界。
- 区域边界线复用现有十档震度配色，未知震度使用中性灰，并在报文切换时通过 `BoundaryLayers` 属性通知合并重绘。
- 地图视图同时补充监听市町村图层变化，避免市町村或边界报文更新后仍显示旧图形。

### 本版本未包含

- 区域面仍保留现有最大震度填充，市町村透明度和无震度边界样式尚未按最终方案重构；这些变化仍属于 `0.34.0`。
- 本版本只验证离线真实边界资源的绘制数据链路，尚未完成启动内存和人工截图基准。

### 验证结果

- App 测试 80 项通过，确认边界分组数据仍保持相邻区域最大震度规则。
- Release 构建 0 警告、0 错误；完整测试 Core 72、Infrastructure 38、App 80，共 190 项通过。
- 固定测试数据校验通过；未新增真实外部数据依赖。

提交日志建议：`0.33.5: 接入区域边界分组绘制`

## 0.33.4 - 2026-08-21

### 已实现

- 在 `EarthquakeMapViewModel` 中根据报文 `IntensityAreas` 建立区域代码到最大有效震度的索引；同一区域重复记录取较大震度，`Unknown` 不覆盖已收到的有效震度。
- 为每条 `EarthquakeMapBoundary` 计算显示震度：共享边界取两侧最大有效震度，外边界取唯一侧，有效震度缺失时归入 `Unknown`。
- 新增 `BoundaryLayers` 分组投影，按 `Unknown → 1 → 2 → 3 → 4 → 5弱 → 5强 → 6弱 → 6强 → 7` 升序输出，为后续高震度边界覆盖低震度边界提供稳定绘制顺序。

### 本版本未包含

- 尚未修改 `EarthquakeMapView` 的 WPF 绘制；区域面填充、市町村填充和边界实际颜色/层级仍属于后续 `0.34.0` 视觉实现。
- 边界显示震度仍是地图 ViewModel 投影，不写入 `EarthquakeReport` 或 SQLite。

### 验证结果

- 新增共享边界、外边界、未知震度、重复区域和升序分组测试；App 测试 80 项通过。
- Release 构建 0 警告、0 错误；Core 72、Infrastructure 38、App 80，共 190 项测试通过。

提交日志建议：`0.33.4: 增加区域边界震度分组投影`

## 0.33.3 - 2026-08-21

### 已实现

- 将审计通过的 `jma-earthquake-area-boundaries-overview.geojson` 加入 `Assets/Data/Map`，随应用输出和发布目录复制。
- 新增 `OfflineMapBoundaryGeometry` 和 `EarthquakeMapBoundary`，独立解析 `LineString`/`MultiLineString`、来源元数据和相邻区域代码。
- 按 `areaCode1/areaCode2` 建立区域索引，外边界只挂接 `areaCode1`；`EarthquakeMapViewModel` 启动时加载并暴露 `BoundaryGeometry`，供下一步震度映射使用。
- 保持当前地图绘制逻辑不变，边界资源本版本只完成加载和索引，不参与界面渲染。

### 本版本未包含

- 尚未按相邻区域最大震度计算边界颜色，也未调整区域、市町村、震源和观测点的绘制顺序；完整视觉变化仍属于 `0.34.0`。
- 尚未把边界索引写入 `EarthquakeReport` 或 SQLite；它属于地图资源投影层。

### 验证结果

- 新增边界解析、元数据、MultiLineString、区域索引和无效要素测试；App 测试由 75 项增至 78 项。
- Release 构建 0 警告、0 错误；Core 72、Infrastructure 38、App 78，共 188 项测试通过。
- 启动回归确认正式资源加载为 1,069 条边界，区域 `100` 索引包含 8 条边界。

提交日志建议：`0.33.3: 接入区域边界资源解析与区域索引`

## 0.33.2 - 2026-08-21

### 已实现

- 新增 [audit_boundary_open_endpoints.py](../tools/audit_boundary_open_endpoints.py)，按相邻区域组合构建候选边界图，统计开放端点、跨相邻关系交汇点和疑似非端点交点。
- 对 `0.33.1` 生成的真实 JMA 候选资源完成审计：1,128 个开放端点全部归属于 376 个三叉交汇节点，每个节点对应 3 组相邻区域。
- 确认孤立开放端点为 `0`，端点近邻匹配为 `0`，端点落在其他边界线段内部为 `0`，当前资源不需要额外的非端点交点拆分。

### 本版本未包含

- 未将候选边界 GeoJSON 复制到 App，也未修改区域面填充、颜色映射或 WPF 图层顺序；用户可见的区域边界渲染仍属于计划 `0.34.0`。
- 审计结论只适用于当前 JMA `20240520` 资源和本版本生成参数；更换源数据、精度或过滤阈值后必须重新审计。

### 验证结果

- `python -B -X utf8 tools\audit_boundary_open_endpoints.py --self-test`：2 个测试通过。
- 真实候选资源：1,069 个 Feature、128,970 条线段、499 组相邻关系，几何无效数 `0`。
- `python -B -X utf8 tools\generate_jma_boundary_topology.py --self-test`：5 个测试通过；完整 .NET 测试 185 项继续通过。

提交日志建议：`0.33.2: 完成区域边界开放端点拓扑审计`

## 0.33.1 - 2026-08-21

### 已实现

- 新增 JMA Polygon Shapefile ZIP 区域边界拓扑生成器，使用 SQLite 临时索引在磁盘上完成千万级线段去重，避免把全部原始边一次性保存在内存中。
- 边界资源以 `LineString` 输出无方向的 `areaCode1/areaCode2` 相邻区域代码；外边界的 `areaCode2` 为空，同一相邻关系的连续线段会合并。
- 增加端点精度、线简化和最小环面积参数；缺少区域代码的要素只写入审计报告，不生成无法取色的边界。
- 使用 `20240520_AreaForecastLocalE_GIS.zip` 完成真实数据验证：约 1,032 万条原始线段经面积过滤、去重、合并和简化后生成 1,069 条连续边、130,039 个坐标点，临时结果约 3.35 MB。

### 本版本未包含

- 拓扑边界尚未复制到 App 资源，也未修改地图填充、图层顺序或用户可见渲染；完整视觉功能仍计划在 `0.34.0` 实现。
- 当前生成器按规范化端点识别共享边；真实报告中的 1,128 个开放链端点仍需下一步定位，非端点交点拆分尚未实现。

### 验证结果

- `python -B -X utf8 tools\generate_jma_boundary_topology.py --self-test`：5 个测试通过。
- 真实区域 ZIP 生成结果可被标准 JSON 解析，1,069 个几何均为 `LineString`，相邻代码字段完整，未输出双空代码边界。
- `python -B -X utf8 tools\audit_area_boundary_topology.py --self-test`：2 个测试通过。

提交日志建议：`0.33.1: 增加区域边界拓扑生成与审计工具`

## 0.33.0 - 2026-08-21

### 已实现

- 新增 1,910 个 JMA 地震/海啸市町村边界的低内存概览资源，并在地图上按当前报文市町村最大震度着色。
- 市町村边界按 `IntensityMunicipality.Code` 与 GeoJSON `municipalityCode` 精确匹配，未匹配数量单独保留为诊断。
- 详情树中的市町村节点可以使用匹配边界包络中心定位地图；没有匹配几何时继续显示“位置未知”。
- 当前事件跟随焦点会同时考虑区域和市町村几何，保留震度速報没有震源或观测点时的区域跟随行为。

### 本版本未包含

- 高精度市町村 GeoJSON 仍不在启动阶段解析，未新增在线地图或空间命中测试。
- 未修改 `EarthquakeReport`、SQLite schema、独立站点目录或 JMAXML 字段；市町村父级关系仍只使用报文本身的代码。

### 验证结果

- 新增市町村 GeoJSON 代码读取、地图图层、定位中心和未映射计数回归测试。
- Release 构建 0 警告、0 错误；Core 72、Infrastructure 38、App 75，共 185 项测试通过；固定测试数据校验通过。

提交日志建议：`0.33.0: 接入市町村震度地图图层与树节点定位`

## 0.32.0 - 2026-08-21

### 已实现

- 将“观测点”标签从平面列表升级为三层 `区域 → 市町村 → 观测点` TreeView。
- 通过 `IntensityArea.Code`、`IntensityMunicipality.AreaCode/Code` 和 `IntensityStation.MunicipalityCode` 建立父子关系，不按名称、数组顺序或坐标猜测归属。
- 缺少区域或市町村父级的记录保留在“未映射”节点；无坐标观测点继续显示并保持“位置未知”。
- 搜索支持递归保留父级上下文，“仅看最高震度”只保留最高震度后代并保留必要父节点；新增展开全部/折叠全部按钮。
- 选择区域、观测点节点继续联动地图定位；市町村和未映射节点没有可用坐标时不强行定位。

### 本版本未包含

- 未修改 `EarthquakeReport`、SQLite schema 或 JMA XML 字段；树是详情页的 App 投影。
- 独立 4,368 条站点目录仍缺少覆盖全部站点的正式 `City.Code`/`IntensityStation.Code` 父级信息；JMAXML 报文自身提供的代码关系可正常构建，目录审计仍待真实数据。

### 验证结果

- 新增树构建、正常父子关系、未映射保留、递归搜索和最高震度筛选测试。
- Release 构建 0 警告、0 错误；Core 72、Infrastructure 38、App 73，共 183 项测试通过；固定测试数据校验通过。

提交日志建议：`0.32.0: 将观测点列表升级为区域市町村观测点树`

## 0.31.5 - 2026-08-21

### 已实现

- 修复震度速報开启“跟随”时没有震源和观测点坐标就退回日本全图的问题；地图现在使用当前事件区域与点位的整体包络中心作为跟随中心，尽量取事件范围中间位置。
- 令和八年熊本地震的三份 JMA 官方 XML 已重新通过官方历史地址核验，远端响应均为 200 且 SHA-256 与仓库样例一致；本机 SQLite 已包含同一事件的 `VXSE51`、`VXSE52`、`VXSE53` 和订正报文共 4 条记录。

### 本版本未包含

- 未重复写入或改写已有 SQLite 缓存；已有完整样例时保持报文主键、原始 XML 和接收时间不变。
- 未把区域包络中心写入 `EarthquakeReport` 或 SQLite，跟随中心仍属于地图显示层计算结果。

### 验证结果

- 新增震度速報无震源/无观测点但有区域时的地图跟随回归测试。
- JMA 三份官方历史 XML 地址返回 200，远端与本地文件哈希完全一致。
- Release 构建和 App 测试通过；固定数据校验通过。

提交日志建议：`0.31.5: 修复震度速報自动跟随并核验熊本样例`

## 0.31.4 - 2026-08-21

### 已实现

- 地图观测点按震度从低到高加入绘制层，高震度观测点在重叠位置显示于低震度点上方；未知震度点排在已知震度点之后。
- 详情观测列表中的区域项使用当前已匹配 JMA 区域几何的包络中心作为定位点，选择区域可以定位地图；没有匹配几何的区域仍显示“位置未知”。
- 保持领域报文和 SQLite 数据不变，不为市町村或观测点目录缺失的父级关系猜测位置。

### 本版本未包含

- 未接入市町村边界或完整 `City.Code` 父级目录，市町村项仍可能显示“位置未知”。
- 未改变无坐标观测点的保留和地图过滤规则。

### 验证结果

- 新增 App 回归测试，验证高震度点位于低震度点之后，以及已匹配区域可以生成定位中心。
- Release 构建 0 警告、0 错误；Core 72、Infrastructure 38、App 71，共 181 项测试通过；固定测试数据校验通过。

提交日志建议：`0.31.4: 优化观测点图层顺序并修复区域定位`

## 0.31.3 - 2026-08-21

### 已实现

- 统一地图区域、观测点、事件列表徽标、摘要和时间线的十档震度配色，改为更高饱和度、层级更清晰的颜色过渡。
- 观测点边框根据震度颜色亮度自动选择深灰或白色；深色震度同时使用白色文字，浅色震度使用深色文字，提升地图上的点位辨识度。
- 地图区域边界和震度图例同步使用新色板，避免同一震度在地图和详情中出现不同颜色。

### 本版本未包含

- 不修改 JMA 震度枚举、数据契约、报文解析或 SQLite 数据；颜色仍属于 App 显示层。
- 不改变海啸状态的既有颜色语义和观测点显示范围。

### 验证结果

- WPF STA 回归测试确认 `6强` 徽标使用新紫色背景、白色边框和白色文字。
- Release 构建、全套单元测试、固定数据校验和 JMA 资源转换器自测通过。

提交日志建议：`0.31.3: 优化震度配色与观测点边框`

## 0.31.2 - 2026-08-21

### 已实现

- 撤回 `0.31.1` 误加入的全国目录点图层；地图现在只绘制当前报文实际观测到震度且坐标可解析的观测点，以及当前报文震源。
- 没有选择事件或当前报文没有可用坐标时，不再显示全国灰色观测点，恢复事件图层为空的行为。
- SQLite 读取旧缓存时，对 `source_id=jma-xml` 且 `Coordinate=null` 的观测点使用当前 JMA 目录按代码优先、唯一规范化名称兜底补坐标；已有坐标保持不变。
- 补坐标只发生在读取投影中，不改 SQLite schema、不删除或重写用户缓存；P2PQuake 等非 JMAXML 数据不使用 JMA 目录补全。

### 本版本未包含

- 不把完整目录点显示在地图或详情列表中；区域 → 市町村 → 观测点树形详情仍等待带正式代码和父级关系的真实目录。
- 不对名称重复、名称未命中或目录缺坐标的记录猜测坐标；这类站点继续保持“位置未知”。

### 验证结果

- 新增旧缓存坐标补全回归测试，覆盖 JMAXML 缺失坐标、已有坐标和 P2PQuake 隔离。
- 固定样本地图标记数量恢复为 76 个（75 个观测点和 1 个震源）；目录中的茨城、和歌山站点仍可为 JMAXML 报文补齐坐标。
- 本机缓存只读核验：225 条 JMAXML 空坐标记录全部能被正式目录匹配；未修改用户缓存文件。
- Release 构建 0 警告、0 错误；Core 72、Infrastructure 38、App 69，共 179 项测试通过；固定数据和两个转换器自测通过。

提交日志建议：`0.31.2: 修复观测点坐标缓存补全`

## 0.31.1 - 2026-08-21

### 已实现

- 修复全量观测点目录只参与坐标解析、没有进入地图图层的问题。正式应用现在将 4,368 个有坐标的 JMA 观测点作为中性底图点绘制。
- 当前报文中的观测点继续使用震度颜色和较大标记，并按规范化名称覆盖目录点，避免同一站点重复绘制。
- 修复地图定位时把全量目录点参与事件中心计算的问题；“定位所选事件”只使用震源和当前报文观测点计算中心。
- 全量目录点合并为一个 `StreamGeometry/Path` 批量绘制，不创建 4,368 个独立 WPF 控件，控制启动时间和内存占用。
- 增加和歌山、茨城站点的地图回归断言，启动固定样本的地图标记数量从 76 个修正为 4,369 个。

### 本版本未包含

- 不新增站点代码或市町村父级关系；目录中仍缺少覆盖全部站点的 JMAXML `IntensityStation.Code` 和 `City.Code`。
- 不改变观测点详情列表的报文范围；详情列表继续展示当前报文中的观测点，全量目录点用于地图底图和后续树形详情的数据准备。

### 验证结果

- Release 构建 0 警告、0 错误；Core 72、Infrastructure 37、App 70，共 179 项测试通过。
- 地图回归测试确认和歌山、茨城目录点进入 `Markers`，事件站点只出现一次。
- 固定数据校验、站点转换器自测和地图转换器自测通过。

提交日志建议：`0.31.1: 修复全量观测点地图接入`

## 0.31.0 - 2026-08-21

### 已实现

- 新增随应用发布的 JMA 震度观测点目录，包含 4,368 条真实名称和坐标，覆盖 47 个都道府县；资源保存来源 URL、获取日期、数据版本和坐标系说明。
- 新增 `JmaStationCoordinateCatalog`，一次性建立站点代码和规范化名称索引，并统计总数、带坐标数、缺代码、缺坐标、重复代码、重复名称和补充代码数。
- 固定样本和实时 JMA XML 共用同一份站点目录。坐标解析先按站点代码精确匹配，代码未命中时才按去除末尾 `＊/*` 的唯一名称补全；重复名称不自动匹配。
- 应用启动优先加载正式目录，并叠加现有 75 条固定报文代码坐标；正式目录缺失、损坏或 schema 不支持时回退固定 CSV，不阻止 SQLite 缓存和页面启动。
- 新增观测点资源转换器和正式资源校验，锁定 4,368 条记录、47 个都道府县、坐标范围和名称唯一性。

### 本版本未包含

- 上游正式站点资源不含 JMAXML `IntensityStation.Code` 和市町村代码；本版本没有伪造代码或父级关系，完整代码目录和区域层级校验仍需真实数据。
- 本版本不实现区域 → 市町村 → 观测点 TreeView，也不在无地震报文时把全部 4,368 个点同时绘制到地图；地图继续只显示当前报文中的有坐标观测点。
- SQLite schema、报文模型、地图几何和网络协议均未改变。

### 验证结果

- 4,368 条正式目录记录全部有合法坐标，规范化名称无重复，覆盖 47 个都道府县；固定 `VXSE53` 的 75 个观测点仍全部有坐标。
- 新增目录加载、名称规范化、代码优先、重复名称拒绝、缺坐标保留、JMAXML 名称补全和资源损坏回退测试。
- Release 构建 0 警告、0 错误；Core 72、Infrastructure 37、App 69，共 178 项测试通过。
- 固定数据校验、JMA 地图转换器自测和 JMA 观测点转换器自测通过。

提交日志建议：`0.31.0: 接入完整观测点目录`

## 0.30.0 - 2026-08-21

### 已实现

- 地图入口切换为 JMA 地震信息细分区域 GeoJSON，不再使用手工日本示意轮廓。
- `OfflineMapGeometry` 完整读取 Polygon/MultiPolygon 的全部外环和内环；同一 Feature 的多岛屿合并为一个可绘制几何，并提供资源范围和无效几何诊断。
- WPF 地图改用 `Path`/`StreamGeometry` 的 EvenOdd 填充规则，保留岛屿和洞；投影范围从真实资源计算并加入纬度纵横比修正，事件点也会纳入范围。
- 地震区域按 `IntensityArea.Code` 聚合匹配同码几何，未匹配区域通过 `UnmappedAreaCount` 保留诊断，不静默丢弃。
- 优化 JMA 地震区域概览资源：调整为 `0.015` 度简化容差和 `0.0002` 平方度最小多边形面积过滤，在控制内存增长的同时减少海岸线缝隙；资源约 3.2 MB，高精度资源仍保留为后续按需加载输入。
- 调整地图配色：海洋使用蓝色背景，陆地使用浅暖色填充，边界使用较深蓝灰线条，震度区域颜色保持原定义。

### 本版本未包含

- 不修改观测点目录；当前仍使用随应用复制的 75 个固定测试站点坐标，完整站点代码目录和三层树 UI 留在后续步骤。
- 不接入 EEW、海啸或历史回放地图图层，不引入 Mapsui/SkiaSharp 运行时依赖；概览地图允许忽略极小岛屿和碎片，不能用于精确空间分析。

### 验证结果

- 新增多环、多岛屿和无效几何诊断测试；App 67 项测试通过。
- Core 68、Infrastructure 37、App 67，共 172 项测试通过；Release 构建 0 警告、0 错误。
- 真实地震区域资源校验：194 个 Feature，几何类型为 Polygon/MultiPolygon，范围约为经度 122.93–153.99、纬度 20.42–45.56。
- 概览资源校验：194 个 Feature、435 个多边形部件、约 3.2 MB；启动入口已切换到该资源。
- 固定测试数据校验和 GIS 转换器自测通过。

提交日志建议：`0.30.0: 接入JMA真实地震地图`

## 0.29.7 - 2026-08-20

### 已实现

- 修复摘要和时间线最大震度徽标的数据上下文，使十档震度背景色和深色白字样式实际生效；WPF STA 测试直接验证 6强为 `#963F68` 背景和白字。
- 只读审计 `resources/map/` 下 10 个 JMA GIS ZIP，记录图层名称、几何类型、记录数、代码字段、功能用途和接入优先级。
- 确认地震细分区域、市町村和海啸预报区三个关键包结构完整并通过现有严格资源审计；新增长期维护的 JMA GIS 地图资源清单。

### 本版本未包含

- 不转换 Shapefile、不修改当前地图渲染器，也不把约 1.366 GiB 原始 ZIP 复制进发布包。
- 不修改 `EarthquakeReport`、SQLite schema、来源解析、区域代码关系或观测点目录。

### 验证结果

- Core 68、Infrastructure 37、App 65，共 170 项测试通过。
- Release 构建通过，0 个警告、0 个错误；固定测试数据和三个关键 GIS 包审计通过。

提交日志建议：`0.29.7: 修复震度配色并审计JMA GIS资源`

## 0.29.6 - 2026-08-20

### 已实现

- 摘要中的最大震度使用与地图图例一致的十档震度背景色；深色等级使用白色文字。
- 摘要关键字段改为独立行并按“最大震度、震源地、震级、深度、海啸”排序；震度速报在最大震度后显示“震源・规模 调查中”和“津波 调查中”。
- 时间线关键字段改为最大震度一行、震级/震源地/深度一行、海啸一行，并保留累计字段与海啸调查中显示规则。

### 本版本未包含

- 不修改 `EarthquakeReport` 字段、来源解析、SQLite schema 或网络协议。
- 不新增地图、通知或其他一级页面功能。

### 验证结果

- Core 68、Infrastructure 37、App 65，共 170 项测试通过。
- Release 构建通过，0 个警告、0 个错误；固定测试数据校验通过。

提交日志建议：`0.29.6: 调整详情摘要和时间线字段布局`

## 0.29.5 - 2026-08-20

### 已实现

- 修正详情时间线的累计字段显示：无变化或当前报文缺失时继续显示已收到的原值，从未收到的字段不显示；摘要仍在未收到海啸说明时显示“津波 调查中”。
- 补充海啸首次收到前不进入时间线变化摘要的回归测试，并同步维护数据契约、UI 设计和工程实现文档。

### 本版本未包含

- 不修改 `EarthquakeReport` 字段、来源解析、SQLite schema 或网络协议。
- 不新增地图、通知或其他一级页面功能。

### 验证结果

- Core 68、Infrastructure 37、App 65，共 170 项测试通过。
- Release 构建通过，0 个警告、0 个错误；固定测试数据校验通过。

提交日志建议：`0.29.5: 修正时间线累计字段显示`

## 0.29.4 - 2026-08-20

### 已实现

- 详情摘要按时间线累计已收到的震度、震源、震级、深度、发生时间和海啸信息；后续报文缺少字段时不清空已知值，海外报文只显示实际存在的要素。
- 时间线变化摘要只比较当前报文实际提供的字段，避免显示 `3 → 不明` 和相同值的 `x → x`；震度更新在已有震源情报后继续显示已收到的震级、深度、震源地和海啸状态。
- 海啸状态单独显示为文字底色：未收到时为“津波 调查中”，收到后归一化为“津波の心配なし”“若干の海面変動”“津波注意報”“津波警報”或“大津波警報”，分别使用灰、蓝、黄、红、紫色背景，红色和紫色使用白字。

### 本版本未包含

- 不修改 `EarthquakeReport` 字段、来源解析、SQLite schema 或数据契约；累计状态只属于 App 详情显示投影。
- 不新增海外地震字段推断、地图图层或通知行为。

### 验证结果

- 新增缺失字段与累计显示回归测试；Core 68、Infrastructure 37、App 65，共 170 项测试通过。
- Release 构建通过，0 个警告、0 个错误；固定测试数据校验通过。

提交日志建议：`0.29.4: 完善报文累计显示和海啸状态分级`

## 0.29.3 - 2026-08-20

### 已实现

- 地震情报页地图图例改为紧凑竖向布局，显示“不明”、1、2、3、4、5-、5+、6-、6+、7 十档，并沿用现有震度颜色定义。
- 顶部数据状态改为带状态点和小标题的状态卡片，最后接收时间与 JST 当前时间使用独立、稳定高度的布局。
- 底部状态栏为自动刷新和版本信息预留固定宽度，错误详情使用弹性列并省略显示，避免文字重叠。

### 本版本未包含

- 不修改地图资源、区域映射、观测点数据、数据契约、网络请求或 SQLite 缓存。
- 不新增设置项、图层开关或其他业务功能。

### 验证结果

- 通过 App Release 构建和现有 Core、Infrastructure、App 单元测试。
- 通过固定测试数据完整性校验。

提交日志建议：`0.29.3: 优化地震情报页状态栏和震度图例`

## 0.29.0 - 2026-08-20

### 已实现

- 完成一次带原始消息捕获的 P2PQuake 真实 10 分钟验证，确认此前缺少 `id` 的对象是 `code=555` 网络节点统计，而不是地震报文或协议 heartbeat。
- `P2pQuakeWebSocketSource` 明确识别 `code=555` 且 `areas` 为数组的消息，返回 `Online` 空报文和“网络节点统计”详情，只更新 `LastMessageAt`。
- 未知非事件对象继续使用通用忽略规则；带事件结构但缺少 `id` 的对象仍为 `ParseFailed`。
- 增加 `code=555` 不生成报文、不增加连接异常和记录消息活性的回归测试。

### 本版本未包含

- 不把 `code=555` 解释为 heartbeat，不新增 heartbeat 时间、成功次数或静默 watchdog；三条真实消息的间隔不固定，不能形成可靠超时阈值。
- 不保存或展示 `areas[].peer` 的节点统计；该数据与地震情报页面当前业务无关，只用于连接协议分类。
- 本次真实验证没有收到地震事件报文，不能据此证明实时事件吞吐。

### 验证结果

- 真实 10 分钟验证：握手成功；两次升级 HTTP `429` 正确映射为 `RateLimited` 且异常数为 0；随后收到 3 条 `code=555`；9 分钟时触发 1 次预期轮换；汇总 `statuses=9`、`online=3`、`expectedDisconnects=1`、`connectionExceptions=0`。
- 三条 `code=555` 的字段集合一致，`areas` 分别为 114、114、112 项，消息间隔为 28.888 秒和 241.817 秒。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：通过，Core 68 个、Infrastructure 37 个、App 64 个，共 169 个测试全部通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，0 个警告、0 个错误。
- `dotnet build tools\\P2pQuakeNetworkProbe\\P2pQuakeNetworkProbe.csproj --configuration Release --no-restore`：通过，0 个警告、0 个错误。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.29.0: 识别 P2PQuake 网络节点统计消息`

## 0.29.1 - 2026-08-20

### 已实现

- 完成 nTool 公开文档、使用条款、事件 API 和实时逐站数据链路的只读评估。
- 确认 nTool 事件 API 与 JMA/P2PQuake 已有事件信息重叠，不能为当前正式版本提供足够的独有价值。
- 确认 nTool 页面使用的 Yahoo/NIED 逐秒 JSON 在技术上可补充连续逐站显示，但端点未文档化，且没有发现明确授予第三方复制、再发布或商业分发的授权。
- 将 nTool 正式决策固定为“仅隔离研究和合规调查，不接入生产数据源”；同步更新需求、数据契约、工程实现和 UI 推进文档。

### 本版本未包含

- 不新增 nTool HTTP 适配器、轮询任务、设置开关、SQLite 表或 UI 控件。
- 不把 nTool 转存值标记为 JMA 官方观测震度，不把 nTool 上游响应转发为公共 API。
- 不把公开可访问、同类软件使用或网页脚本可见性解释为再利用许可；后续接入必须先获得明确授权并重新验证字段和稳定性。

### 验证结果

- 只读核对 nTool API 文档、条款、隐私页和实时页面；确认事件 API 字段及 `sitelist.json`/逐秒 JSON 的技术链路。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：通过，Core 68 个、Infrastructure 37 个、App 64 个，共 169 个测试全部通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，0 个警告、0 个错误。
- `python -X utf8 tools\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.29.1: 完成 nTool 数据源合规与补充价值评估`

## 0.29.2 - 2026-08-20

### 已实现

- 新增 `tools/audit_jma_resources.py`，只读审计 JMA 站点 JSON、固定站点 CSV 和 GIS ZIP，不修改输入文件。
- 核对临时真实站点目录：共 4,368 条记录、4,368 个唯一名称、覆盖 47 个都道府县；固定 `VXSE53` 的 75 个站点全部能按名称对应。
- 确认临时站点 JSON 只有 `lat/lon/name/pref/affi`，没有 JMAXML 的观测点代码，暂不能作为正式代码目录。
- 确认现有 `tmp\\20240520_AreaForecastLocalE_GIS.zip` 是截断/损坏文件，审计工具会在严格模式返回失败；没有把它复制到 App 资源。

### 本版本未包含

- 不替换当前测试站点 CSV，不把临时站点 JSON 直接打包到发布版。
- 不生成正式地图 GeoJSON，不修改地图渲染和区域匹配逻辑；正式地图和完整站点代码目录留待后续版本。
- 不把站点名称匹配升级为运行时主键；本次名称匹配只用于资源覆盖率审计。

### 验证结果

- `python -X utf8 tools\\audit_jma_resources.py --stations-json tmp\\jma-stations.json --fixed-csv tests\\TestData\\JmaStations.csv --gis-zip tmp\\20240520_AreaForecastLocalE_GIS.zip --strict`：按预期返回失败，站点审计通过，GIS ZIP 报告 `File is not a zip file`。
- `python -m py_compile tools\\audit_jma_resources.py`：通过。
- 本版本只增加开发期审计工具和文档；未改变领域、SQLite 或 WPF 运行时代码。

提交日志建议：`0.29.2: 完成 JMA 地图与观测点资源审计`

## 0.28.0 - 2026-08-20

### 已实现

- 为开发期 `P2pQuakeNetworkProbe` 增加 `--capture-messages PATH`，按完整 WebSocket 文本消息边界追加保存原始 JSONL。
- `P2pQuakeWebSocketSource` 增加可选原始文本消息观察回调；默认不启用，不改变正式应用的消息处理、SQLite 写入或 UI 状态。
- 捕获发生在分片重组和文本类型确认之后，确保每行对应一条完整文本消息，便于确认非事件消息是否为 heartbeat 或其他控制消息。
- 增加原始消息观察回调的完整消息测试，并保留现有非事件、事件缺少 `id` 和握手 `429` 回归覆盖。

### 本版本未包含

- 尚未根据捕获样本定义 heartbeat/ping/pong 的字段、方向、周期、超时和 UI 展示规则；当前不会把捕获到的非事件对象解释为 heartbeat。
- 捕获文件是开发期诊断产物，可能包含完整地震原文，不纳入正式资源和 SQLite，不建议提交到 Git。
- 未重新执行 10 分钟真实网络捕获；上游限流和连接策略仍需在用户环境中按需验证。

### 验证结果

- `dotnet test tests\\EarthquakeShow.Infrastructure.Tests\\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：通过，36 个测试全部通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：通过，Core 68 个、Infrastructure 36 个、App 64 个，共 168 个测试全部通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，0 个警告、0 个错误。
- `dotnet build tools\\P2pQuakeNetworkProbe\\P2pQuakeNetworkProbe.csproj --configuration Release --no-restore`：通过，0 个警告、0 个错误。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.28.0: 增加 P2PQuake WebSocket 原始消息捕获`

## 0.27.0 - 2026-08-20

### 已实现

- 定义 P2PQuake WebSocket 非事件 JSON 的最小判定边界：对象同时缺少 `id`、`issue` 和 `earthquake` 时视为控制/状态消息，不转换为地震报文。
- 非事件消息记录最近消息时间并返回在线状态，继续读取后续消息，不触发重连、不增加连接异常计数，也不显示为普通解析失败。
- 带有事件结构但缺少 `id` 的对象继续按报文格式错误处理，避免把损坏的地震事件静默丢弃。
- 增加非事件消息忽略、后续报文恢复解析和事件结构缺少 `id` 的回归测试。

### 本版本未包含

- 尚未根据上游未公开的具体 `code` 值赋予 heartbeat、服务状态或其他控制语义；非事件消息只作为“忽略并保持连接”的传输层结果。
- WebSocket 升级阶段明确识别 HTTP `429` 为 `RateLimited`，不计入连接异常，并继续由重连策略退避处理；本版本不绕过上游限流。
- 尚未实现协议级 ping/pong 统计或静默连接 watchdog。

### 验证结果

- `dotnet test tests\\EarthquakeShow.Infrastructure.Tests\\EarthquakeShow.Infrastructure.Tests.csproj --configuration Release --no-restore`：通过，35 个测试全部通过。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：通过，Core 68 个、Infrastructure 35 个、App 64 个，共 167 个测试全部通过。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，0 个警告、0 个错误。
- `dotnet build tools\\P2pQuakeNetworkProbe\\P2pQuakeNetworkProbe.csproj --configuration Release --no-restore`：通过，0 个警告、0 个错误。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。
- 非事件对象后紧接有效地震对象时，测试确认流继续产出 `EarthquakeReport`；握手 `429` 测试确认状态为 `RateLimited` 且不累计连接异常。

提交日志建议：`0.27.0: 处理 P2PQuake WebSocket 非事件消息`

## 0.26.1 - 2026-08-20

### 已实现

- 新增开发期 `tools\\P2pQuakeNetworkProbe`，直接复用生产 WebSocket 源和重连包装器，不加入默认 xUnit 测试套件。
- 探测工具先验证真实 TLS/WebSocket 握手，再按指定时长输出状态、报文数、预期轮换数和连接异常数；达到轮换验证时长但未发生预期轮换时返回失败。
- 完成一次真实 1 分钟握手检查和一次默认 10 分钟长时间运行检查，观察到默认 9 分钟主动轮换。

### 本版本未包含

- 本次真实运行没有收到有效地震 `Online` 报文；不能据此证明实时事件报文链路已长期稳定。
- 真实运行期间观察到 WebSocket 升级 `429` 限流，以及缺少 `id` 的非事件 JSON；尚未定义这些上游消息的独立契约和展示规则。
- 第二次使用 1 分钟轮换参数验证多次会话的命令因执行审批服务过载被拒，未实际启动。

### 验证结果

- `dotnet build tools\\P2pQuakeNetworkProbe\\P2pQuakeNetworkProbe.csproj --configuration Release --no-restore`：通过，0 个警告、0 个错误。
- 真实 1 分钟检查：TLS/WebSocket 握手成功；无有效地震报文，未产生连接异常。
- 真实 10 分钟检查：握手成功；前两次升级返回 `429`，随后收到缺少 `id` 的非事件 JSON；在约 9 分钟观察到 `IsExpectedDisconnect=true`，汇总 `expectedDisconnects=1`、`connectionExceptions=4`、`reports=0`。
- 常规解决方案测试和固定数据校验沿用 `0.26.0` 结果，未因验证工具改变生产代码逻辑。

提交日志建议：`0.26.1: 增加 P2PQuake 真实网络验证工具`

## 0.26.0 - 2026-08-20

### 已实现

- 新增原生 P-04 设置页面，当前开放数据源分类；齿轮按钮可进入，返回后保留地震情报页面现场。
- 新增独立 `%LOCALAPPDATA%\\EarthquakeShow\\settings.json` 设置文件，不与 SQLite 事件缓存混用。
- 支持调整 P2PQuake WebSocket 传输层 keep-alive（10–120 秒）和单连接最大持续时间（1–9 分钟），默认仍为 30 秒和 9 分钟。
- 设置文件缺失、损坏、版本不支持或参数越界时回退默认值并在设置页显示警告；应用设置后重建当前 WebSocket 流，不重建 HTTP 刷新和 SQLite 仓储。
- 新增设置契约、持久化、页面交互、连接重建和 keep-alive 参数测试；全套测试增至 163 个。

### 本版本未包含

- 尚未实现常规、显示与地图、通知、存储与日志、关于等其他设置分类。
- 尚未实现协议级 ping/pong 结果展示、静默 watchdog 或连接策略历史统计。
- 尚未使用真实网络报文执行长时间运行验证；自动化测试仍使用本地假流。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 68 个、App 64 个、Infrastructure 31 个，共 163 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.26.0: 增加 WebSocket 连接策略设置`

## 0.25.0 - 2026-08-20

### 已实现

- `StreamingReconnectPolicy` 增加单次连接最大持续时间，默认 9 分钟，在 P2PQuake 10 分钟强制断开前主动轮换连接。
- `ReconnectingEarthquakeSource` 使用每个会话独立的可取消令牌实现连接轮换；轮换标记为预期断开，不增加连接异常计数、不覆盖最近错误。
- 正式 `ClientWebSocket` 启用 30 秒传输层 keep-alive；不发送未经 P2PQuake 协议确认的自定义 heartbeat 消息。
- 页面和数据契约记录主动轮换状态，继续区分预期轮换、正常 Close、连接异常和解析失败。
- 新增连接最大时长、轮换取消、预期断开和 keep-alive 配置测试；全套测试增至 154 个。

### 本版本未包含

- 尚未提供用户可配置的 keep-alive 间隔或连接轮换时长设置。
- 尚未实现协议级 ping/pong 结果展示、静默 watchdog 或连接策略历史统计。
- 尚未使用真实网络报文执行长时间运行验证；自动化测试仍使用本地假流。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 68 个、App 55 个、Infrastructure 31 个，共 154 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.25.0: 增加 WebSocket keep-alive 和主动连接轮换`

## 0.24.0 - 2026-08-20

### 已实现

- `SourceStatus` 增加 `LastMessageAt`、`ConnectionExceptionCount` 和 `LastConnectionExceptionAt`，用于当前 WebSocket 消息活性与连接异常统计。
- P2PQuake WebSocket 完整消息、解析失败消息都会更新最近消息时间；连接建立/读取异常累计异常次数，普通 Close 帧和解析失败不计入连接异常。
- 地震情报页面状态栏显示最近消息年龄和累计连接异常次数；没有协议 heartbeat 帧时明确显示消息活性，而不伪造心跳结果。
- SQLite schema 仍为 v1；消息活性和连接异常统计只保存在当前进程，HTTP 刷新期间继续保留 WebSocket 瞬时诊断。
- 新增消息活性、连接异常累计、解析失败不计数、P2PQuake WebSocket 字段映射和页面文案测试；全套测试增至 153 个。

### 本版本未包含

- 尚未实现 P2PQuake 协议级主动 ping/pong 或独立 heartbeat 消息。
- 尚未实现主动心跳失败后的连接轮换、连接异常趋势图或历史统计持久化。
- 尚未使用真实网络报文执行长时间运行验证；自动化测试仍使用本地假流。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 68 个、App 55 个、Infrastructure 30 个，共 153 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.24.0: 增加 WebSocket 消息活性和连接异常统计`

## 0.23.0 - 2026-08-20

### 已实现

- `SourceStatus` 增加进程内 WebSocket 连接诊断字段：连接开始时间、连接结束时间和最近错误详情。
- `ReconnectingEarthquakeSource` 为在线、断开、解析失败和等待重连状态补齐连接生命周期诊断；最近错误在重新连接后继续保留，便于排查瞬时故障。
- 地震情报页面底部状态栏显示当前 WebSocket 连接持续时间、上一次连接持续时间和最近错误详情。
- 主窗口已有 1 秒时钟同时刷新连接持续时间，不新增后台计时线程。
- SQLite schema 仍为 v1；连接时间和最近错误不写入数据库，HTTP 刷新只保留当前进程内的 WebSocket 诊断。
- 新增连接生命周期、错误保留、动态持续时间、页面文案和仓储状态保持测试；全套测试增至 151 个。

### 本版本未包含

- 尚未实现 WebSocket 心跳状态、主动 ping/pong 统计或连接异常次数汇总。
- 尚未实现主动关闭握手、提前于 10 分钟强制断开的连接轮换。
- 尚未使用真实网络报文执行长时间运行验证；自动化测试仍使用本地假流。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 68 个、App 54 个、Infrastructure 29 个，共 151 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.23.0: 增加 WebSocket 连接持续时间和最近错误详情`

## 0.22.0 - 2026-08-20

### 已实现

- `SourceStatus` 增加进程内 WebSocket 重连诊断字段：当前连续重连次数和下次重试时间。
- `ReconnectingEarthquakeSource` 在退避等待前发布 `Delayed` 状态，记录确定性的重试次数和 UTC 时间；继续复用原有指数退避和取消生命周期。
- SQLite 仓储保留带重连诊断的 WebSocket 内存状态，同时让 HTTP 刷新状态覆盖同一 HTTP 来源的旧快照，避免状态回退。
- SQLite schema 仍为 v1；重连次数和下次重试时间不写入数据库，避免把瞬时连接状态误当成历史数据。
- 地震情报页面底部状态栏显示 WebSocket 在线、断开、解析失败、等待重连，以及 JST 格式的下次重试时间。
- 新增重连诊断时间、页面显示、HTTP 状态不回退和瞬时字段边界测试；全套测试增至 149 个。

### 本版本未包含

- 尚未显示 WebSocket 连接持续时间、最近错误详情或心跳状态。
- 尚未实现主动关闭握手、提前于 10 分钟强制断开的连接轮换。
- 尚未使用真实网络报文执行长时间运行验证；自动化测试仍使用本地假流。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 68 个、App 53 个、Infrastructure 28 个，共 149 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.22.0: 增加 WebSocket 重连诊断`

## 0.21.0 - 2026-08-20

### 已实现

- `MainWindowViewModel` 在缓存加载和首次 HTTP 补取后启动带重连的 P2PQuake WebSocket 消费任务，窗口关闭时使用统一生命周期令牌取消。
- 新增 `SqliteEarthquakeEventRepository.ApplyStreamingResultAsync()`，把每条 WebSocket 报文和连接状态原子写入 SQLite 并发布完整事件快照。
- SQLite 写入增加异步互斥，HTTP 自动刷新与 WebSocket 持续接收不会并发写入同一数据库。
- WebSocket 通道状态 ID 改为 `p2pquake-ws`，报文来源 ID 仍为 `p2pquake`；HTTP 和 WebSocket 状态分开显示，相同报文继续按原身份键去重。
- 页面收到仓储事件后同时同步事件和来源状态，WebSocket 新报文可以更新列表、详情、地图和缓存状态。
- 正式发布资源目录明确为 App `Assets/Audio`、`Assets/Images`、`Assets/Textures` 和 `Assets/Data`；根目录 `resources` 仅作为被 Git 忽略的本机素材暂存区。
- 新增流式持久化、状态保持、页面发布和窗口关闭取消测试；全套测试增至 148 个。

### 本版本未包含

- 尚未显示 WebSocket 当前重连次数、下次重试时间或连接持续时间。
- 尚未实现主动关闭握手、提前于 10 分钟强制断开的连接轮换或心跳诊断。
- 尚未使用真实网络报文执行长时间运行验证；自动化测试仍使用本地假流。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 68 个、App 52 个、Infrastructure 28 个，共 148 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.21.0: 接入 P2PQuake WebSocket 实时消息`

## 0.20.0 - 2026-08-20

### 已实现

- 新增 `StreamingReconnectPolicy`，默认按 5 秒起步指数退避，最大间隔 5 分钟。
- 新增 `ReconnectingEarthquakeSource`，在底层流断开、正常结束或抛出 WebSocket/IO 连接异常后顺序创建下一条连接。
- 收到在线报文后清零连续失败次数；同一时刻只允许一个底层流，窗口取消时同时终止读取和重连等待。
- 补充资源目录约定：原始音频、贴图和数据暂存于被 Git 忽略的 `resources` 分类目录；确认发布的文件放入 App `Assets` 分类目录，并已配置自动复制到构建和发布输出。
- 分析并登记 `resources\\Lee2025VpVs.txt` 的初步格式：规则经纬度/深度网格和 Vp/Vs 两列；尚未接入应用解析链路。
- 新增重连策略、失败计数重置、并发消费互斥和取消等待测试；全套测试增至 146 个。

### 本版本未包含

- 尚未把重连包装器接入主窗口、SQLite 仓储或自动刷新调度。
- 尚未实现心跳、连接数监控、主动关闭握手或基于服务端时间的提前换连接。
- `Lee2025VpVs.txt` 尚未用于地图、震源计算或任何用户可见显示；列单位仍需 Equake 原始元数据确认。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 68 个、App 51 个、Infrastructure 27 个，共 146 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.20.0: 增加 P2PQuake WebSocket 断线重连`

## 0.19.0 - 2026-08-20

### 已实现

- 新增 `IStreamingEarthquakeSource` 流式来源契约和可取消的 `StreamAsync()` 接口。
- 新增 `P2pQuakeWebSocketSource`，使用 `wss://api.p2pquake.net/v2/ws` 建立单条连接并读取文本消息。
- 支持 WebSocket 文本分片重组；每条完整 JSON 对象独立转换为一个 `EarthquakeReport`，JSON 数组、二进制消息或字段错误返回 `ParseFailed` 后继续读取。
- 连接异常和服务端关闭返回 `Disconnected`；取消令牌向上抛出，避免窗口退出时吞掉取消信号。
- 抽取 P2PQuake 单对象解析方法，HTTP 数组源与 WebSocket 源共用同一字段映射。
- 新增假的 WebSocket 流边界、解析失败恢复和取消行为测试；全套测试增至 141 个。

### 本版本未包含

- 尚未把 WebSocket 接入主窗口、SQLite 仓储或自动刷新循环。
- 尚未实现 P2PQuake 10 分钟断开后的自动重连、指数退避和同 IP 连接数管理。
- 尚未实现人工确认/取消候选关联、历史回放、海啸页面或 EEW 页面。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 68 个、App 51 个、Infrastructure 22 个，共 141 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.19.0: 建立 P2PQuake WebSocket 消息流契约`

## 0.18.0 - 2026-08-20

### 已实现

- 新增自动刷新循环：首次网络刷新完成后按 60 秒在线间隔自动检查 JMA JSON、JMA XML 和 P2PQuake。
- 新增刷新退避策略：普通失败从 30 秒开始指数退避，限流从 2 分钟开始指数退避，最大间隔 15 分钟；所有来源恢复在线后重置退避。
- 底部状态栏显示自动刷新等待、检查中和已停止状态；窗口关闭时取消自动刷新任务。
- `EarthquakePageViewModel.RefreshAsync` 增加异步互斥，自动刷新和手动刷新不会并行写入缓存。
- 新增退避策略单元测试；全套测试增至 137 个。

### 本版本未包含

- 尚未接入 P2PQuake WebSocket、10 分钟重连和连接数管理。
- 尚未实现人工确认/取消候选关联、关联结果持久化或跨事件折叠。
- 尚未实现历史回放、海啸页面和 EEW 页面。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 68 个、App 51 个、Infrastructure 18 个，共 137 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.18.0: 增加自动刷新和退避策略`

## 0.17.0 - 2026-08-20

### 已实现

- 新增 `EarthquakeEventAssociator`，对不同事件、不同来源的最新有效报文生成候选关联结果，不修改事件 ID或时间线。
- 关联规则要求发生时间差不超过 60 秒、震源坐标距离不超过 80 km；两侧均有震级时，震级差必须不超过 0.5。
- 结果分为高置信度和中置信度，并保留时间差、距离、震级差和双方来源消息身份。
- 详情摘要新增“可能关联来源”区域，显示关联事件、来源、消息 ID、置信度和匹配依据；候选关联不会自动合并或折叠事件。
- 新增关联算法和详情投影测试；全套测试增至 133 个。

### 本版本未包含

- 尚未提供人工确认/取消关联操作，候选关联只读展示。
- 尚未实现自动轮询、指数退避、P2PQuake WebSocket 和关联结果持久化。
- 尚未根据关联结果跨事件合并列表或共享报文时间线。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 68 个、App 47 个、Infrastructure 18 个，共 133 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.17.0: 增加跨源事件候选关联提示`

## 0.16.0 - 2026-08-20

### 已实现

- 详情摘要新增“来源差异”区域，比较同一事件内不同来源的最新报文快照。
- 差异字段覆盖状态、最大震度、震级、震源、深度和观测点数量；无差异时明确显示“无字段差异”。
- 每个差异项显示来源 ID、来源消息 ID和来源角色；JMA XML 显示为优先来源，JMA JSON 显示为摘要来源，P2PQuake 显示为第三方补充。
- 差异比较只在同一 `EarthquakeEvent` 内执行；P2PQuake 当前使用独立事件 ID，因此不会被猜测合并到 JMA 事件。
- 新增详情 ViewModel 测试，验证 JMA JSON/XML 差异投影；全套测试增至 126 个。

### 本版本未包含

- 尚未实现 P2PQuake 与 JMA 的跨源事件关联、重复事件折叠和跨事件差异比较。
- 尚未实现 P2PQuake WebSocket、自动轮询和指数退避。
- 尚未提供逐字段原始值并列编辑或导出；当前以摘要文本展示差异。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 62 个、App 46 个、Infrastructure 18 个，共 126 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。

提交日志建议：`0.16.0: 增加同事件来源差异展示`

## 0.15.0 - 2026-08-20

### 已实现

- 新增 `P2pQuakeEarthquakeSource`，读取 P2PQuake JSON API v2 的 `https://api.p2pquake.net/v2/jma/quake` 列表端点。
- 映射 P2PQuake 的报文 ID、报文类型、发布时间、震源坐标/深度/震级、最大震度、海啸状态和观测点地址/震度。
- P2PQuake 观测点使用明确的来源内合成键（`p2p:{prefecture}:{address}`），不伪装成 JMA 官方观测点编码；原始单条 JSON 和来源 URL 一并保留。
- 应用刷新链路加入 P2PQuake 来源；HTTP 429、网络错误和 JSON/字段错误分别映射来源状态，失败不会清空 JMA 或 SQLite 缓存。
- P2PQuake 报文使用独立 `p2pquake:{id}` 事件身份，暂不根据时间和坐标猜测 JMA `EventID`，避免错误跨源合并；来源优先级设为低于 JMA JSON/XML。
- 新增 P2PQuake 正常字段、限流和格式错误测试；全套测试增至 125 个。

### 本版本未包含

- 尚未接入 P2PQuake WebSocket、10 分钟重连、指数退避或自动轮询。
- 尚未实现 P2PQuake 与 JMA 事件的跨源关联、重复事件折叠和逐字段差异展示。
- 尚未接入 P2PQuake 海啸端点；nTool 仍未接入。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 62 个、App 45 个、Infrastructure 18 个，共 125 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。
- 只读核验 P2PQuake `/v2/jma/quake` 真实响应，确认 `id/issue/earthquake/points` 字段和 `maxScale` 编码规则；没有把该端点当作 JMA 官方主源。

### 已知限制

- P2PQuake 是第三方转发源，存在延迟、限流、连接中断和格式变化可能；页面必须显示其来源身份，不得作为生命安全信息唯一依据。
- 当前一次刷新只请求 HTTP 列表端点，最多返回 API 默认的最近事件集合；没有 WebSocket 持续订阅或历史分页。

提交日志建议：`0.15.0: 接入 P2PQuake 补充地震源`

## 0.14.0 - 2026-08-20

### 已实现

- 新增 `JmaXmlEarthquakeSource`，读取 JMA 公共 Atom Feed `https://www.data.jma.go.jp/developer/xml/feed/eqvol.xml`。
- 按 Feed 条目的报文链接下载 `VXSE51`、`VXSE52` 和 `VXSE53`，复用现有 `JmaXmlParser`，保留区域、市町村、观测点和 XML 原文详情。
- SQLite 仓储现在按 `jma-json`、`jma-xml` 两个实时来源依次刷新，并分别保存来源状态；任一来源失败都不会清空已有缓存。
- 事件时间线增加来源稳定排序：同一事件、发布时间、报次和接收时间相同时，JMA XML 排在 JMA JSON 之后并成为 `LatestReport`。
- XML Feed、单报文 HTTP 失败和 XML 格式错误分别映射为可展示的来源状态；单条报文失败不会阻止同一 Feed 中其他报文入库。
- 新增来源优先级、真实 XML Feed 解析、损坏 Feed 和详情限流处理测试；全套测试增至 122 个。

### 本版本未包含

- 尚未接入 P2PQuake、nTool 或其他第三方补充源。
- 尚未在详情界面逐字段展示 JSON/XML 冲突差异；当前只按优先级选择 XML 作为最新报文。
- 尚未实现自动轮询、指数退避、缓存保留上限和设置界面中的数据源开关。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore`：Core 62 个、App 45 个、Infrastructure 15 个，共 122 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。
- 只读核验 JMA XML Feed，确认 Atom `entry/id/link[type=application/xml]` 结构和 `VXSE51/52/53` 文件名规则；真实报文可由现有解析器转换。

### 已知限制

- JMA XML Feed 不是本项目控制的版本化 API；Feed 字段、链接或报文格式变化会进入 `ParseFailed` 并保留缓存，需要结合真实响应更新契约和测试。
- 当前每次刷新最多读取 20 个 Feed 条目，并逐条下载详情；尚未实现自动刷新、退避或并发下载策略。

提交日志建议：`0.14.0: 接入 JMA XML 实时 Feed 和来源优先级`

## 0.13.0 - 2026-08-20

### 已实现

- 新增 `JmaJsonEarthquakeSource`，读取 JMA 公共 `https://www.jma.go.jp/bosai/quake/data/list.json`，不需要 API Key。
- 按 2026-08-20 只读核验的真实响应映射 `eid/rdt/at/anm/acd/cod/mag/maxi/json`，生成统一 `EarthquakeReport`；无偏移时间固定按 JST 解释，显式偏移原样保留。
- 应用启动先显示 SQLite 缓存，再以 15 秒超时后台刷新 JMA JSON；列表刷新按钮复用同一仓储刷新路径。
- 刷新成功后按 JMA `json` 文件名作为来源消息身份写入 SQLite，同一消息重复刷新不会重复；缓存事件和新摘要继续由 `EarthquakeEventMerger` 合并。
- HTTP 429 映射为 `RateLimited`，其他 HTTP/网络失败映射为 `Disconnected`，JSON 或字段错误映射为 `ParseFailed`；失败只更新来源状态，不清空已有事件。
- 新增可选 `IEarthquakeSourceStatusProvider`，页面在加载和刷新后同步来源状态与离线状态；网络测试使用假的 `HttpMessageHandler`，不依赖在线服务。
- README 新增运行环境说明，明确无需单独安装 SQLite、源码运行需要 .NET 8 SDK、框架依赖版需要 .NET 8 Desktop Runtime，实时刷新只要求可访问 JMA 公共网站。
- 新增 6 个测试，覆盖真实 JMA JSON 字段、JST 时间、坐标/深度、限流、格式错误、在线持久化、失败保留缓存和页面来源状态；全套测试增至 117 个。

### 本版本未包含

- JMA JSON 当前只作为事件列表摘要源，不提供完整区域、市町村、观测点或权威 XML 报文时间线。
- 尚未接入 JMA XML 实时 Feed、P2PQuake、来源优先级和冲突差异展示。
- 尚未实现自动轮询、指数退避、缓存保留上限和清缓存设置；启动后刷新一次，之后由用户使用刷新按钮。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 61 个、App 45 个、Infrastructure 11 个，共 117 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。
- 只读访问 JMA 公共端点成功，确认当前响应包含 `ctt/eid/rdt/ttl/ift/ser/at/anm/acd/cod/mag/maxi/int/json`。

### 已知限制

- JMA 公共 JSON 并非本项目控制的版本化 API；字段变化会进入 `ParseFailed` 并保留缓存，需要结合运行日志和真实响应更新契约。
- 当前每份 JMA JSON 报文保存完整列表响应，便于排查但会增加缓存体积；接入保留上限前需要测量实际数据库增长。

提交日志建议：`0.13.0: 接入 JMA JSON 实时摘要和刷新机制`

## 0.12.0 - 2026-08-20

### 已实现

- 新增 `EarthquakeShow.Infrastructure` 工程和 `Microsoft.Data.Sqlite` 依赖，SQLite schema 版本固定为 `1`。
- 新增 `SqliteEarthquakeEventRepository`，保存报文标准字段、显式 JSON DTO、原始 XML、来源身份、接收时间和来源状态；事件仍由 `EarthquakeEventMerger` 统一派生。
- 启动时优先读取 `%LOCALAPPDATA%\\EarthquakeShow\\earthquake-cache.db`；首次运行或缓存为空时写入固定 JMAXML 样本，后续启动直接读取缓存。
- 缓存初始化失败、数据库损坏或 schema 不支持时不阻塞 WPF 启动，回退固定样本并在顶部状态显示缓存失败原因；页面同时显示离线来源状态。
- 新增 Infrastructure 测试工程，覆盖缓存写入/重启读取、重复报文去重、订正字段和原始 XML 保留、来源状态、数据库损坏/schema 回退和取消令牌；全套测试增至 111 个。

### 本版本未包含

- 尚未接入 JMA、P2PQuake 或其他实时网络数据源，`RefreshAsync` 仍不产生网络更新。
- 尚未实现设置页中的清除缓存按钮、缓存分页、缓存保留上限和数据库迁移脚本；schema 不支持时当前回退固定样本。
- 固定样本仍是开发期 seed，不代表实时数据，也不需要用户提供新的真实数据。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 61 个、App 44 个、Infrastructure 6 个，共 111 个测试全部通过。
- `python -X utf8 tools\\validate_test_data.py`：固定测试数据完整性校验通过。
- 程序集版本为 `0.12.0.0`；验证了空缓存首次写入、重启读取和损坏数据库回退。

### 已知限制

- 当前缓存是完整快照读取，未定义分页游标；大量历史数据接入前需要增加分页或增量契约。
- 来源状态固定为离线 `Disabled`，真实连接、限流和解析失败状态留给网络适配器实现。

提交日志建议：`0.12.0: 实现 SQLite 缓存和离线启动`

## 0.11.0 - 2026-08-20

### 已实现

- 新增右侧 `EarthquakeDetailsView` 和 `EarthquakeDetailsViewModel`，实现摘要、区域/市町村/观测点、报文时间线和原始数据四个标签。
- 摘要显示当前 `ViewedReport` 的震度、震源、时间、坐标、深度、震级、海啸、报文状态、上下文、来源和事件 ID；未知值统一显示“不明”。
- 观测点标签支持名称/代码搜索和“仅看最高震度”，无坐标观测点保留并显示“位置未知”；选择有坐标观测点会定位地图。
- 报文时间线显示报文类型、报次、发布时间、接收时间、状态、来源和相对上一报的关键字段变化；支持上一报、下一报、返回最新和直接选择报文。
- 地图图层改为读取 `ViewedReport`，时间线切换后区域、震源和观测点跟随当前报文快照；地图支持定位震源和观测点。
- 原始数据标签提供只读 XML、元数据、自动换行和复制按钮；不执行原始内容中的链接或脚本。
- 新增 3 个详情 ViewModel 测试和 1 个 WPF STA 启动测试，覆盖快照联动、时间线边界、订正字段变化、观测点定位、筛选、原始负载保留和详情 XAML 资源加载；全套测试增至 105 个。

### 本版本未包含

- 尚未实现独立历史回放页面；时间线只支持手动查看离散报文快照。
- 尚未实现 SQLite 缓存、实时网络数据源、来源链接打开和标准化 JSON 导出。
- 区域和市町村目前作为详情列表展示，正式 JMA GIS 边界和区域地图点选仍未实现。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：App、Core 和两个测试工程全部通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 61 个、App 44 个，共 105 个测试全部通过。
- `python -X utf8 tools\validate_test_data.py`：固定测试数据完整性校验通过。
- 详情集成测试确认：选择首报后摘要、地图和当前报文一致；固定样本订正时间线包含 4 份报文。

### 已知限制

- 详情 ViewModel 为当前固定样本规模同步重建列表；接入大量历史缓存后需要分页或增量更新。
- 观测点定位只改变地图局部焦点，不改变页面选中事件；重新选择事件或报文时焦点会重置。

## 0.10.0 - 2026-08-20

### 已实现

- 新增 `JmaXmlParser`，把固定 `VXSE51`、`VXSE52` 和 `VXSE53` JMAXML 转换为统一 `EarthquakeReport`，保留事件 ID、时间、报次、状态、震源、震级、震度和海啸说明。
- 新增 `JmaStationCatalog`，按观测点代码读取 `JmaStations.csv`，为官方详情报中的 75 个观测点补齐经纬度；无法匹配时保留观测点并使用空坐标。
- 解析器显式处理订正、取消、训练、测试、未知震度、`NaN` 震级和缺失坐标，原始 XML 保存在 `SourcePayload` 中。
- XML 读取禁止 DTD 和外部解析器，并限制单文档最多 1000 万字符；缺少必填字段或坐标格式无效时返回明确异常。
- WPF App 输出和发布时复制官方三报文、订正夹具和站点 CSV；启动时解析四份报文并注入内存仓储，同一事件合并为包含 4 个节点的时间线。
- 正常启动后事件列表显示熊本地震及订正状态，地图显示震源和有坐标的观测点；字段缺失夹具只参与测试，不进入用户界面。
- 新增 11 个 Core 解析测试和 1 个 App 启动装配测试，覆盖三种官方报文、订正、缺字段、站点坐标、事件合并、必填字段、解析选项、非法坐标、DTD 及列表/地图注入；全套测试增至 101 个。

### 本版本未包含

- 固定 XML 是开发期随程序分发的样本，不会从 JMA Feed 自动下载或更新。
- 尚未实现右侧详情内容、报文时间线交互、实时数据源、SQLite 缓存和来源优先级。
- 缺字段合成夹具不会注入应用；订正夹具用于验证并展示时间线状态，不代表另一条官方发布记录。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：App、Core 和两个测试工程全部通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 61 个、App 40 个，共 101 个测试全部通过。
- Release 输出包含三份官方 XML、订正 XML、`JmaStations.csv` 和离线地图资源。
- `python -X utf8 tools\validate_test_data.py`：固定测试数据完整性校验通过。

### 已知限制

- 当前只实现固定样本使用到的 `VXSE51/52/53` 字段子集，不执行完整 JMAXML Schema 验证。
- 固定资源在窗口 ViewModel 创建时同步解析；当前数据量很小，接入网络和历史缓存后必须移到后台适配器。
- 应用仍使用内存仓储，退出后不保存新增状态或已读标记。

## 0.9.0 - 2026-08-20

### 已实现

- 新增随应用复制的 `Assets/japan-overview.geojson` 离线日本示意轮廓，并在资源元数据中明确其不是官方 JMA 边界。
- 新增 `OfflineMapGeometry`，解析 GeoJSON `Polygon`/`MultiPolygon` 外环和 `EPSG:4326` 经纬度坐标，拒绝不支持的几何类型。
- 新增 `EarthquakeMapViewModel`，将当前事件最新有效报文投影为震度区域、震源和观测点图层；区域按 `areaCode` 匹配，坐标未知的点不绘制。
- 新增 WPF 原生 `EarthquakeMapView`，使用 `Canvas`、`Polygon` 和 `Ellipse` 绘制轮廓、震度颜色和事件点，不依赖浏览器、在线底图或地图 SDK。
- 地图工具支持放大、缩小、日本全图、定位所选事件和跟随选择；没有事件时仍显示离线轮廓。
- 地图状态继续复用 `EarthquakePageState.Map`，未创建第二份事件选择；底部状态显示“地图：离线示意”。
- 新增 5 个 App 地图测试，覆盖 GeoJSON 元数据、几何校验、区域/震源/观测点图层、无坐标过滤、地图命令和空状态；全套测试增至 89 个。

### 本版本未包含

- `japan-overview.geojson` 是 UI 示意轮廓，不是 JMA 官方行政或预报区域边界，不能用于空间查询或区域归属判断。
- 尚未接入 Mapsui、SkiaSharp、在线底图、真实 GIS Shapefile 或地图空间分析。
- 运行时内存仓储仍为空，固定 JMAXML 尚未解析，因此正常启动时只有离线轮廓，没有事件图层。
- 地图平移、全屏、重叠点选择和正式图例细分留到后续地图增强阶段。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：App、Core 和两个测试工程全部通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 50 个、App 39 个，共 89 个测试全部通过。
- `python -X utf8 tools\validate_test_data.py`：固定测试数据完整性校验继续通过。
- 原生窗口在 `1440 × 900` 和 `1180 × 720` 下验证离线轮廓、地图工具栏、状态文字和紧凑详情入口；窗口可正常关闭。
- App 和 Core Release 程序集的产品版本前缀、文件版本分别为 `0.9.0` / `0.9.0.0`。

### 已知限制

- 当前投影使用固定日本范围和等比例经纬度绘制，未进行 Web Mercator 或地理投影校正；正式 GIS 阶段需要重新评估。
- 区域图层只绘制能在本地 GeoJSON 中找到相同 `areaCode` 的区域；缺失几何不会阻塞事件详情显示。

## 0.8.0 - 2026-08-19

### 已实现

- 新增 `EarthquakeEventListView`、列表 ViewModel 和事件卡片显示投影，左栏由状态占位升级为可操作的原生 WPF 事件列表。
- 事件卡片显示最大震度、震源、震级、发生/发布时间、报文状态、海啸摘要和新事件标记；未知震级显示为“`M 不明`”。
- 列表默认按最新发布时间排序，可切换最新发生时间和最大震度；未知时间/震度排在已知值之后，并使用 `EventId` 确保稳定顺序。
- 搜索支持事件 ID、震源名称/代码、报文代码和来源 ID；搜索文字输入后即时更新结果。
- 新增最近 24 小时/7 天/30 天、最低震度、最低震级、区域和来源筛选；空仓储与筛选无结果使用不同提示。
- 单击或键盘上下键切换事件，`Enter` 聚焦详情区域；顶部搜索按钮可直接聚焦左栏搜索框。
- 仓储更新时保持当前历史事件选择，新事件显示“新”标记但不抢走选择；用户选择新事件后清除标记。
- 刷新按钮接入现有仓储刷新边界，刷新进行中禁止重复触发。
- 列表启用回收式 WPF 虚拟化，更新时同步同一个可观察集合并保留未变化卡片实例；新增 19 个 App 测试，覆盖排序、搜索、筛选、未知值、选择、新事件、100 条数据和刷新防重入，全套测试增至 84 个。
- 修复独立事件列表控件无法解析父窗口可见性转换器而导致启动退出的问题，详见工程实现文档 `BUG-003`。

### 本版本未包含

- 运行时内存仓储仍为空，固定 JMAXML 尚未解析成领域事件；正常启动时仍显示空列表，不注入合成数据。
- 当前仓储返回完整事件快照，没有更早缓存的分页游标；“加载更早记录”随 SQLite 缓存契约实现。
- 筛选使用左栏可折叠区域并即时应用，尚未实现跨业务页面共用的搜索/筛选抽屉。
- 地图、详情内容、缓存、报文解析和网络数据源仍未实现。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：App、Core 和两个测试工程全部通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 50 个、App 34 个，共 84 个测试全部通过。
- `python -X utf8 tools\validate_test_data.py`：固定数据完整性校验继续通过。
- 原生窗口在 `1440 × 900` 和 `1180 × 720` 下完成空列表、筛选控件和紧凑详情布局检查。
- App 和 Core Release 程序集的产品版本前缀、文件版本分别为 `0.8.0` / `0.8.0.0`。

### 已知限制

- 列表当前在每次页面状态变化时重建最多当前快照规模的卡片投影；已验证 100 条数据，接入大规模历史缓存后需重新测量。
- 新事件标记只在当前进程内维护，没有持久化已读状态；重启后的已读恢复需要缓存表支持。

## 0.7.0 - 2026-08-19

### 已实现

- 新增 `EarthquakePageDisplayState`，把页面数据状态投影为事件数量、总体状态、最后接收、网络、来源、空状态和详情状态文字。
- 显示投影明确区分加载、读取失败、无事件、在线、部分可用、离线和未配置，不再在 XAML 中写死“离线”或“0 条”。
- 顶部状态栏使用固定布局轨道显示应用名、数据状态、最后接收时间、搜索/设置图标和 JST 时钟，避免动态文字改变主内容尺寸。
- 左侧事件区域根据页面状态显示加载、错误、空数据或数据已就绪；错误文字支持限制宽度和换行。
- 底部状态栏动态显示网络、缓存、当前来源、地图数据状态和应用版本，并限制长来源文本宽度。
- 新增 `WindowLayoutViewModel`，使用 `1280 px` 作为桌面与紧凑布局阈值。
- 桌面布局保留 `380 px` 右侧详情栏；紧凑布局释放该列宽，详情改为覆盖地图右侧的可开关抽屉。
- 搜索、设置和详情抽屉按钮使用 Segoe Fluent Icons，并补充自动化名称和工具提示。
- 新增 9 个 App 显示投影和布局状态测试；App 测试增至 15 个，全套测试增至 65 个。

### 本版本未包含

- 尚未实现事件列表项、列表选择、键盘导航、搜索、筛选或排序行为；有事件时只显示“事件数据已就绪”状态。
- 搜索和设置按钮仍为禁用状态，分别在事件列表和设置页面阶段实现。
- 地图工具、GIS 图层和事件详情内容仍未实现，现有地图和详情区域只展示状态。
- 紧凑详情抽屉没有动画和背景遮罩，当前只保证布局、打开和关闭行为。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：App、Core 和两个测试工程全部通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 50 个、App 15 个，共 65 个测试全部通过。
- `python -X utf8 tools\validate_test_data.py`：固定数据完整性校验继续通过。
- `1440 × 900` 和 `1180 × 720` 原生窗口截图未发现应用内文字裁切、区域重叠或越界。
- UI Automation 验证紧凑窗口详情抽屉可以打开和关闭；应用主窗口能够正常退出。
- App 和 Core Release 程序集的产品版本前缀、文件版本分别为 `0.7.0` / `0.7.0.0`。

### 已知限制

- 响应阈值目前固定为 `1280 px`，后续引入真实事件卡片和地图控件后需要再次测量最小可用宽度。
- 显示投影使用来源 ID 作为底栏来源文字；正式数据源接入后需要增加独立的本地化显示名称。

## 0.6.0 - 2026-08-19

### 已实现

- 新增 `IEarthquakeEventRepository`，统一定义事件列表、按 ID 查询、完整快照更新通知和主动刷新入口。
- 新增线程安全的 `InMemoryEarthquakeEventRepository`，接收标准化报文并复用 `EarthquakeEventMerger` 完成合并、去重和排序。
- 内存仓储发布不可变事件快照，支持取消令牌；主动刷新保持为空操作，因为当前没有外部数据源。
- WPF App 首次引用 Core，但只通过仓储接口消费事件，不直接访问 HTTP、SQLite 或 XML。
- 新增不可变 `EarthquakePageState`，分离事件列表、选中事件、当前报文、搜索、排序、地图、来源、加载、刷新、离线和错误状态。
- 新增 `EarthquakePageViewModel`，支持首次加载、主动刷新、事件选择、报文版本选择和返回最新报。
- 仓储更新后使用 `EventId` 和来源消息身份保持当前浏览位置；新事件不会抢走用户正在查看的历史事件。
- 主窗口首次显示后异步初始化空内存仓储，关闭时解除更新订阅；现有空状态界面行为不变。
- 新增[数据契约与格式文档](./数据契约与格式.md)，长期记录领域字段、枚举、未知值、时间、身份、排序、仓储、页面状态和固定数据格式。
- 新增 4 个 Core 仓储测试和 6 个 App 页面状态测试；加上原有测试共 56 个。

### 本版本未包含

- 内存仓储默认为空，尚未把固定 JMAXML 解析成事件或显示到现有列表。
- 搜索、排序和地图状态目前只保存条件，不执行筛选、排序切换或地图渲染。
- 尚未实现 SQLite、网络数据源、后台订阅或来源优先级；`RefreshAsync` 在内存实现中不访问外部系统。
- 主窗口布局和事件控件没有在本版本调整。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：App、Core 和两个测试工程全部通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：Core 50 个、App 6 个，共 56 个测试全部通过。
- `python -X utf8 tools\validate_test_data.py`：固定数据完整性校验继续通过。
- App 和 Core Release 程序集的产品版本前缀、文件版本分别为 `0.6.0` / `0.6.0.0`。

### 已知限制

- 仓储更新事件携带完整快照，适合当前事件规模；真实缓存接入后需要测量大量历史记录下的分配和更新时间，再决定是否增加增量消息。
- 页面 ViewModel 只保存搜索、排序和地图条件，步骤 7/8 实现具体行为时必须继续遵守当前选择保持规则。

## 0.5.0 - 2026-08-19

### 已实现

- 新增纯领域服务 `EarthquakeEventMerger`，按 `EventId` 将多份报文合并为不可变事件时间线。
- 使用 `SourceId + SourceMessageId` 去除同一来源消息的重复输入；重复到达时保留最早 `ReceivedAt` 的记录。
- 事件内报文按 `IssuedAt`、报次、`ReceivedAt`、来源 ID 和来源消息 ID 升序稳定排列；无报次排在数字报次之前。
- 多事件按最后报文发布时间降序排列，发布时间相同时使用 `EventId` 确定顺序。
- `EarthquakeEvent` 提供当前 `LatestReport` 和只包含发布/订正状态的 `LatestEffectiveReport`。
- 新增 `EarthquakeEventSummary`，当前状态来自最后一报；震源、震级和最大震度来自最后有效报文。
- 订正报更新摘要但保留原报；取消报决定当前状态，同时沿用最后有效报文的数据摘要。
- 合并入口拒绝空 `EventId`、`SourceId` 和 `SourceMessageId`，避免无法追踪的报文被错误合并。
- 新增 15 个测试用例，覆盖乱序输入、去重、最早接收记录、完整排序键、多事件排序、旧报、订正、取消、空输入、空时间线和无效标识。

### 本版本未包含

- 尚未解析固定 JMAXML；测试使用直接构造的领域报文验证合并规则。
- WPF App 尚未引用 Core，事件列表仍未显示固定或实时地震数据。
- 尚未建立页面状态、数据仓库接口、SQLite 缓存或网络数据源。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：App、Core 和测试工程全部通过，`0` 个警告、`0` 个错误。
- `dotnet test EarthquakeShow.sln --configuration Release --no-restore --no-build`：46 个测试全部通过，`0` 个失败、`0` 个跳过。
- `python -X utf8 tools\validate_test_data.py`：固定数据完整性校验继续通过。
- App 和 Core Release 程序集的产品版本前缀、文件版本分别为 `0.5.0` / `0.5.0.0`。

### 已知限制

- 去重身份依赖数据适配器提供稳定的 `SourceId` 和 `SourceMessageId`；跨来源内容相同的报文不会在本步骤中合并为同一时间线节点。
- 当前摘要按最后有效报文整体取值，不在多来源之间逐字段仲裁；来源优先级和冲突保留在真实数据源接入阶段实现。

## 0.4.0 - 2026-08-19

### 已实现

- 新建无 WPF、数据库和网络依赖的 `EarthquakeShow.Core` 项目，并加入解决方案。
- 定义 `EarthquakeEvent`、`EarthquakeReport`、`Hypocenter`、`IntensityArea`、`IntensityMunicipality`、`IntensityStation`、`SourceReference` 和 `SourceStatus`。
- 定义报文类型、发布/订正/取消状态、正常/训练/测试上下文和数据源在线、延迟、断开、限流、解析失败、停用状态。
- 使用 `JmaIntensity` 明确表示 `unknown/1/2/3/4/5-lower/5-upper/6-lower/6-upper/7`，提供稳定代码的双向转换。
- 使用 `GeoCoordinate` 拒绝非有限或越界坐标；观测点和震源仍可显式没有坐标。
- 使用可空震源深度区分 `0 km` 和未知；使用 `Magnitude` 拒绝 `NaN/Infinity`，未知震级使用 `null`。
- 所有领域时间使用 `DateTimeOffset` 保留来源偏移量；持久化约定为 ISO 8601 `O` 格式。
- `EarthquakeReport` 保留原始报文代码、来源 ID、来源消息 ID、原文 URL 和 `SourcePayload`，避免标准化丢失来源字段。
- 报文时间线、区域、市町村和观测点集合使用 `ImmutableArray<T>`，避免构建后被外部列表修改。
- 新建 `EarthquakeShow.Core.Tests`，引入 xUnit 2.9.2、xunit.runner.visualstudio 2.8.2 和 Microsoft.NET.Test.Sdk 17.12.0。

### 本版本未包含

- 尚未实现 JMAXML 解析器，固定 XML 还没有转换成领域对象。
- 尚未实现按 `EventID` 合并、报文去重、排序、订正或取消处理。
- WPF App 尚未引用 Core，页面没有新增用户可见地震数据。
- 尚未创建 Infrastructure、SQLite 仓储或数据源接口。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release --no-restore --no-incremental`：App、Core 和测试工程全部通过，`0` 个警告、`0` 个错误。
- `dotnet test tests\EarthquakeShow.Core.Tests\EarthquakeShow.Core.Tests.csproj --configuration Release --no-restore`：31 个测试全部通过，`0` 个失败、`0` 个跳过。
- 测试覆盖震度代码往返、未知震度、`0 km`/未知深度、非有限震级、合法/越界坐标、JST 偏移量、来源原文和不可变报文集合。
- `python -X utf8 tools\validate_test_data.py`：固定数据完整性校验继续通过。

### 已知限制

- 模型只约束单个值和单份报文，不验证一个 `EarthquakeEvent` 内所有报文是否具有相同 `EventID`；该规则在步骤 4 的合并服务中实现。
- `DateTimeOffset` 只保证内存中保留偏移量，SQLite 和 JSON 的 ISO 8601 序列化需要在后续持久化实现中继续测试。

## 0.3.0 - 2026-08-19

### 已实现

- 保存同一 JMA 事件 `20260819071048` 的官方 `VXSE51`、`VXSE52` 和 `VXSE53` XML 原文，覆盖震度速報、震源情报和震源/震度详情链路。
- 官方 `VXSE53` 样本包含 4 个都道府县、7 个细分区域、41 个市町村和 75 个观测点，可用于多区域及多观测点解析测试。
- 增加基于官方 `VXSE53` 的订正夹具，保留同一 `EventID`，使用 `InfoType=訂正`、`Serial=2` 和修正后的震级。
- 增加字段缺失夹具，覆盖震级 `NaN`、震源无坐标、可选字段缺失和观测点坐标无法补齐。
- 从 JMA 官方震度观测点坐标 JSON 筛选并关联详情报中的 75 个观测点编码，生成离线 `JmaStations.csv`。
- 根据官方观测点坐标生成 7 个区域测试包络 GeoJSON，用于区域编码匹配和离线图层加载验证；文件明确标记为非官方边界。
- 增加 `unknown` 到震度 `7` 的标准化、排序和应用显示颜色定义；颜色只作为辅助编码。
- 增加数据清单，记录官方/合成来源、下载日期、SHA-256 和每份报文的预期结果。
- 增加无第三方依赖的离线校验脚本，检查哈希、XML 事件链、订正状态、缺失字段、观测点、GeoJSON 和震度定义。
- 修复校验器将 XML `NaN` 保留为浮点值的问题，详见工程实现文档 `BUG-002`。

### 本版本未包含

- 固定数据尚未接入 WPF 页面，没有新增用户可见的地震事件。
- 尚未实现 JMAXML 解析器、统一领域模型、事件合并或报文排序。
- 区域 GeoJSON 是测试包络，不是 JMA 官方细分区域边界，不得用于正式地图或区域归属判断。
- 未加入 xUnit 工程；本步骤验证的是静态数据契约和完整性，使用独立离线校验脚本更直接。

### 验证结果

- `python tools\validate_test_data.py`：通过，验证 5 份 XML、75 个观测点、7 个区域测试包络和 10 个震度定义。
- 三份官方报文具有同一 `EventID`；订正夹具归属于同一事件；缺坐标观测点未出现在坐标表中。
- 所有固定文件的 SHA-256 与 `manifest.json` 一致，校验过程未访问网络。
- `dotnet build EarthquakeShow.sln --configuration Release --no-restore`：通过，`0` 个警告、`0` 个错误。

### 已知限制

- JMA 官方细分区域 Shapefile 约 110 MB，本版本没有将其纳入测试数据；正式地图阶段只提交经简化和许可核验的派生资源。
- 合成夹具用于边界行为测试，不代表 JMA 实际发布事件，也不替代完整 JMAXML Schema 验证。

## 0.2.0 - 2026-08-19

### 已实现

- 建立 `EarthquakeShow.sln` 和 `EarthquakeShow.App` WPF 工程，目标框架为 `.NET 8 Windows`，输出类型为 `WinExe`。
- 使用 WPF 原生 `Window`、`Grid`、`RadioButton`、`TabControl` 等控件实现地震情报页面外壳，不启动浏览器、WebView 或本地 Web 服务。
- 主窗口包含顶部应用状态、左侧地震导航、事件列表空状态、地图占位区、摘要/观测点/时间线/原始数据四个详情标签和底部状态栏。
- 默认窗口尺寸为 `1440 × 900`，最小尺寸为 `1180 × 720`。
- `MainWindowViewModel` 每秒更新一次 JST 时间，并显示网络、缓存和程序版本占位状态；窗口关闭时停止计时器。
- 使用 `Directory.Build.props` 统一维护 `0.2.0` 程序版本、可空引用、隐式 using、警告即错误和确定性构建设置。
- 修复启动时版本文本绑定到只读属性导致的异常，详见工程实现文档 `BUG-001`。

### 本版本未包含

- 未加入固定或实时地震报文、事件领域模型、合并逻辑和历史数据。
- 未引入 Mapsui/SkiaSharp、SQLite、CommunityToolkit.Mvvm 或其他外部 NuGet 依赖。
- 地图按钮为禁用占位控件；EEW、海啸、搜索、设置和历史回放入口尚未显示。
- 当前没有适合单元测试的领域或数据逻辑，因此未建立测试工程；将在固定报文解析和事件合并逻辑出现时加入 xUnit 测试。

### 验证结果

- `dotnet build EarthquakeShow.sln --configuration Release`：通过，`0` 个警告、`0` 个错误。
- 原生进程 `EarthquakeShow.App.exe` 成功启动，窗口标题为 `Earthquake Show`，未启动浏览器或服务器。
- 实测默认窗口为 `1440 × 900`；请求缩小至 `800 × 600` 时，WPF 正确限制为 `1180 × 720`。
- 通过 Windows UI Automation 验证四个详情标签均存在，可切换至“时间线”并恢复“摘要”。
- 主窗口正常关闭，进程在 5 秒内退出，`dotnet run` 返回代码 `0`。
- 已检查 `1440 × 900` 截图，未发现空白渲染、文字裁切、控件重叠或越界。

### 已知限制

- 当前页面只展示无数据状态，尚不能加载或查看真实地震事件。
- UI 响应式收缩、地图渲染和高 DPI 场景将在对应实现步骤单独验证。

## 0.1.0 - 2026-08-19（已废弃）

- 曾建立 Tauri 2 + Vue 3 + TypeScript 的桌面技术原型。
- 该方案依赖 WebView，不符合最终的原生图形界面要求，未继续作为正式应用实现。
- 相关工程文件和构建产物已按用户要求删除；本版本仅保留为历史技术决策记录，不能从当前工作区运行。
