# 实时地震监测页面功能需求

## 1. 文档信息

- 文档状态：Draft 0.1
- 编写日期：2026-09-01
- 适用页面：P-02 实时 / EEW
- 依据文档：[日本地震信息桌面应用需求文档](./日本地震信息桌面应用需求文档.md)、[日本地震信息桌面应用UI设计文档](./日本地震信息桌面应用UI设计文档.md)、[数据契约与格式](./数据契约与格式.md)
- 目标平台：Windows 10/11 桌面应用
- 术语说明：本文的“实时”指收到上游报文后尽快更新页面，不表示上游数据零延迟；“EEW”指紧急地震速報预测报文；“观测”指 JMA 发布后的震度结果。

本文是实时监测页面的长期功能基线。实现、测试或发现缺陷后，应优先更新本文对应章节，再更新工程实现文档和数据契约；不能只修改界面而不记录行为变化。

## 2. 目标与边界

### 2.1 页面目标

用户打开页面后，应在一个窗口内完成以下任务：

1. 判断当前是否存在有效的 EEW、最高警戒级别和最新报次；
2. 查看当前预测震源、预测震度和警报区域；
3. 观察 P 波、S 波传播估计的当前范围，并知道其为本地计算结果；
4. 查看同一事件的报文演进、订正、最终报和取消报；
5. 在实时事件和已缓存事件之间切换，不丢失当前查看上下文；
6. 判断数据来自哪个来源、是否延迟、是否处于离线或降级状态。
7. 在地图上查看当前有效实时观测点及其震度，并能区分实时观测、JMA 事后观测和 EEW 预测。

### 2.2 与其他页面的边界

| 页面 | 负责内容 | 不负责内容 |
| --- | --- | --- |
| 地震情报 P-01 | JMA 震度速報、震源情报、震源/震度情报及事后观测 | 连续逐站实时加速度、EEW 波前动画 |
| 实时 / EEW P-02 | EEW 报次、预测震度、警报区域和波前估计；连续全国逐站震度 | 用事后观测替代缺失的 EEW |
| 海啸 P-03 | 海啸预报、注意报、警报、观测和解除 | 将海啸等级推断为地震预测结果 |
| 设置 P-04 | 来源、刷新、显示、缓存和通知策略 | 直接修改事件或报文数据 |

### 2.3 本页面的实时观测边界

- 实时观测点是独立数据流和独立地图图层，不使用 P-01 的事后观测填充实时数据。
- 在正式数据源授权、稳定性和覆盖范围完成验收前，实时观测层必须标记为“实时观测（来源名称）”和当前延迟，不得宣称为 JMA 官方逐站报文。
- 不能获得实时样本时，页面保留最后一次有效观测并显示“观测数据延迟/不可用”，不能用 EEW 预测震度替代观测点震度。
- 连续实时 PGA、PGV、PGD 和自研摇晃检测可以与实时震度共用站点流，但必须作为独立字段和独立图层，不能从震度等级反推加速度。
- 不把本地计算的波前、震源或预测震度标为 JMA 官方值。
- 不提供生命安全、避难、交通控制或设备控制的唯一决策依据。

## 3. 使用场景

| 场景 | 用户操作 | 必须得到的结果 |
| --- | --- | --- |
| 正常监看 | 打开应用并停留在 P-02 | 自动显示最新 EEW 事件和报次，页面持续更新 |
| 新报到达 | 收到同一事件的新 Serial | 原事件更新，不产生重复事件；地图、摘要、时间线同步 |
| 高级别预警 | 收到警报或高预测震度 | 顶部全局警报条和事件列表立即突出，但不强制抢走用户正在看的历史事件 |
| 查看旧报 | 点击时间线中的旧报 | 地图、预测区域和摘要切换到该报快照，波前保持静态 |
| 断线 | EEW WebSocket 断开 | 显示来源异常、重连倒计时，并保留最后快照 |
| 离线启动 | 无网络启动 | 显示本地缓存的 EEW 历史，明确标注缓存时间和离线状态 |
| 恢复连接 | 网络恢复 | 先补取缺失报文，再恢复实时订阅；按身份去重，不重复弹出事件 |
| 取消/最终报 | 收到取消或最终报 | 停止相关动画，保留完整时间线并显示最终状态 |
| 训练/假定 | 收到训练或假定震源 | 使用独立标签和视觉样式，不触发正式警报样式或通知 |
| 实时观测 | 收到新的逐站样本 | 更新地图上的观测点震度、时间戳和数据新鲜度；不改变 EEW 报次 |
| 观测流断开 | 实时观测源不可用 | 保留最后快照，显示最后有效时间和来源错误；不清空 EEW 页面 |

## 4. 页面布局

### 4.1 主窗口结构

```text
┌─────────────────────────────────────────────────────────────────────┐
│ 应用名称 │ 日本时间 │ EEW来源状态 │ 最近接收 │ 全局警报 │ 搜索 │ 设置 │
├──────────┬────────────────────────────────────┬─────────────────────┤
│ 一级导航 │                                    │                     │
│ 地震     │                                    │ 当前报摘要          │
│ 实时/EEW │             实时 EEW 地图           │ 预测区域 / 警报区域  │
│ 海啸     │                                    │ 报文时间线 / 原始数据│
│          │                                    │                     │
│ EEW事件列表                                    │                     │
├──────────┴────────────────────────────────────┴─────────────────────┤
│ 网络/缓存 │ 当前来源 │ 波前计算状态 │ 地图数据版本 │ 状态说明          │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 尺寸和响应

- 默认窗口尺寸 `1440 × 900`，最小尺寸 `1180 × 720`。
- 左侧事件列表建议 `280 px`，右侧详情建议 `380 px`；地图占据剩余宽度。
- 地图、列表和详情的宽度不能因动态文本改变；长文本在详情栏内换行或省略。
- 窗口较窄时，右侧详情收为抽屉；实时状态栏和全局警报条仍保持可见。
- 全局警报条不得遮挡地图控制、图例或详情标签。

## 5. 顶部和底部状态栏

### 5.1 顶部状态

必须显示：

- 当前时间，默认 `YYYY-MM-DD HH:mm:ss JST`；切换本地时区时必须带时区标识；
- EEW 来源状态：在线、延迟、断开、等待重连、限流、解析失败、已停用；
- 最近成功接收时间和消息年龄；
- 当前最高有效 EEW 状态：无警报、预报、警报、训练、假定、已取消；
- 当前页面是否跟随最新事件。

状态只用颜色是不充分的，必须同时显示文字和状态点。状态项可点击打开来源诊断抽屉。

### 5.2 底部状态

- WebSocket 当前状态、连续重连次数、下次重试时间；
- HTTP 补取状态和最近一次补取范围；
- 当前波前动画状态：运行、暂停、已取消、历史静态、计算不可用；
- 本地缓存状态、最新缓存时间和地图数据版本；
- 最近错误摘要；完整异常通过诊断抽屉查看。

## 6. EEW 事件列表

### 6.1 事件身份和排序

- 事件主键为上游 `EventID`，报次由 `Serial` 表示；事件列表只能出现一条事件代表项。
- 同一事件收到新报时更新代表项，完整报次进入该事件的时间线。
- 默认按“最新发布时间”倒序；可切换“发生时间”“最大预测震度”“状态优先级”。
- 旧报不得覆盖新报；Serial 相同但来源不同的消息按来源身份去重。
- 用户查看历史事件时，新事件只更新未读标记和全局提示；开启“跟随最新事件”后才切换选择。

### 6.2 事件卡片字段

每条事件至少显示：

- 状态标签：预报、警报、最终、取消、训练、假定；
- 当前报次，如 `第 3 报`；
- 震源地、发生时间；
- 最大预测震度；未知显示“不明”；
- 是否最终报、是否取消；
- 发布时间、接收时间或消息年龄；
- 数据来源标识；多来源合并时显示“多源”。

最大预测震度使用与 P-01 一致的十档震度色板；深色背景使用白字，浅色背景使用深色字，并同时显示文字等级。

### 6.3 列表操作

| 操作 | 行为 |
| --- | --- |
| 单击事件 | 选中事件，更新地图和详情 |
| 上/下键 | 切换事件；选择变化不启动历史波前动画 |
| Enter | 将焦点移入右侧详情 |
| 刷新 | 请求允许主动刷新的来源；期间禁用重复点击 |
| 筛选 | 按状态、时间、预测震度、震源区域、来源筛选 |
| 排序 | 在发生时间、发布时间、预测震度、状态优先级间切换 |
| 返回最新 | 选择最新有效报次并恢复自动跟随 |
| 加载更早记录 | 从 SQLite 读取更早的缓存事件 |

## 7. 实时 EEW 地图

### 7.1 图层顺序

从底到顶固定为：

1. 日本及周边海域底图；
2. 行政/预报区域边界；
3. EEW 警报区域；
4. 区域预测震度面；
5. P 波估计波前；
6. S 波估计波前；
7. 官方预测震源；
8. 本地推测图层（默认关闭）；
9. 选中对象高亮和标签。

图层顺序必须保证震源和选中高亮不被区域填充遮挡；预测和推测使用不同线型、颜色和图例。

### 7.2 图层语义

- 警报区域：来自上游 EEW 报文，不根据震级或距离自行扩大。
- 区域预测震度：来自上游预测值；未知区域不着色，不使用事后实测值替换。
- P/S 波前：本地计算的传播估计，必须在图例和详情中标注“计算估计”。
- 震源：使用独立图标；官方预测震源与本地推测震源不可使用相同图标。
- 实时观测点：默认开启；仅绘制坐标有效、样本未过期、震度值有效的站点。图例和状态栏必须显示“实时观测”及来源名称。
- 事后观测点：只有在切换到已结束事件并主动启用“JMA 事后观测”图层时显示，不能与实时观测使用相同标签。
- 实验观测源：nTool/Yahoo 逐秒 JSON、Wolfx SeisJS 或未确认授权的 NIED 接口默认关闭，并显示“研究数据/非官方”。

### 7.3 地图交互

- 鼠标左键拖动平移，滚轮以指针位置为锚点缩放；缩放和拖动不改变事件选择。
- `放大`、`缩小`、`日本全图`、`定位震源`、`跟随最新`、`图层`、`全屏地图`按钮与 P-01 保持一致。
- 点击警报区域显示区域名称、警报级别、预测震度、报次和来源；点击震源显示震源详情。
- 波前动画运行时地图视角默认跟随当前事件包络；用户手动拖动/缩放后暂停自动跟随。
- 无有效震源坐标时不绘制波前，地图保留区域预测图层并说明“震源坐标未知”。
- 当前报文切换、地图精度异步切换和刷新不得重置用户视口，除非用户主动点击定位或日本全图。

### 7.5 实时观测点显示规则

- 每个站点至少显示：站点名称、站点编码、经纬度、当前震度、样本时间、来源和数据新鲜度。
- 站点颜色沿用 P-01 的十档震度色板；浅色震度使用深色边框/文字，深色震度使用浅色边框和白字。
- 站点绘制顺序按震度从低到高，高震度站点位于低震度站点上方；选中站点使用额外高亮环。
- 默认视图只绘制最新有效样本；缩放较小时隐藏文字标签但保留点，放大后显示名称和震度。
- 样本年龄不超过 `5 秒`（可在设置中调整）的站点为“实时”；超过阈值但不超过 `30 秒` 的站点显示为“延迟”并降低透明度；超过 `30 秒` 或时间未知的站点不绘制，只在列表/诊断中保留。
- 震度为未知、欠测、设备异常或质量标记不合格时不着色为有效震度；站点可显示中性标记或仅保留在列表，不能显示为震度 0。
- 同一站点同时收到多个来源时，按来源优先级选取显示值，并在详情中保留其他来源差异；不得把不同来源样本平均成一个震度。
- 实时观测层不改变事件的最大预测震度、EEW 状态或时间线；它只更新站点样本和实时观测统计。

### 7.6 实时观测模式与事件关联

- `当前实时`模式显示全量有效站点的最新样本，不要求已经识别出地震事件。
- 选中某个 EEW 事件时，可切换为`事件观测`模式；按站点样本时间与事件发生时间窗口筛选，并在页面上明确显示筛选窗口。
- 实时样本带有上游事件 ID 时使用该 ID 关联；没有事件 ID 时只能作为独立实时观测，不能仅凭最近震源名称强行归并。
- 事件结束、取消或切换历史报次时，实时观测层可以继续运行，但历史快照中的事后观测必须单独标记和存储。
- 页面必须同时显示“实时观测站点数”“延迟站点数”“无效/缺失站点数”和最新样本时间。

### 7.4 波前计算规则

- 输入至少包括震源经纬度、深度、发生时间、当前时间和选定走时模型。
- P 波和 S 波使用独立传播速度/模型参数；参数版本和计算基准时间必须可见。
- 波前只表示估计到达范围，不表示实际地震动强度或安全边界。
- 发生时间、震源或深度更新时重新计算；仅状态字段变化时不重复计算。
- 计算必须在后台线程执行，取消、切换事件或报次时取消旧任务。
- 暂停页面动画不停止数据接收；切回最新时以当前时间重新计算。
- 取消报、最终报或切换到历史报时停止动画计时器，保留最后一帧或静态波前。

## 8. 右侧详情

右侧详情使用固定宽度的标签页：`摘要`、`预测区域`、`时间线`、`原始数据`。

### 8.1 摘要

按以下顺序逐行显示：

1. 状态和报次；
2. 最大预测震度；
3. 震源地；
4. 震级；
5. 深度；
6. 经纬度；
7. 发布时间、发生时间、接收时间；
8. 最终/取消/训练/假定/精度标记；
9. 数据来源和原始消息身份。

字段缺失时显示“不明”或隐藏，具体按数据契约；不能显示 `0`、空字符串或把缺失预测值当成观测值。摘要底部提供 `上一报`、`下一报`、`返回最新`，与时间线共用同一选择状态。

### 8.2 预测区域

每行显示：区域名称、预测震度、是否警报区域、预测到达状态（如有）、报次和来源。支持按预测震度、警报状态筛选和按震度降序排序。选择行后地图定位并高亮；无地图几何时保留列表并说明“地图区域未知”。

### 8.3 时间线

每个节点显示：报文类型、报次、状态、发布时间、接收时间、来源和相对上一报的变化。

- 首次出现的字段只显示当前值；
- 值发生变化显示 `旧值 → 新值`；
- 相同值、缺失值或未变化值不显示箭头；
- 不能把已收到的值重置为“不明”；
- 报次按 `IssuedAt → Serial → ReceivedAt → Source` 稳定排序；
- 用户主动点击旧报后，摘要、预测区域、地图和波前全部切换到该报快照；
- 历史报文不运行动画，只显示静态计算结果或“未计算”。

### 8.4 原始数据

- 默认显示结构化摘要；可切换原始 XML/JSON 和标准化 JSON；
- 原始数据只读，不执行脚本；
- 支持复制、自动换行和打开来源链接；
- 显示来源、消息 ID、抓取时间和校验/解析状态；
- 原文缺失时显示“原始内容未缓存”，不能伪造空 JSON。

## 9. 报文处理和事件状态机

### 9.1 接收类型

正式接入的 EEW 适配器以 Wolfx JMA EEW WebSocket 为主，HTTP 用于启动和断线补取；具体字段以数据契约定义为准。至少兼容：预报、警报、最终、取消、训练、假定震源和未知扩展字段。

### 9.2 状态优先级

事件状态按以下规则解释：

1. 取消报优先显示“已取消”，但不删除此前预测；
2. 最终报显示“最终报”，停止波前动画；
3. 训练/假定始终保留独立标签，不升级为正式警报；
4. 警报高于普通预报；
5. 未知状态保留原始值并显示“未知状态”，不得静默归入普通预报。

### 9.3 旧报保护

- 以 `EventID + Serial` 作为事件内排序身份；
- 同一身份重复收到时只更新接收时间/来源状态，不新增时间线节点；
- Serial 较小的迟到报文不能覆盖当前最新快照；
- 跨来源合并须同时满足事件时间、坐标和震级阈值；无法确认时保留独立来源记录并在详情显示差异。

### 9.4 实时观测数据源

实时观测点必须通过独立适配器接入，不能把 EEW 报文或 JMA 事后震度报文当成连续站点流。实现时保留 `RealtimeObservationSource` 抽象，使来源可以替换而不改变地图和 ViewModel 契约。

| 优先级 | 数据源 | 可提供内容 | 接入方式 | 正式使用条件 |
| --- | --- | --- | --- | --- |
| P0（技术主路线） | NIED 强震监视器 KMONI | 全国强震监视器站点的实时震度、PGA/PGV/PGD 等图像编码信息 | 按时间戳轮询 GIF/PNG；使用版本化站点像素映射表和颜色表解析 | 必须取得 NIED 对持续抓取、解析、再展示和再分发的明确许可；没有许可只能做本地研究适配器 |
| P0（商业生产备选） | DM-D.S.S | JMA 电文和实时监测相关数据，低延迟、服务稳定性较高 | 按其正式 WebSocket/HTTP 配信协议接入 | 需要购买服务并遵守合同；不符合当前“仅免费 API”成本约束时不进入默认发布版 |
| P1（待核验） | NIED/强震监视器 JSON 或手机应用接口 | 设计上可提供站点目录和逐时刻数值 | 站点表 + 时间键 JSON 轮询 | 现有公开资料标注大部分接口不可用；必须重新验证可用性、字段版本、限流和授权后才能进入生产 |
| P2（研究验证） | nTool/Yahoo 逐秒 JSON | 站点目录、逐秒震度、P/S 波和震源摘要 | 读取 `SiteList/sitelist.json` 与 `RealTimeData/{日期}/{时间}.json` | 未发现面向第三方的稳定 API 和再分发许可；默认关闭，不得作为正式依赖 |
| P2（研究验证） | Wolfx SeisJS | 社区测站 PGA、三轴加速度、计测震度和长周期等级 | HTTP/ WebSocket | 不是 JMA/NIED 全国官方网，覆盖不完整；只能作为实验图层，必须显示非官方和站点来源 |
| P3（事件后补充） | JMA XML `VXSE53`、JMA 地震详情 JSON、P2PQuake | 事件发布后的观测点震度和坐标 | Feed/HTTP/WebSocket 事件报文 | 可作为 P-01 事后观测和实时流断线后的事件补充，但不是连续实时站点源 |

#### 9.4.1 推荐落地策略

1. 先实现来源无关的站点快照、质量状态、新鲜度和地图图层契约；
2. 用固定 KMONI 图像和站点映射样例完成解析器与地图联调，适配器明确标记为 `research-nied-kmoni`；
3. 在没有书面许可前，发布版不自动请求或再分发 KMONI 图像解析结果；
4. 若需要可对外发布的全国实时站点能力，优先评估 DM-D.S.S 合同或取得 NIED 书面授权；
5. NIED JSON、nTool/Yahoo、Wolfx SeisJS 只用于兼容性实验、回放和故障对比，不作为 P0 正式数据保证。

#### 9.4.2 站点快照最小字段

每个实时样本至少保存：`SourceId`、`StationCode`、`StationName`、`Latitude`、`Longitude`、`SampledAt`、`ReceivedAt`、`Intensity`、`Quality`、`IsRealtime`、`EventId?`、原始消息身份和原始内容引用。PGA、PGV、PGD、三轴分量和算法版本作为可空扩展字段保存，不能用震度反推。

#### 9.4.3 来源失败与数据质量

- 图像 HTTP 失败、时间戳缺帧、图像尺寸变化、站点表版本不匹配、颜色无法识别和数组长度不一致都必须进入 `ParseFailed` 或 `Delayed`，并保留错误原因。
- 单个站点异常不得阻断其他站点；整帧校验失败时不得把上一帧误标为当前时间。
- 来源时间使用上游时间戳，接收时间单独记录；页面显示消息年龄和本机时钟偏差告警。
- 站点目录更新时使用目录版本和校验值；禁止按数组下标静默套用旧坐标。
- 同一时刻只允许一个实时观测轮询/订阅任务；页面关闭、来源停用或应用退出时必须取消任务并释放图像、网络和计时器资源。

## 10. 实时刷新、离线与恢复

### 10.1 正常刷新

- WebSocket 接收线程只负责读取、校验和发布标准化报文；
- UI 线程消费不可变快照，单次批量更新列表、地图和详情；
- 收到报文后目标是 1 秒内完成页面更新；
- 同一报文重复推送不得造成列表闪烁、重复声音或重复时间线节点。

### 10.2 断线和限流

- WebSocket 断开后使用指数退避并显示重连次数和下次重试时间；
- HTTP 补取和 WebSocket 连接必须遵守来源限流，禁止并发建立多个生产连接；
- 429、超时、DNS/网络错误、TLS 错误和解析错误分别记录诊断原因；
- 单一来源失败不得清空已有事件或阻断其他来源；
- 连续失败达到阈值后显示“来源不可用”，仍保留缓存页面。

### 10.3 离线启动和回补

- 启动顺序：读取本地快照 → 显示缓存页面 → 后台连接来源；
- 以 SQLite 中各来源最新 `IssuedAt` 为增量起点；
- 恢复后优先补取起点之后的所有可用报文，再切换到实时订阅；
- 补报按消息身份去重并保持原始发布时间；
- 上游覆盖不足时在来源状态中说明“无法保证完整回补”，不能假装没有缺报；
- 网络恢复不强制改变用户正在查看的历史事件，除非开启自动跟随。

## 11. 设置项

设置页按以下分组提供选项，所有设置都持久化到独立 settings 文件：

### 11.1 数据源

- 启用/停用 Wolfx EEW WebSocket；
- 启用/停用 HTTP 补取；
- P2PQuake 是否作为补充源（默认按全局策略关闭或只读）；
- 连接超时、重试上限和轮换周期（提供推荐值，普通用户不必修改）；
- 来源和地图数据版本查看。

### 11.2 显示和跟随

- 默认打开页面；
- 自动跟随最新事件；
- 是否显示 P 波、S 波、预测区域、警报区域和标签；
- 波前动画开关和帧率档位；
- 时间显示时区；
- 地图默认缩放和低内存模式。
- 实时观测点图层开关（默认开启）；
- 仅显示实时/包含延迟站点/显示全部诊断站点；
- 实时样本过期阈值（默认 `5 秒`）和隐藏阈值（默认 `30 秒`）；
- 是否显示站点名称、震度标签和 PGA/PGV 扩展值。

### 11.3 实时观测源

- 选择实时观测来源；
- 研究适配器总开关（默认关闭）；
- KMONI 图像轮询间隔（来源许可允许时才可启用）；
- 是否在来源延迟时保留最后有效站点；
- 站点目录版本、映射校验结果和最近更新时间。

### 11.4 通知

- 是否显示桌面通知；
- 触发级别（警报、预测震度阈值、仅状态变化）；
- 训练/假定事件是否通知（默认关闭）；
- 声音资源和音量（资源缺失时不阻塞页面）。

### 11.5 缓存和诊断

- 保留事件数量和原始报文开关；
- 查看缓存目录、日志目录和来源诊断；
- 清除缓存前必须二次确认，并明确设置文件不会被删除；
- 导出诊断包时去除用户本地路径之外的个人信息。

## 12. 异常、可访问性与合规

- 任何状态不能只依赖颜色；每个等级必须同时有文字、图标或纹理差异。
- 支持键盘导航、事件列表上下移动、时间线切换和 Esc 关闭抽屉。
- 地图中的信息必须能在预测区域表格和摘要中等价获取。
- 来源异常、字段缺失、坐标未知、计算失败和缓存过期必须分别表述。
- 页面及“关于”中显示 JMA、Wolfx、P2PQuake 和地图数据来源、使用条件及免责声明。
- XML/JSON 只作为数据解析，不执行其中脚本或动态内容；来源链接使用外部浏览器打开前明确提示。

## 13. 性能、资源和生命周期

- 常驻监测时 UI 线程不得执行网络、XML 解析、数据库写入或波前计算。
- 默认只绘制当前事件和当前报次图层，历史报文按需加载；
- 地图继续使用现有低/中精度 LOD 和可取消异步加载；实时页面不得强制加载高精度全量地图；
- 波前动画在窗口最小化、页面不可见或用户暂停时降低或停止绘制，但后台接收继续；
- 单次刷新必须释放旧的取消令牌、计时器、事件订阅和临时几何，防止后台任务挂起和内存增长；
- 目标基线：空闲页面内存增长趋近于零；连续监测 30 分钟无持续线性增长；收到重复报文不造成集合无限增长；
- 列表、时间线和地图使用不可变快照替换，避免 UI 线程与后台线程共享可变集合。

## 14. 数据存储和日志

- SQLite 保存事件、报文、标准化字段、原始内容（按设置决定）和来源身份；
- 事件与报次必须支持按 `EventID + Serial + SourceId + SourceMessageId` 查询；
- 诊断日志记录连接状态变化、重连、限流、解析失败、补报范围、去重结果和波前计算失败；
- 日志不得记录 API 密钥（当前不需要）或无关个人信息；
- 版本升级先更新数据契约，再实现迁移；未知 schema 版本不得静默读取。

## 15. 测试需求

### 15.1 单元测试

- EventID/Serial 身份和重复报去重；
- 旧报不能覆盖新报；
- 预报、警报、最终、取消、训练、假定状态映射；
- 缺失字段、未知枚举和扩展字段处理；
- 跨报次累计字段和变化箭头；
- P/S 波计算、时间推进、取消令牌和边界坐标；
- 预测震度颜色、深浅色文字和未知值显示；
- 实时站点快照的新鲜度、过期/隐藏阈值和质量状态；
- KMONI 图像颜色到震度的映射、站点像素映射、时间戳和目录版本校验；
- 实时观测点按震度排序、重叠绘制和多来源优先级；
- 增量补报起点、回补去重和排序；
- 来源状态到页面文案的映射。

### 15.2 集成测试

- Wolfx WebSocket 报文进入仓储、事件合并和 ViewModel；
- WebSocket 断线后 HTTP 补取再恢复订阅；
- 同一事件多来源到达不产生重复列表项；
- 取消/最终报停止动画且时间线完整；
- 离线启动、恢复连接和缓存回补；
- 页面刷新期间保持事件、报次和地图视口选择。
- 实时观测源连续更新时只替换站点快照，不重复创建 EEW 事件或时间线节点；
- 实时观测源断开、缺帧、目录版本变化和恢复后的站点状态；
- 实时观测层与 JMA 事后观测层同时存在时的视觉和数据隔离。

### 15.3 手动验收

- 使用固定发布、警报、最终、取消、训练、假定样例逐项确认视觉标签；
- 连续拖动/缩放地图，确认波前和区域不越界、视口不乱跳；
- 使用固定站点快照确认实时、延迟、过期、欠测和无坐标站点的显示规则；
- 运行 30 分钟并观察进程、句柄、线程和内存趋势；
- 断网、限流、解析错误和恢复场景均能继续查看缓存；
- 窄窗口、键盘操作、深色等级白字和无障碍等价信息均可用。

## 16. 验收标准

1. 页面可在不打开浏览器或本地 Web 服务的情况下启动并显示缓存/实时状态。
2. 同一 `EventID` 的所有 Serial 只形成一条事件卡片，时间线完整且顺序稳定。
3. 新报到达后 1 秒内同步更新列表、摘要、预测区域、地图和状态栏。
4. 预测值、观测值、本地波前计算和研究推测在文字、颜色、图例和原始来源上均可区分。
5. 旧报、最终报和取消报不会被迟到消息覆盖；取消/最终后动画停止。
6. WebSocket 断线、429、超时和解析错误不会导致应用退出或清空现有数据。
7. 网络恢复后能够按最新缓存时间补取可用报文，并对覆盖不足给出明确提示。
8. 手动拖动/缩放后不会自动抢回视角；点击跟随或定位按钮后恢复预期视角。
9. 地图默认显示实时观测点；每个点能查看震度、站点、来源、样本时间和新鲜度，过期点不会伪装成当前数据。
10. 实时观测流断开或解析失败时，页面保留最后有效快照并明确显示延迟/不可用，不影响 EEW 事件接收。
11. 页面在 30 分钟连续运行中无可观察的任务、计时器、订阅或内存持续泄漏。
12. 所有关键行为有固定样例测试或可重复的手动验收记录。

## 17. 实现阶段建议

### 阶段 A：页面骨架和状态契约

- 增加 P-02 页面状态、事件选择和报次选择模型；
- 复用现有三栏外壳、来源状态和设置导航；
- 先用固定 EEW 样例验证摘要、列表和时间线。

### 阶段 B：实时接收和事件聚合

- 接入 Wolfx WebSocket 适配器；
- 完成 `EventID + Serial` 去重、排序、取消/最终状态机；
- 增加断线重连、HTTP 补取和来源诊断。

### 阶段 C：预测区域和地图图层

- 接入预测区域和最大预测震度；
- 复用地图 LOD、缩放、拖动和视口保持；
- 增加警报区域、预测面、震源、实时观测点和图例；
- 先用固定实时站点快照验证点位、颜色、层级和新鲜度，再接网络源。

### 阶段 D：P/S 波估计

- 固化走时模型和参数版本；
- 实现可取消计算、暂停/继续、返回现在和最终/取消停止；
- 完成性能和长时间运行验证。

### 阶段 E：设置、通知和发布验收

- 增加来源、跟随、动画、实时观测、通知和缓存设置；
- 完成来源声明、免责声明、安装包和回归测试；
- 只有在 NIED 书面授权或 DM-D.S.S 合同验证通过后，才在正式发布版启用全国连续实时观测；否则仅发布无实时站点图层的版本。

## 18. 依赖、真实数据和未决事项

- 需要至少一组 Wolfx JMA EEW 的真实或脱敏固定报文，覆盖预报、警报、最终、取消、训练和假定字段；如果无法提供，应从公开样例或已授权抓包中建立固定测试样例。
- 需要确认 Wolfx 当前 WebSocket 字段和使用条款是否允许正式再分发；字段变化必须先更新数据契约。
- P/S 波速度、走时模型和警报区域的官方语义需要在实现阶段以来源文档为准；不确定字段必须保留原文。
- 实时逐站震度已列为本页面目标功能，但正式发布依赖 NIED 书面授权或可替代的稳定授权数据源；PGA、PGV 和摇晃检测仍属于后续扩展。
- 需要一组真实或脱敏的逐站实时样本（含站点目录、时间戳、震度、缺帧和欠测），用于联调和验收；如果用户无法提供，应先取得可合法使用的官方/授权样例。
- 是否启用桌面通知和声音提醒需要用户在设置中主动开启，训练和假定事件默认不通知。

## 19. 版本记录

| 日期 | 文档版本 | 内容 |
| --- | --- | --- |
| 2026-09-01 | Draft 0.1 | 首次定义实时/EEW 页面目标、布局、事件状态机、波前估计、离线恢复、测试和阶段拆分。 |
| 2026-09-01 | Draft 0.2 | 将实时逐站观测点提升为目标功能，增加实时地图图层、站点新鲜度、数据质量、来源分级和正式启用条件。 |
