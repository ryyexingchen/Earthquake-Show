项目按 `C# + .NET 8 + WPF + Mapsui/SkiaSharp + SQLite` 推进。应用使用 Windows 原生窗口和控件，地图在本地原生画布中渲染，不使用浏览器、WebView、HTML/CSS 或本地 Web 服务。

首轮不接入 NIED/Yahoo 实时 Demo，也不直接从实时 API 开始。先用固定报文把页面闭环跑通，再接后台数据源。

## 当前进度

- `0.1.0` Tauri/Vue 外壳已停止开发，相关工程文件和构建产物已于 2026-08-19 删除，历史信息只保留在文档中。
- 本机已具备 `.NET SDK 8.0.412` 和 Windows Desktop Runtime，可直接开发 WPF。
- `0.2.0` 已完成步骤 1：WPF 原生桌面外壳已建立，并通过 Release 构建、窗口交互、最小尺寸和进程退出验证。
- `0.3.0` 已完成步骤 2：准备官方 `VXSE51/52/53`、合成异常夹具、观测点坐标、区域测试 GeoJSON 和震度定义，并通过离线完整性校验。
- `0.4.0` 已完成步骤 3：建立地震情报统一领域模型和值类型，并通过 31 个 xUnit 测试验证未知值、坐标、深度、时区和来源字段契约。
- `0.5.0` 已完成步骤 4：实现事件合并、来源消息去重、报文稳定排序和最新有效报文摘要，并通过 46 个 xUnit 测试。
- `0.6.0` 已完成步骤 5：建立事件仓储接口、内存仓储和地震情报页面状态，并通过 56 个 xUnit 测试。
- `0.7.0` 已完成步骤 6：完善主窗口动态状态绑定和紧凑窗口详情抽屉，并通过 65 个 xUnit 测试及双尺寸视觉验证。
- `0.8.0` 已完成步骤 7：实现虚拟化地震事件列表、搜索、筛选、排序、刷新、键盘选择和新事件标记，并通过 84 个 xUnit 测试。
- `0.9.0` 已完成步骤 8：实现随应用打包的离线日本示意地图、震度区域/震源/观测点图层、缩放和事件定位，并通过 89 个 xUnit 测试及原生窗口验证。
- `0.10.0` 已完成数据前置步骤 8A：解析固定 `VXSE51/52/53` 和订正夹具，将真实样本合并后注入事件列表和地图，并通过 101 个 xUnit 测试。
- `0.11.0` 已完成步骤 9：实现摘要、区域/观测点、报文时间线和原始数据详情，并通过 105 个 xUnit 测试。
- `0.12.0` 已完成步骤 10：实现 SQLite 缓存和离线启动，并通过 111 个 xUnit 测试。
- `0.13.0` 已完成步骤 11 第一阶段：接入 JMA JSON 实时摘要和刷新机制，并通过 117 个 xUnit 测试。
- `0.14.0` 已完成步骤 11 第二阶段：接入 JMA XML 实时 Feed、详情报文和 JMA JSON/XML 来源优先级，并通过 122 个 xUnit 测试。
- `0.15.0` 已完成步骤 11 第三阶段：接入 P2PQuake HTTP 补充源，并通过 125 个 xUnit 测试。
- `0.16.0` 已完成步骤 11 第四阶段：实现同一事件内的来源差异展示，并通过 126 个 xUnit 测试。
- `0.17.0` 已完成步骤 11 第五阶段：实现跨源事件候选关联提示，并通过 133 个 xUnit 测试。
- `0.18.0` 已完成步骤 11 第六阶段：实现自动刷新、指数退避和刷新互斥，并通过 137 个 xUnit 测试。
- `0.19.0` 已完成步骤 11 第七阶段的连接契约切片：建立 P2PQuake WebSocket 文本消息边界、单对象解析和取消行为，并通过 141 个 xUnit 测试。
- `0.20.0` 已完成步骤 11 第八阶段的生命周期切片：实现 P2PQuake WebSocket 顺序重连、指数退避、在线重置和取消等待，并通过 146 个 xUnit 测试。
- `0.21.0` 已完成步骤 11 第九阶段：将 P2PQuake WebSocket 接入应用生命周期、SQLite 和页面状态，并通过 148 个 xUnit 测试。
- `0.22.0` 已完成步骤 11 第十阶段：显示 P2PQuake WebSocket 重连次数和下次重试时间，并通过 149 个 xUnit 测试。
- `0.23.0` 已完成步骤 11 第十一阶段：显示 P2PQuake WebSocket 连接持续时间和最近错误详情，并通过 151 个 xUnit 测试。
- `0.24.0` 已完成步骤 11 第十二阶段：显示消息活性和连接异常统计，并通过 153 个 xUnit 测试。
- 下一实现步骤：实现 P2PQuake WebSocket 主动心跳策略和连接轮换。

## 文档同步规则

- 每次实现一个可独立说明的小功能时，更新[版本记录](./版本记录.md)。
- 工程结构、运行方式、数据流、关键决策或故障修复发生变化时，更新[工程实现文档](./工程实现文档.md)。
- 领域字段、枚举、身份、排序、仓储或数据文件格式变化时，更新[数据契约与格式](./数据契约与格式.md)。
- 功能范围变化同步更新需求文档，页面结构或交互变化同步更新 UI 设计文档。
- 文档与代码必须在同一次变更中更新，不能留到版本结束后集中补写。

**完成标准**

地震情报页面完成后，应满足：

1. 桌面程序可启动，不依赖浏览器。
2. `VXSE51/52/53` 同一 `EventID` 合并为一个事件。
3. 最近事件和历史记录在同一个列表中浏览。
4. 选择事件后，列表、地图和右侧详情同步更新。
5. 地图显示震源、区域震度和有坐标的观测点。
6. 时间线可以切换上一报、下一报和最新报。
7. 离线启动可以读取缓存。
8. 未知、订正、取消、训练和测试状态不会被错误显示成普通值。

正式发布使用 Windows x64 自包含构建，最终用户不需要另行安装 .NET Runtime；该项在发布阶段验证，不属于桌面外壳的首轮任务。

## 具体步骤

### 1. 确定工程技术栈并创建桌面外壳

状态：已于 `0.2.0` 实现并验证。

建立 .NET 解决方案和 WPF 应用工程，先生成一个真正的 Windows 桌面窗口。运行时不得启动 HTTP 服务，也不得依赖浏览器或 WebView。

第一步只使用 WPF 和 .NET 8 自带能力：

- WPF `Window`、`Grid`、`ListBox`、`TabControl` 等原生控件建立三栏外壳。
- C# ViewModel 管理 JST 时钟、当前标签和基础页面状态。
- Windows `Segoe Fluent Icons` 或矢量资源提供工具栏图标。

`CommunityToolkit.Mvvm`、Mapsui、SQLite 和 xUnit 在对应职责真正出现时再引入，避免第一步保留未使用依赖。

验证：

- [x] Windows 下能够启动开发版本。
- [x] 启动时不出现控制台、浏览器或本地服务器窗口。
- [x] 窗口默认 `1440 × 900`，最小 `1180 × 720`。
- [x] 四个详情标签可以正常切换。
- [x] 关闭窗口后后台进程正常退出。

本步骤只包含页面外壳和基础 ViewModel 状态，没有领域解析或事件合并逻辑，因此未建立单元测试工程。验证采用 Release 构建、原生窗口启动、Windows UI Automation 交互和截图检查；单元测试从步骤 3/4 的可测试领域逻辑开始加入。

### 2. 准备固定测试数据

状态：已于 `0.3.0` 实现并验证。

建立测试样本目录，至少包含：

- 同一事件的 `VXSE51`。
- 同一事件的 `VXSE52`。
- 同一事件的 `VXSE53`。
- 一份订正报。
- 一份字段缺失报文。
- 一份包含多个区域和观测点的详情报文。
- 一份震源或观测点无坐标的报文。

同时准备：

- 简化后的 JMA 区域 GeoJSON。
- JMA 观测点编码和坐标表。
- 震度颜色及排序定义。

验证：

- [x] 样本可以离线加载。
- [x] 每份样本都有明确来源、SHA-256 和预期解析结果。
- [x] 官方 `VXSE51/52/53` 使用同一 `EventID`，详情报包含多个区域和观测点。
- [x] 订正、震级不明、震源无坐标和观测点坐标无法补齐均有独立夹具。
- [x] GIS 测试文件和观测点表无需运行时下载。

固定数据位于 `tests/TestData/`，执行 `python tools\validate_test_data.py` 可离线校验。`Gis/jma-area-test-envelopes.geojson` 是由官方观测点坐标生成的测试包络，不是 JMA 官方区域边界；它只用于步骤 2 的编码匹配和图层加载测试，正式地图边界仍在步骤 8 从 JMA GIS 数据生成并验证。

### 3. 定义前后端统一数据模型

状态：已于 `0.4.0` 实现并验证。

先定义领域模型，再写页面组件。核心模型至少包括：

```text
EarthquakeEvent
EarthquakeReport
Hypocenter
IntensityArea
IntensityMunicipality
IntensityStation
SourceReference
SourceStatus
```

震度不能直接使用普通数字，建议定义明确枚举：

```text
unknown, 1, 2, 3, 4, 5-lower, 5-upper, 6-lower, 6-upper, 7
```

所有时间保存为带时区 ISO 8601 字符串，显示时再转换。

验证：

- [x] 未知震度使用 `JmaIntensity.Unknown`，不会变成震度 `1` 或普通数字 `0`。
- [x] 深度使用 `int?`，`0 km` 与深度未知可以区分。
- [x] 震级未知使用 `null`，`NaN` 和无穷值不能进入领域模型。
- [x] 经纬度必须是合法范围内的有限值，观测点坐标可以为未知。
- [x] 内存时间使用 `DateTimeOffset` 保留原始偏移量，后续持久化使用 ISO 8601 `O` 格式。
- [x] 原始报文代码、来源 ID、来源消息 ID、原文 URL 和 `SourcePayload` 不会因标准化而丢失。

模型位于 `src/EarthquakeShow.Core/Models/`，测试位于 `tests/EarthquakeShow.Core.Tests/`。本步骤只定义数据契约，不解析固定 XML，也不检查事件内报文顺序；解析属于数据源适配器，合并和排序属于步骤 4。

### 4. 实现事件合并与报文排序

状态：已于 `0.5.0` 实现并验证。

实现纯函数形式的事件归并逻辑：

- 按 `EventID` 聚合报文。
- 按发布时间、报次和接收时间稳定排序。
- 新报更新已有事件，不新增重复列表项。
- 旧报不得覆盖新报。
- 订正和取消保留之前的版本。
- 根据最新有效报文生成列表摘要。

实际实现规则：

- 先检查 `EventId`、`SourceId` 和 `SourceMessageId`，空标识直接拒绝。
- 按 `EventId` 聚合，使用 `SourceId + SourceMessageId` 识别同一来源消息。
- 同一来源消息重复到达时保留最早的 `ReceivedAt` 记录。
- 事件内按 `IssuedAt → Serial → ReceivedAt → SourceId → SourceMessageId` 升序排列；无报次排在有报次之前。
- 事件列表按最后报文 `IssuedAt` 降序排列，时间相同时按 `EventId` 稳定排列。
- `LatestReport` 表示当前最后一报，取消报可以成为当前状态。
- `LatestEffectiveReport` 只接受发布或订正报；取消后的摘要继续沿用最后有效报文的震源、震级和最大震度。
- 订正和取消报均保留在不可变时间线中，不覆盖或删除原报。

这部分应先写测试，再接 UI。

验证：

- 输入 `VXSE51 → 52 → 53` 后只有一个事件。
- 打乱输入顺序后结果仍然一致。
- 重复输入同一报文不会生成重复时间线节点。
- 订正报可以更新最新值，但旧值仍可查看。

验证结果：上述场景以及取消报、旧报、同时间排序、多事件排序、空输入、空时间线和无效标识均已有单元测试；Core 测试共 `46` 个，全部通过。

### 5. 建立页面状态和数据访问边界

状态：已于 `0.6.0` 实现并验证。

建议将状态分成：

- 事件列表。
- 当前选中事件。
- 当前查看的报文版本。
- 搜索和排序条件。
- 地图视图状态。
- 数据源状态。
- 加载、离线和错误状态。

页面不应直接调用 HTTP 或 SQLite。建立一个简单的数据仓库接口：

```text
listEvents()
getEvent(eventId)
subscribeUpdates()
refresh()
```

开发初期由内存中的固定样本仓储实现，后期替换为 Infrastructure 项目中的 SQLite 和数据源适配器。

实际实现：

- `IEarthquakeEventRepository` 提供异步列表、按 ID 查询、更新通知和主动刷新边界。
- `InMemoryEarthquakeEventRepository` 接收标准化报文，统一复用事件合并服务，并发布不可变完整快照。
- `EarthquakePageState` 分离事件列表、当前事件、当前报文、搜索、排序、地图、来源、加载、刷新、离线和错误状态。
- `EarthquakePageViewModel` 首次加载默认选择最新事件和最后一报；仓储更新时保持用户当前事件和报文版本，新事件不会打断历史浏览。
- 主窗口在首次显示后异步初始化页面状态，关闭时解除仓储订阅。
- 当前内存仓储默认为空，不在解析器实现前向界面注入合成事件。

验证：

- [x] 页面只依赖仓储接口，不直接访问 HTTP 或 SQLite。
- [x] 初次加载、刷新、读取失败和空仓储具有明确状态。
- [x] 选择事件后默认查看该事件最后一报，并可选择旧报或返回最新报。
- [x] 收到新事件或当前事件新报时，已有事件和报文选择保持不变。
- [x] 搜索、排序、地图、来源和离线状态可以独立保存；筛选和地图行为留到对应步骤实现。
- [x] Core 50 个测试和 App 6 个测试全部通过，共 56 个。

### 6. 实现主窗口三栏布局

状态：已于 `0.7.0` 实现并验证。

先完成无地图业务逻辑的稳定布局：

- 顶部：时间、数据状态、最后接收时间、搜索和设置。
- 左侧：业务导航和地震事件列表。
- 中间：地图容器。
- 右侧：摘要、区域与观测点、时间线、原始数据。
- 底部：网络、缓存、来源和地图数据版本。

验证：

- 动态文本不会改变地图尺寸。
- 窄窗口下右侧栏可以收为抽屉。
- 无事件时地图和数据状态仍然可见。

实际实现：

- 顶部状态栏使用固定轨道显示应用名、总体数据状态、最后接收时间、搜索/设置图标和 JST 时钟，动态文字不会改变主内容列宽。
- 左侧事件区根据页面状态显示加载、读取失败、空数据或事件已就绪状态，事件数量来自不可变事件快照。
- 中间地图容器在无事件、无 GIS 数据时仍保持可见，地图工具继续保持禁用直到步骤 8。
- 右侧四个详情标签绑定当前事件显示投影；窗口宽度低于 `1280 px` 时，固定详情列收起并改为可打开、关闭的覆盖式抽屉。
- 底部状态栏绑定网络、缓存、当前来源、地图数据和应用版本，各字段限制宽度并使用省略显示。
- 搜索和设置图标已进入稳定布局，但对应功能未实现，因此保持禁用。

验证结果：

- [x] `1440 × 900` 下详情栏固定为 `380 px`，地图使用剩余空间。
- [x] `1180 × 720` 下详情列不占宽度，抽屉可以通过 UI Automation 打开和关闭。
- [x] 空事件状态下地图、总体数据状态、最后接收和底部状态栏保持可见。
- [x] 加载、错误、在线、离线、事件数量、来源和最后接收文字由显示投影单元测试覆盖。
- [x] Core 50 个测试和 App 15 个测试全部通过，共 65 个。

### 7. 实现地震事件列表

状态：已于 `0.8.0` 实现并验证。

拆分为 WPF `UserControl` 和 ViewModel：

```text
EarthquakeEventListView / EarthquakeEventListViewModel
EarthquakeEventItemView
EarthquakeFilterView / EarthquakeFilterViewModel
IntensityBadgeControl
EventStatusBadgeControl
```

实现：

- 最近事件和历史记录混合浏览。
- 按发生时间、发布时间、最大震度排序。
- 按时间、震度、震级、区域和来源筛选。
- 单击选择事件。
- 键盘上下键切换。
- 新事件只显示标记，不打断历史浏览。
- 加载更早的本地缓存。

验证：

- 100 条事件下滚动流畅。
- 事件更新后列表项原地更新。
- 当前选择不会因列表更新丢失。
- 筛选无结果时显示明确空状态。

实际实现：

- 新增 `EarthquakeEventListView`、`EarthquakeEventListViewModel` 和 `EarthquakeEventListItemViewModel`；没有为单次使用的卡片、筛选和徽章额外拆分空壳控件。
- 列表使用 WPF `ListBox + VirtualizingStackPanel` 回收容器，事件卡片显示最大震度、震源、震级、发生/发布时间、报文状态、海啸摘要和新事件标记。
- 默认按最新发布时间排序，可切换最新发生和最大震度；未知发生时间或震度排在已知值后，所有排序最终使用 `EventId` 稳定兜底。
- 搜索匹配事件 ID、震源名称/代码、报文代码和来源 ID；筛选支持最近 24 小时/7 天/30 天、最低震度、最低震级、区域和稳定来源 ID。
- 搜索和筛选即时生效；左栏使用可折叠筛选区，在当前单业务页面中减少额外覆盖层和筛选草稿状态。
- 单击和键盘上下键使用 `ListBox` 原生选择，`Enter` 聚焦详情；选择统一调用 `EarthquakePageViewModel.SelectEvent()`。
- 仓储快照更新后按 `EventId` 恢复选择，新事件标为“新”但不抢走历史浏览；用户选择该事件后清除标记。
- 刷新按钮调用现有 `RefreshAsync()`，刷新未完成时禁止重复请求；顶部搜索按钮聚焦左栏搜索框。
- 当前仓储契约一次返回全部事件，没有“更早一页”的游标。列表展示仓储提供的全部本地事件；加载更早缓存随步骤 10 的 SQLite 分页契约实现。

验证结果：

- [x] 100 条事件可以生成稳定列表源，XAML 已启用回收式虚拟化。
- [x] 三种排序、搜索字段、五类筛选、多条件组合和未知值行为有单元测试。
- [x] 仓储更新后保持当前选择，新事件只增加标记。
- [x] 空仓储与筛选无结果显示不同状态，结果数量显示“匹配 / 全部”。
- [x] 刷新期间防止重复触发。
- [x] Core 50 个测试和 App 34 个测试全部通过，共 84 个。

### 8. 实现日本地图

状态：已于 `0.9.0` 实现并验证。

按以下顺序添加图层：

1. 日本轮廓和 JMA 区域边界。
2. 区域最大观测震度配色。
3. 震源标记。
4. 观测点标记。
5. 图例和地图工具栏。

不要一开始接在线底图。先用随应用打包的简化 GeoJSON，保证离线可用。

验证：

- 切换事件时旧图层被正确清理。
- 区域颜色来自观测结果，不根据震级推测。
- 无坐标观测点只显示在右侧列表。
- 重叠观测点能够弹出选择列表。
- 日本全图、定位震源、缩放和全屏按钮有效。

实际实现：

- 新增 `Assets/japan-overview.geojson`，作为随应用复制的离线示意轮廓；资源元数据明确 `officialBoundary=false`，不把它当作 JMA 行政或预报区域边界。
- 新增 `OfflineMapGeometry`，使用 `System.Text.Json` 解析 `Polygon`/`MultiPolygon` 的外环和 `EPSG:4326` 经纬度坐标；不支持的几何类型直接报格式错误。
- 新增 `EarthquakeMapViewModel`，订阅页面状态并将当前事件的有效报文投影为震度区域、震源和观测点图层。区域按 `areaCode` 与本地几何匹配；无坐标观测点不绘制。
- 新增 `EarthquakeMapView`，使用 WPF 原生 `Canvas` 绘制离线轮廓、区域填充和点标记；颜色来自 `JmaIntensity`，未知震度使用中性颜色。
- 地图工具包括放大、缩小、日本全图、定位所选事件和跟随选择；地图状态仍通过 `EarthquakePageState.Map` 保存，不创建第二份事件选择。
- 没有选择事件时仍绘制离线轮廓；选择事件但没有任何可绘制坐标时显示明确提示，详情仍可保留无坐标记录。
- 本步不引入 Mapsui、在线底图、GIS 空间判断或真实 JMA 边界；这些能力需要正式边界资源和性能/许可证验证后再决定。

验证结果：

- [x] 离线 GeoJSON 资源随 Release 输出复制，应用启动不访问网络。
- [x] GeoJSON 元数据、Polygon/不支持几何、区域匹配、震源/观测点坐标过滤和未知震度行为有单元测试。
- [x] 空事件仍显示离线轮廓，事件选择后地图 ViewModel 与页面选择同步。
- [x] 放大、缩小、复位、定位所选事件和跟随选择状态有单元测试。
- [x] Core 50 个测试和 App 39 个测试全部通过，共 89 个。
- [x] 原生窗口在 `1440 × 900` 和 `1180 × 720` 下完成地图轮廓、工具栏和紧凑布局检查。

### 8A. 接入固定 JMAXML 解析与真实样本

状态：已于 `0.10.0` 实现并验证。

本步骤是地图与右侧详情之间的数据前置里程碑，不改变原有 UI 步骤编号：

- 使用 `System.Xml.Linq` 将官方 `VXSE51/52/53` 映射为统一领域报文。
- 使用观测点代码表补齐 75 个观测点坐标，无法匹配时保留无坐标记录。
- 解析订正和缺字段夹具，验证未知值、报次和原始负载保留规则。
- 将官方三报文和订正报随 App 复制并注入内存仓储；缺字段夹具只用于测试。
- 同一 `EventID` 合并为一个事件，列表和地图消费同一页面选择状态。

验证结果：

- [x] 三类官方报文、订正报和缺字段行为有解析单元测试。
- [x] 官方三报文合并为单一事件，最新官方报文为 `VXSE53`。
- [x] App 输出包含固定 XML 和站点 CSV，启动不访问网络。
- [x] XML DTD、必填字段、来源身份和非法坐标边界有测试。
- [x] Core 61 个测试和 App 40 个测试全部通过，共 101 个。

### 9. 实现右侧详情和报文时间线

状态：已于 `0.11.0` 实现并验证。

四个标签依次实现：

- 摘要。
- 区域与观测点。
- 报文时间线。
- 原始数据。

时间线切换报文时，地图和详情都必须显示该版本的快照，而不是继续使用最新值。

验证：

- [x] `上一报/下一报/返回最新` 和时间线直接选择行为正确，边界处不循环。
- [x] 观测点列表支持搜索、最高震度筛选；选择有坐标观测点后定位地图，无坐标项仍保留。
- [x] 未知字段统一显示“不明”，订正报显示相对上一报的关键字段变化。
- [x] 原始报文只读，支持复制和自动换行，不执行其中的链接或代码。
- [x] 历史回放尚未实现，因此不显示“进入历史回放”按钮。

实际实现：

- `EarthquakeDetailsViewModel` 只消费 `EarthquakePageState.ViewedReport`，摘要、观测列表、时间线和原始 XML 不复制事件选择状态。
- `EarthquakeMapViewModel` 同样消费 `ViewedReport`；`FocusLocation()` 只改变地图局部焦点，不修改当前事件或报文选择。
- 详情通过独立 WPF `EarthquakeDetailsView` 承载，紧凑窗口继续复用现有详情抽屉，不引入浏览器或外部 UI 框架。
- 当前未实现来源链接打开和标准化 JSON 导出，因为固定样本没有来源 URI；该能力留给数据源/缓存阶段。

验证结果：

- [x] 3 个详情 ViewModel 测试覆盖报文快照、地图联动、时间线边界、观测点定位、筛选和原始负载；另有 1 个 WPF STA 启动测试覆盖详情 XAML 资源加载。
- [x] Core 61 个测试和 App 44 个测试全部通过，共 105 个。

### 10. 实现缓存和离线启动

状态：已于 `0.12.0` 实现并验证。

SQLite 至少需要保存：

- 标准化事件。
- 每份报文版本。
- 原始响应。
- 来源信息。
- 接收时间。
- 当前数据源状态。

当前启动顺序固定为：

```text
创建 SQLite 仓储 → 初始化 schema v1 → 优先加载缓存
缓存为空时写入固定 seed → 设置离线来源状态 → 显示页面
```

验证：

- [x] 断网后重新启动仍可查看缓存事件。
- [x] 页面明确显示离线和缓存状态；缓存失败显示回退原因。
- [x] 数据库损坏、schema 不支持或 JSON 损坏时应用回退固定样本，不直接退出。
- [x] 标准字段、原始 XML、来源身份、接收时间和来源状态均有 SQLite 集成测试。
- [ ] 清除缓存不删除设置（设置按钮留待后续版本）。

实现边界：当前 `RefreshAsync` 不连接网络，缓存读取返回完整事件快照，不提供分页；固定样本是开发期 seed，不作为实时数据源。

### 11. 分阶段接入真实数据源

状态：第十二阶段 WebSocket 消息活性和连接异常统计已于 `0.24.0` 实现并验证；协议级主动心跳、连接轮换、nTool、人工关联确认和历史回放尚未实现。

推荐接入顺序：

1. JMA JSON：先打通列表和详情。（已完成摘要、刷新、缓存和状态）
2. JMA XML：支持 `VXSE51/52/53`，作为权威来源。（已完成 Feed、详情下载、缓存和状态）
3. P2PQuake：用于补充和降级。（已完成 HTTP 列表源、WebSocket 消息边界、重连以及仓储/UI 数据接入）
4. nTool：仅在确有字段补充价值时接入。

每个适配器只负责“获取、解析、转成统一模型”，不得直接操作页面。

`JmaXmlEarthquakeSource` 使用 `https://www.data.jma.go.jp/developer/xml/feed/eqvol.xml` 的 Atom `entry/id` 和 `link[type=application/xml]`，默认最多下载 20 条 `VXSE51/52/53` 详情，并复用 `JmaXmlParser` 和随应用复制的 `JmaStations.csv`。Feed 或单条报文失败只更新 `jma-xml` 来源状态；成功报文与 JSON 摘要一起写入 SQLite，由 `EarthquakeEventMerger` 按来源优先级派生页面快照。

`P2pQuakeEarthquakeSource` 使用 `https://api.p2pquake.net/v2/jma/quake` 的 JSON 列表，映射震源、震级、最大震度和观测点；由于没有 JMA `EventID`，报文以 `p2pquake:{id}` 独立事件身份保存，不做猜测式跨源合并。它是非官方补充/降级源，HTTP 429、网络错误和解析错误只更新 `p2pquake` 来源状态。

验证：

- [x] JMA JSON API 失败不会清空已有内容。
- [x] 相同 `json` 来源消息重复刷新不会生成重复报文。
- [x] HTTP 429、断开和解析错误反映到数据源状态。
- [x] 启动先显示 SQLite 缓存，再依次请求 JSON 摘要和 XML 详情；手动刷新复用同一路径。
- [x] 同一事件、发布时间、报次和接收时间相同时，JMA XML 按来源优先级成为最新报文。
- [x] XML Feed/详情失败不会清空已有内容；单条详情失败不阻止其他条目入库。
- [ ] 来源冲突时显示 JMA 官方 XML 值，并逐字段保留差异。
- [x] P2PQuake HTTP JSON 补充适配器失败不会清空 JMA/SQLite 内容。
- [x] 同一 `EarthquakeEvent` 内显示 JMA JSON/XML 的状态、震度、震级、震源、深度和观测点数量差异。
- [x] 来源差异显示来源 ID、消息 ID和来源角色；P2PQuake 独立事件不参与猜测式比较。
- [x] 对不同事件生成基于时间、坐标和震级阈值的候选关联，并显示中/高置信度及匹配依据。
- [x] 候选关联不合并事件、不改变事件列表和报文时间线。
- [x] 首次网络刷新后自动按 60 秒在线间隔检查所有 HTTP 来源。
- [x] 普通失败和限流分别使用指数退避，最大等待 15 分钟；窗口关闭时取消刷新循环。
- [x] 自动刷新和手动刷新使用异步互斥，避免并发写入缓存。
- [x] P2PQuake WebSocket 单连接文本消息边界、分片重组、解析失败恢复和取消行为有 Infrastructure 测试。
- [x] P2PQuake WebSocket 按单连接顺序重连，连续失败指数退避，在线后重置失败次数，取消可中断等待。
- [x] P2PQuake WebSocket 结果串行写入 SQLite，发布页面事件和独立 `p2pquake-ws` 通道状态。
- [x] P2PQuake WebSocket 重连等待发布连续失败次数和下次重试时间，页面状态栏按 JST 显示。
- [x] P2PQuake WebSocket 显示当前/最近连接持续时间和最近错误详情，主窗口时钟每秒刷新展示。
- [x] P2PQuake WebSocket 显示最近消息活性和累计连接异常次数；解析失败与正常 Close 不计入连接异常。
- [ ] P2PQuake WebSocket 主动心跳、连接轮换和 nTool。

### 12. 完成页面级验证

至少覆盖：

- 事件合并单元测试。
- 震度排序和显示测试。
- 订正、重复报文和字段缺失测试。
- 列表选择与筛选 ViewModel 测试。
- 地图切换事件后的图层清理测试。
- 离线缓存集成测试。
- `1440 × 900`、`1180 × 720` 和高 DPI 截图检查。
- 键盘操作和文字溢出检查。

## 推荐目录

```text
EarthquakeShow.sln
src/
  EarthquakeShow.App/             # WPF 窗口、页面、控件和资源
  EarthquakeShow.Core/            # 领域模型、事件合并和接口
  EarthquakeShow.Infrastructure/  # API 适配器、SQLite 和系统服务
tests/
  EarthquakeShow.Core.Tests/
  EarthquakeShow.Infrastructure.Tests/
tools/                             # GIS 和测试样本预处理脚本
```

最合理的第一个开发里程碑是做到第 7 步：桌面外壳能够使用固定样本完整展示事件列表和选择状态。之后再加入地图，能显著降低同时调试数据、GIS 和布局的复杂度。
